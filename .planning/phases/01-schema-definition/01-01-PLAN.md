---
phase: 01-schema-definition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/schema/device.schema.json
  - specs/schema/command-group.schema.json
  - validate_schemas.py
autonomous: true
requirements:
  - SCH-01
must_haves:
  truths:
    - "specs/schema/device.schema.json 파일이 존재하고 기존 specs/devices/*.yaml 12개가 모두 유효성을 통과한다"
    - "specs/schema/command-group.schema.json 파일이 존재하고 기존 specs/commands/*.yaml 10개가 모두 유효성을 통과한다"
    - "command-group.schema.json에 meta: 블록 필드(id, name, category, description, requires, conflicts)가 optional로 정의되어 있다"
    - "validate_schemas.py 실행 시 모든 YAML 파일이 OK로 통과하고 'All schemas valid.' 메시지가 출력된다"
  artifacts:
    - path: "specs/schema/device.schema.json"
      provides: "장치 YAML 구조 계약 (core strict + ui flexible)"
      contains: "\"$schema\": \"http://json-schema.org/draft-07/schema#\""
    - path: "specs/schema/command-group.schema.json"
      provides: "커맨드 그룹 YAML 구조 계약 (patternProperties + optional meta)"
      contains: "\"patternProperties\""
    - path: "validate_schemas.py"
      provides: "YAML 전체 검증 스크립트"
      exports: ["validate_all"]
  key_links:
    - from: "validate_schemas.py"
      to: "specs/schema/device.schema.json"
      via: "json.load"
      pattern: "device.schema.json"
    - from: "validate_schemas.py"
      to: "specs/schema/command-group.schema.json"
      via: "json.load"
      pattern: "command-group.schema.json"
    - from: "validate_schemas.py"
      to: "specs/devices/*.yaml"
      via: "jsonschema.validate"
      pattern: "devices.*glob"
    - from: "validate_schemas.py"
      to: "specs/commands/*.yaml"
      via: "jsonschema.validate"
      pattern: "commands.*glob"
---

<objective>
Phase 1 산출물인 JSON Schema 2개와 검증 스크립트를 생성한다.

Purpose: 장치 YAML과 커맨드 그룹 YAML의 구조를 JSON Schema로 명문화하여 Phase 2(meta: 블록 추가), Phase 3(loader 연동)의 데이터 계약을 확립한다. 잘못된 YAML이 런타임 오류로 발견되는 대신 스키마 검증 단계에서 조기에 탐지된다.

Output:
- specs/schema/device.schema.json — 장치 YAML 구조 계약 (core strict + ui flexible)
- specs/schema/command-group.schema.json — 커맨드 그룹 YAML 구조 계약 (patternProperties + optional meta)
- validate_schemas.py — 기존 YAML 22개 파일 전체 검증 스크립트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-definition/01-CONTEXT.md
@.planning/phases/01-schema-definition/01-RESEARCH.md
@.planning/phases/01-schema-definition/01-VALIDATION.md

<interfaces>
<!-- 이 Plan이 생성하는 파일들의 핵심 구조. 이후 Phase에서 참조한다. -->

device.schema.json 핵심 구조:
- $schema: "http://json-schema.org/draft-07/schema#"
- type: object, additionalProperties: false (core strict)
- required: ["name", "family", "channels", "command_groups"]
- family enum: ["one_port", "security", "two_port", "security_two_port"]
- channels: integer, minimum 1, maximum 2
- command_groups items enum: ["base", "ddns", "gpio", "modbus", "pppoe", "retransmit", "security", "telnet", "two_port", "w55rp20_ext"]
- overrides, fw_constraints, ui: additionalProperties: true (ui flexible)

command-group.schema.json 핵심 구조:
- $schema: "http://json-schema.org/draft-07/schema#"
- type: object, additionalProperties: false
- properties.meta: optional, additionalProperties: false, 6개 필드
- patternProperties "^[A-Z0-9]{2}$": 커맨드 코드 항목
  - required: ["description", "regex", "values", "access"]
  - access enum: ["RO", "RW", "WO"]
  - ui: oneOf [null, {type: object, additionalProperties: true}]

validate_schemas.py 인터페이스:
- validate_all() 함수: 실패 시 SystemExit(1), 성공 시 "All schemas valid." 출력
- 실행: python validate_schemas.py (또는 uv run python validate_schemas.py)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: device.schema.json 작성</name>
  <files>specs/schema/device.schema.json</files>
  <read_first>
    - specs/devices/WIZ750SR.yaml (대표 device YAML — 필드 구조 확인)
    - specs/devices/WIZ107SR.yaml (fw_constraints 특수 필드 확인)
    - specs/devices/W55RP20-S2E.yaml (overrides 혼합 타입 확인)
    - .planning/phases/01-schema-definition/01-RESEARCH.md (Pattern 1, Pitfall 2·5 참조)
    - .planning/phases/01-schema-definition/01-CONTEXT.md (Decision 2 — core strict + ui flexible)
  </read_first>
  <action>
specs/schema/ 디렉토리를 생성하고 device.schema.json을 작성한다. 반드시 RESEARCH.md Pattern 1을 그대로 구현한다.

정확한 파일 내용:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WIZnet Device Spec",
  "description": "Schema for specs/devices/*.yaml files. Core fields are strict; ui internals are flexible.",
  "type": "object",
  "required": ["name", "family", "channels", "command_groups"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Canonical device name (e.g. WIZ750SR)"
    },
    "display_name": {
      "type": "string",
      "description": "Human-readable display name"
    },
    "aliases": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Alternative names for device matching"
    },
    "family": {
      "type": "string",
      "enum": ["one_port", "security", "two_port", "security_two_port"],
      "description": "Device family — determines available features"
    },
    "channels": {
      "type": "integer",
      "minimum": 1,
      "maximum": 2,
      "description": "Number of serial channels"
    },
    "search_cmd_order": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Command codes sent during device search, in order"
    },
    "command_groups": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["base", "ddns", "gpio", "modbus", "pppoe", "retransmit",
                 "security", "telnet", "two_port", "w55rp20_ext"]
      },
      "minItems": 1,
      "description": "Command group names. Must match specs/commands/*.yaml filenames."
    },
    "overrides": {
      "type": "object",
      "additionalProperties": true,
      "description": "Per-command overrides. Values may be plain strings or {value, min_version} objects."
    },
    "fw_constraints": {
      "type": "object",
      "additionalProperties": true,
      "description": "Firmware upload constraints. Fields vary per device (WIZ107/108SR have extra fields)."
    },
    "ui": {
      "type": "object",
      "additionalProperties": true,
      "description": "UI layout hints. Internal structure is flexible to allow editor evolution."
    }
  }
}
```

**주의사항:**
- `additionalProperties: false`가 최상위에 있으므로 위 properties에 없는 필드를 가진 device YAML이 있으면 검증 실패한다. 스크립트 실행 후 실패가 나오면 해당 필드를 properties에 추가해야 한다.
- `overrides`와 `fw_constraints`는 반드시 `additionalProperties: true` — WIZ107SR.yaml의 hw_version_field 등 특수 필드 허용 필요 (Pitfall 5).
- `channels`는 `"type": "integer"` — YAML에서 숫자로 파싱됨.
  </action>
  <verify>
    <automated>python -c "import json; s=json.load(open('specs/schema/device.schema.json')); assert s['$schema']=='http://json-schema.org/draft-07/schema#'; assert 'name' in s['required']; assert 'family' in s['required']; assert 'channels' in s['required']; assert 'command_groups' in s['required']; assert s['additionalProperties']==False; assert set(s['properties']['family']['enum'])=={'one_port','security','two_port','security_two_port'}; print('device.schema.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `specs/schema/device.schema.json` 파일이 존재한다
    - `grep -c "one_port" specs/schema/device.schema.json` 결과가 1 이상
    - `grep -c "security_two_port" specs/schema/device.schema.json` 결과가 1 이상
    - `grep -c "w55rp20_ext" specs/schema/device.schema.json` 결과가 1 이상
    - `grep "additionalProperties" specs/schema/device.schema.json` 에 `false`와 `true` 모두 나타남 (core strict + ui flexible)
    - Python 검증 명령이 "device.schema.json OK"를 출력하며 종료 코드 0
  </acceptance_criteria>
  <done>specs/schema/device.schema.json이 존재하고, required 4개 필드, family enum 4개 값, command_groups enum 10개 값, additionalProperties:false(최상위)/true(overrides, fw_constraints, ui)가 모두 포함되어 있다.</done>
</task>

<task type="auto">
  <name>Task 2: command-group.schema.json 작성</name>
  <files>specs/schema/command-group.schema.json</files>
  <read_first>
    - specs/commands/base.yaml (ui:null 케이스 확인 — SV, RT, FR, EX 커맨드)
    - specs/commands/security.yaml (ui:null 케이스 확인 — UF 커맨드, password:true)
    - .planning/phases/01-schema-definition/01-RESEARCH.md (Pattern 2, Pitfall 1·3 참조)
    - .planning/phases/01-schema-definition/01-CONTEXT.md (Decision 3 — meta optional, Decision 2 — ui flexible)
  </read_first>
  <action>
specs/schema/command-group.schema.json을 작성한다. RESEARCH.md Pattern 2를 그대로 구현한다.

정확한 파일 내용:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WIZnet Command Group",
  "description": "Schema for specs/commands/*.yaml files. Command codes are dynamic keys matched by patternProperties.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "meta": {
      "type": "object",
      "additionalProperties": false,
      "description": "Module metadata (optional in Phase 1, populated in Phase 2).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique module identifier (e.g. 'base', 'ddns')"
        },
        "name": {
          "type": "string",
          "description": "Human-readable module name"
        },
        "category": {
          "type": "string",
          "description": "Module category for editor grouping (e.g. 'network', 'security')"
        },
        "description": {
          "type": "string",
          "description": "Brief description of the module's purpose"
        },
        "requires": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Other module IDs this module depends on"
        },
        "conflicts": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Other module IDs that cannot be used with this module"
        }
      }
    }
  },
  "patternProperties": {
    "^[A-Z0-9]{2}$": {
      "type": "object",
      "required": ["description", "regex", "values", "access"],
      "additionalProperties": false,
      "description": "A single command entry keyed by its 2-character command code.",
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable command description"
        },
        "regex": {
          "type": "string",
          "description": "Validation regex for command value. Empty string means no validation."
        },
        "values": {
          "type": "object",
          "description": "Allowed value map. Empty object ({}) for commands with no predefined values."
        },
        "access": {
          "type": "string",
          "enum": ["RO", "RW", "WO"],
          "description": "Command access mode"
        },
        "ui": {
          "oneOf": [
            {"type": "null"},
            {
              "type": "object",
              "additionalProperties": true,
              "description": "UI hints. null for WO-only commands (SV, RT, FR, EX, UF). Internal fields are flexible."
            }
          ],
          "description": "UI layout hints. null is valid for write-only commands."
        }
      }
    }
  }
}
```

**반드시 확인할 사항:**
- `patternProperties`의 패턴은 `"^[A-Z0-9]{2}$"` — 영대문자+숫자 2자 조합 (S0, U1 같은 코드 포함)
- `ui` 필드의 `oneOf: [null, object]` — base.yaml의 SV/RT/FR/EX, security.yaml의 UF가 `ui: null`임 (Pitfall 1)
- draft-07에서 `additionalProperties: false`와 `patternProperties` 조합: patternProperties 패턴에 매칭된 키는 additionalProperties 검사에서 제외됨 (Pitfall 3). 즉 `meta` (properties에 명시) + 커맨드 코드들 (patternProperties 매칭) 모두 허용됨.
- `meta`는 `required`에 포함하지 않는다 (optional — Phase 2에서 추가 예정).
  </action>
  <verify>
    <automated>python -c "import json; s=json.load(open('specs/schema/command-group.schema.json')); assert 'meta' in s['properties']; meta=s['properties']['meta']; assert set(meta['properties'].keys())=={'id','name','category','description','requires','conflicts'}; assert '^[A-Z0-9]{2}$' in s['patternProperties']; cmd=s['patternProperties']['^[A-Z0-9]{2}$']; assert set(cmd['required'])=={'description','regex','values','access'}; print('command-group.schema.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `specs/schema/command-group.schema.json` 파일이 존재한다
    - `grep -c "patternProperties" specs/schema/command-group.schema.json` 결과가 1 이상
    - `grep -c "meta" specs/schema/command-group.schema.json` 결과가 1 이상
    - `grep -c "oneOf" specs/schema/command-group.schema.json` 결과가 1 이상 (ui null 허용)
    - `grep -c "A-Z0-9" specs/schema/command-group.schema.json` 결과가 1 이상 (커맨드 코드 패턴)
    - meta.properties에 id, name, category, description, requires, conflicts 6개 필드 모두 존재
    - access enum에 "RO", "RW", "WO" 3개 모두 존재
    - Python 검증 명령이 "command-group.schema.json OK"를 출력하며 종료 코드 0
  </acceptance_criteria>
  <done>specs/schema/command-group.schema.json이 존재하고, optional meta 블록(6개 필드), patternProperties로 커맨드 코드 처리, access enum 3개 값, ui null 허용이 모두 포함되어 있다.</done>
</task>

<task type="auto">
  <name>Task 3: validate_schemas.py 작성 및 전체 YAML 검증 통과 확인</name>
  <files>validate_schemas.py</files>
  <read_first>
    - specs/schema/device.schema.json (Task 1 산출물 — 경로 확인)
    - specs/schema/command-group.schema.json (Task 2 산출물 — 경로 확인)
    - .planning/phases/01-schema-definition/01-RESEARCH.md (Pattern 3 — 스크립트 전체 코드)
    - .planning/phases/01-schema-definition/01-VALIDATION.md (검증 명령 확인)
  </read_first>
  <action>
프로젝트 루트에 validate_schemas.py를 작성하고, jsonschema 패키지를 설치한 뒤 실행하여 모든 YAML이 통과하는지 확인한다.

**Step 1: validate_schemas.py 작성**

정확한 파일 내용:

```python
"""
validate_schemas.py — WIZnet DeviceSpec YAML 스키마 검증 스크립트

사용법:
    python validate_schemas.py
    uv run python validate_schemas.py

종료 코드:
    0 — 모든 YAML이 스키마 통과
    1 — 하나 이상의 YAML이 스키마 실패
"""
import json
import sys
from pathlib import Path

import yaml

try:
    import jsonschema
except ImportError:
    print("ERROR: jsonschema 패키지가 필요합니다.")
    print("       pip install jsonschema==4.26.0")
    sys.exit(1)

SPECS_DIR = Path(__file__).parent / "specs"
SCHEMA_DIR = SPECS_DIR / "schema"


def validate_all() -> None:
    device_schema = json.loads((SCHEMA_DIR / "device.schema.json").read_text(encoding="utf-8"))
    cmd_schema = json.loads((SCHEMA_DIR / "command-group.schema.json").read_text(encoding="utf-8"))

    errors: list[str] = []

    print("=== Device YAML ===")
    for f in sorted((SPECS_DIR / "devices").glob("*.yaml")):
        data = yaml.safe_load(f.read_text(encoding="utf-8"))
        try:
            jsonschema.validate(instance=data, schema=device_schema)
            print(f"  OK  {f.name}")
        except jsonschema.ValidationError as e:
            errors.append(f"FAIL {f.name}: {e.message}")
            print(f"  FAIL {f.name}: {e.message}")

    print("\n=== Command Group YAML ===")
    for f in sorted((SPECS_DIR / "commands").glob("*.yaml")):
        data = yaml.safe_load(f.read_text(encoding="utf-8"))
        try:
            jsonschema.validate(instance=data, schema=cmd_schema)
            print(f"  OK  {f.name}")
        except jsonschema.ValidationError as e:
            errors.append(f"FAIL {f.name}: {e.message}")
            print(f"  FAIL {f.name}: {e.message}")

    print()
    if errors:
        print(f"FAILED: {len(errors)} error(s)")
        sys.exit(1)
    print("All schemas valid.")


if __name__ == "__main__":
    validate_all()
```

**Step 2: jsonschema 설치**

```bash
pip install jsonschema==4.26.0
```

(requirements.txt 추가는 Phase 3 착수 전 확인 사항 — CONTEXT.md Decision 1)

**Step 3: 검증 실행**

```bash
python validate_schemas.py
```

**실패 시 처리 방법:**

| 오류 메시지 | 원인 | 수정 방법 |
|------------|------|----------|
| `Additional properties are not allowed ('X' was unexpected)` in device YAML | device.schema.json에 없는 필드 발견 | device.schema.json의 properties에 해당 필드 추가 (additionalProperties: true로) |
| `Additional properties are not allowed ('X' was unexpected)` in command YAML (최상위) | 커맨드 코드가 `^[A-Z0-9]{2}$` 패턴에 안 맞는 경우 | patternProperties 패턴 수정 |
| `'ui' is not valid under any of the given schemas` | ui 필드가 null도 object도 아닌 타입인 경우 | 해당 YAML 파일의 ui 값 직접 확인 |
| `'X' is not one of ['RO', 'RW', 'WO']` | 실제 파일에 다른 access 값 있음 | access enum에 해당 값 추가 |

모든 파일이 `OK`로 통과하고 마지막에 `All schemas valid.`가 출력되어야 Task 완료.
  </action>
  <verify>
    <automated>python validate_schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - `validate_schemas.py` 파일이 프로젝트 루트에 존재한다
    - `python validate_schemas.py` 실행 종료 코드가 0
    - `python validate_schemas.py` 출력에 "All schemas valid." 텍스트가 포함된다
    - 출력에 "FAIL"로 시작하는 줄이 없다
    - 출력에 "OK  WIZ750SR.yaml", "OK  base.yaml" 등이 포함된다 (12개 device + 10개 command = 22개 OK)
    - `grep -c "OK" $(python validate_schemas.py 2>&1)` — OK 카운트가 22 (또는 출력에 22개 OK 라인 존재)
  </acceptance_criteria>
  <done>validate_schemas.py 실행 시 specs/devices/*.yaml 12개, specs/commands/*.yaml 10개 총 22개가 모두 OK로 통과하고 "All schemas valid."가 출력된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파일시스템 → Python 스크립트 | validate_schemas.py가 specs/의 YAML 파일을 읽어 파싱 |
| 스키마 파일 → jsonschema | JSON Schema 파일 내용이 검증 엔진에 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | device.schema.json / command-group.schema.json | mitigate | core strict (`additionalProperties: false` + required 필드 명시)로 구조 오류를 검증 단계에서 탐지. 잘못된 YAML이 런타임까지 도달하지 않음. |
| T-1-02 | Denial of Service (스키마 과잉 제한) | command-group.schema.json `ui` 필드 | mitigate | ui 내부를 `additionalProperties: true` (flexible)로 정의. 에디터가 새 ui 힌트 필드 추가 시 스키마 재수정 없이 통과. |
| T-1-03 | Tampering | validate_schemas.py 실행 경로 | accept | 로컬 개발 도구. 검증 스크립트 자체가 수정되어도 제품 코드(device_spec_loader.py)에는 영향 없음. Phase 3에서 loader에 통합 시 재평가. |
| T-1-04 | Information Disclosure | yaml.safe_load() | accept | PyYAML safe_load() 사용 — arbitrary code execution 방지. YAML 파일은 프로젝트 내부 파일이므로 외부 입력 아님. |
| T-1-05 | Spoofing | command_groups enum | mitigate | command_groups 항목을 `enum: ["base", "ddns", ...]`로 제한. 존재하지 않는 group 이름은 스키마에서 즉시 거부. |
</threat_model>

<verification>
## Phase 1 전체 검증

1. specs/schema/ 디렉토리 존재 확인:
   ```
   ls specs/schema/
   ```
   예상 출력: `command-group.schema.json  device.schema.json`

2. 스키마 구조 확인:
   ```
   python -c "import json; d=json.load(open('specs/schema/device.schema.json')); c=json.load(open('specs/schema/command-group.schema.json')); print('device required:', d['required']); print('cmd meta fields:', list(c['properties']['meta']['properties'].keys()))"
   ```
   예상 출력:
   ```
   device required: ['name', 'family', 'channels', 'command_groups']
   cmd meta fields: ['id', 'name', 'category', 'description', 'requires', 'conflicts']
   ```

3. 전체 YAML 검증:
   ```
   python validate_schemas.py
   ```
   예상 출력: 22개 OK + "All schemas valid."

4. meta 블록 optional 확인 (현재 YAML에 meta 없어도 통과):
   ```
   python -c "
   import json, jsonschema, yaml
   schema = json.load(open('specs/schema/command-group.schema.json'))
   data = yaml.safe_load(open('specs/commands/base.yaml').read())
   jsonschema.validate(instance=data, schema=schema)
   print('meta optional: PASS')
   "
   ```
</verification>

<success_criteria>
- [ ] `specs/schema/device.schema.json` 존재, core strict + ui flexible 구조
- [ ] `specs/schema/command-group.schema.json` 존재, optional meta(6필드), patternProperties, access enum, ui null 허용
- [ ] `validate_schemas.py` 존재, 실행 시 종료 코드 0, "All schemas valid." 출력
- [ ] specs/devices/*.yaml 12개 전체 OK 통과 (수정 없이)
- [ ] specs/commands/*.yaml 10개 전체 OK 통과 (수정 없이)
- [ ] SCH-01 요구사항 완전 충족
</success_criteria>

<output>
완료 후 `.planning/phases/01-schema-definition/01-01-SUMMARY.md`를 생성한다.

포함 내용:
- 생성된 파일 목록 및 경로
- 스키마 구조 요약 (device: 필수 4개 필드, command: patternProperties + optional meta)
- validate_schemas.py 실행 결과 (22개 OK 확인)
- Phase 2에서 사용할 계약 요약 (meta 블록 6개 필드 명세)
- Phase 3에서 참조할 스키마 파일 경로
</output>
