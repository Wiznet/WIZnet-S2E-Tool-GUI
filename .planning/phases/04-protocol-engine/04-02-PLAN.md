---
phase: "04-protocol-engine"
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-00"
  - "04-01"
files_modified:
  - WIZ550Profile.py
  - tests/test_wiz550_profile.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03

must_haves:
  truths:
    - "WIZ550Profile.py가 존재하며 parse_sr/build_sr, parse_s2e/build_s2e, parse_web/build_web 6개 공개 함수를 export한다 (D-02)"
    - "parse_sr(162B) → build_sr(dict) → 원본과 동일한 bytes 왕복 검증이 통과한다 (PROF-01)"
    - "parse_s2e가 데이터 길이 + fw_ver[1] 홀짝으로 base/modbus/mqtt를 판별한다 (D-04, PROF-02)"
    - "parse_web(133B) → build_web(dict) → 원본과 동일한 bytes 왕복 검증이 통과한다 (PROF-03)"
    - "WEB 구조체 반환 dict에 pw_connect 키가 없거나 빈 문자열이다 (Pitfall 6)"
    - "assert struct.calcsize(SR_FORMAT) == 162 와 assert struct.calcsize(WEB_FORMAT) == 133 이 import 시점에 통과한다"
    - "uv run pytest tests/test_wiz550_profile.py tests/test_wiz550_handler.py -x 가 전부 PASS한다"
  artifacts:
    - path: "WIZ550Profile.py"
      provides: "SR/S2E/WEB 구조체 파서/빌더"
      exports:
        - parse_sr
        - build_sr
        - parse_s2e
        - build_s2e
        - parse_web
        - build_web
        - SR_SIZE
        - WEB_SIZE
        - SR_FORMAT
        - WEB_FORMAT
    - path: "tests/test_wiz550_profile.py"
      provides: "PROF-01~03 왕복 테스트 PASS 상태"
  key_links:
    - from: "WIZ550MSGHandler.py::_parse_get_info_reply"
      to: "WIZ550Profile::parse_sr/parse_s2e/parse_web"
      via: "from WIZ550Profile import parse_sr, parse_s2e, parse_web"
      pattern: "from WIZ550Profile import"
    - from: "parse_s2e"
      to: "_parse_base_162"
      via: "내부 헬퍼 호출 (D-02)"
      pattern: "_parse_base_162"
---

<objective>
WIZ550Profile.py 구현 — SR/S2E/WEB 3종 Config 구조체 파서/빌더 완성.
Wave 2 완료 후 test_wiz550_handler.py의 test_get_info_length_parse도 완전 PASS한다.

Purpose: Phase 6(GUI 통합)에서 main_gui.py가 이 모듈을 import하여
         장치 응답 bytes를 dict로 변환하고 UI 필드를 채운다.
Output: WIZ550Profile.py (루트 디렉토리, WIZ1x0Profile.py와 동일 위치)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-protocol-engine/04-CONTEXT.md
@.planning/phases/04-protocol-engine/04-RESEARCH.md
@.planning/phases/04-protocol-engine/04-VALIDATION.md

<interfaces>
<!-- WIZ1x0Profile.py에서 직접 이식할 패턴 -->

WIZ1x0Profile.py 핵심 패턴:
```python
# 1. struct 포맷 + assert 패턴 (import 시점 크기 검증)
STRUCT_FORMAT = ('>' '6s' 'B' '4s' ...)
BOARD_INFO_SIZE = 159
assert struct.calcsize(STRUCT_FORMAT) == BOARD_INFO_SIZE

# 2. parse 함수: try-except struct.error → None
try:
    fields = struct.unpack(STRUCT_FORMAT, raw)
except struct.error:
    return None

# 3. build 함수: assert len(raw) == SIZE 후 반환
raw = struct.pack(STRUCT_FORMAT, ...)
assert len(raw) == BOARD_INFO_SIZE
return b'SETT' + raw

# 4. IP 유틸: bytes ↔ str
def ip_bytes_to_str(ip4: bytes) -> str:
    return '.'.join(str(b) for b in ip4)
def ip_str_to_bytes(ip_str: str) -> bytes:
    return ipaddress.IPv4Address(ip_str.strip()).packed

# 5. cstr 유틸
def cstr_to_str(raw: bytes) -> str:
    return raw.rstrip(b'\x00').decode('ascii', errors='replace')
def str_to_cstr(s: str, length: int) -> bytes:
    return s.encode('ascii', errors='replace')[:length].ljust(length, b'\x00')
```

04-RESEARCH.md Pattern 5 구조체 포맷 (검증 완료):
```
SR_FORMAT (162B):
  '<H3s25s3s6s4s4s4sBB4sHHHHHB4sBBIBBBB10s10sBB4s50sBs3s'
  = packet_size(2) + module_type(3) + module_name(25) + fw_ver(3) + mac(6)
  + local_ip(4) + gw(4) + sn(4) + working_mode(1) + state(1)
  + remote_ip(4) + local_port(2) + remote_port(2) + inactivity(2)
  + reconnection(2) + packing_time(2) + packing_size(1) + packing_delimiter(4)
  + packing_delimiter_length(1) + packing_data_appendix(1)
  + baud_rate(4) + data_bits(1) + parity(1) + stop_bits(1) + flow_control(1)
  + pw_setting(10) + pw_connect(10) + dhcp_use(1) + dns_use(1)
  + dns_server_ip(4) + dns_domain_name(50) + serial_command(1) + serial_trigger(3)
  = 162B [VERIFIED]

WEB_FORMAT (133B):
  '<H3s25s3s6s4s4s4sIBBBBIBBBB10sBB4s50s'
  = packet_size(2) + module_type(3) + module_name(25) + fw_ver(3) + mac(6)
  + local_ip(4) + gw(4) + sn(4)
  + uart0_baud_rate(4) + uart0_data_bits(1) + uart0_parity(1) + uart0_stop_bits(1) + uart0_flow_control(1)
  + uart1_baud_rate(4) + uart1_data_bits(1) + uart1_parity(1) + uart1_stop_bits(1) + uart1_flow_control(1)
  + pw_setting(10) + dhcp_use(1) + dns_use(1) + dns_server_ip(4) + dns_domain_name(50)
  = 133B [VERIFIED]

MQTT_FORMAT: '<10s10s25s25s' = 70B [확인 필요: 10+10+25+25=70]
MODBUS_FORMAT: '<BB' = 2B
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SR 162B + WEB 133B 파서/빌더 구현</name>
  <files>WIZ550Profile.py</files>
  <read_first>
    - WIZ1x0Profile.py (전체 — struct 포맷, assert calcsize, parse/build 함수 쌍, IP/cstr 유틸)
    - .planning/phases/04-protocol-engine/04-RESEARCH.md (Pattern 5 struct 포맷, Pattern 6 _parse_base_162, Pitfall 3 signed/unsigned, Pitfall 6 WEB pw_connect 없음)
    - tests/test_wiz550_profile.py (test_sr_roundtrip, test_web_roundtrip 기대값 확인)
    - tests/conftest.py (sr_bytes, web_bytes 픽스처 — 오프셋 구조 확인)
  </read_first>
  <behavior>
    - test_sr_parse_returns_dict(sr_bytes): parse_sr() → dict with device_type='WIZ550SR', 'mac', 'local_ip', 'baud_rate', '_proto'='wiz550'
    - test_sr_roundtrip(sr_bytes): parse_sr(sr_bytes) → build_sr(parsed) == sr_bytes[:162]
    - test_sr_parse_too_short: parse_sr(b'\x00'*100) → {} (빈 dict, 예외 없음)
    - test_web_parse_returns_dict(web_bytes): parse_web() → dict with device_type='WIZ550WEB', 'uart0_baud_rate', 'uart1_baud_rate'; pw_connect 키 없거나 ''
    - test_web_roundtrip(web_bytes): parse_web(web_bytes) → build_web(parsed) == web_bytes[:133]
  </behavior>
  <action>
`WIZ550Profile.py` 파일을 루트 디렉토리에 생성한다.

**1. 파일 헤더 및 import:**
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
WIZ550Profile.py — WIZ550SR/S2E/WEB Config 구조체 변환

3종 장치 Config bytes ↔ Python dict 변환.
Java 원본: WIZ550SR_Config.java / WIZ550S2E_Config.java / WIZ550WEB_Config.java

공개 함수:
  parse_sr(data) / build_sr(d)   — SR 162B
  parse_s2e(data) / build_s2e(d) — S2E 162~232B 가변
  parse_web(data) / build_web(d) — WEB 133B

내부 헬퍼:
  _parse_base_162(data) — SR과 S2E 공유 기본 구조 (D-02)

주의:
  - baud_rate: 'I' (unsigned int 4B LE) — Pitfall 3
  - local_port/remote_port: 'H' (unsigned short 2B LE) — Pitfall 3
  - WEB에는 pw_connect 없음 — Pitfall 6
"""

import struct
import ipaddress
```

**2. struct 포맷 + 크기 assert (PROF-01, PROF-03, import 시점 검증):**
```python
# ─────────────────────────────────────────────────────────────
# WIZ550SR 162B (module_type = [0x02, 0x00, 0x00])
# 04-RESEARCH.md Pattern 5 기준 [VERIFIED: struct.calcsize = 162]
# ─────────────────────────────────────────────────────────────
SR_FORMAT = (
    '<'
    'H'    # packet_size LE [0~1]
    '3s'   # module_type[3] [2~4]
    '25s'  # module_name[25] [5~29]
    '3s'   # fw_ver[3] [30~32]
    '6s'   # mac[6] [33~38]
    '4s'   # local_ip[4] [39~42]
    '4s'   # gateway[4] [43~46]
    '4s'   # subnet[4] [47~50]
    'B'    # working_mode [51]
    'B'    # state [52]
    '4s'   # remote_ip[4] [53~56]
    'H'    # local_port LE [57~58]
    'H'    # remote_port LE [59~60]
    'H'    # inactivity LE [61~62]
    'H'    # reconnection LE [63~64]
    'H'    # packing_time LE [65~66]
    'B'    # packing_size [67]
    '4s'   # packing_delimiter[4] [68~71]
    'B'    # packing_delimiter_length [72]
    'B'    # packing_data_appendix [73]
    'I'    # baud_rate 4B LE [74~77]  ← 'I' (unsigned) Pitfall 3
    'B'    # data_bits [78]
    'B'    # parity [79]
    'B'    # stop_bits [80]
    'B'    # flow_control [81]
    '10s'  # pw_setting[10] [82~91]
    '10s'  # pw_connect[10] [92~101]
    'B'    # dhcp_use [102]
    'B'    # dns_use [103]
    '4s'   # dns_server_ip[4] [104~107]
    '50s'  # dns_domain_name[50] [108~157]
    'B'    # serial_command [158]
    '3s'   # serial_trigger[3] [159~161]
)
SR_SIZE = 162
assert struct.calcsize(SR_FORMAT) == SR_SIZE, \
    f"SR struct 크기 오류: {struct.calcsize(SR_FORMAT)} != {SR_SIZE}"

# ─────────────────────────────────────────────────────────────
# WIZ550WEB 133B (module_type = [0x01, 0x02, 0x00])
# ─────────────────────────────────────────────────────────────
WEB_FORMAT = (
    '<'
    'H'    # packet_size LE [0~1]
    '3s'   # module_type[3] [2~4]
    '25s'  # module_name[25] [5~29]
    '3s'   # fw_ver[3] [30~32]
    '6s'   # mac[6] [33~38]
    '4s'   # local_ip[4] [39~42]
    '4s'   # gateway[4] [43~46]
    '4s'   # subnet[4] [47~50]
    'I'    # uart0_baud_rate 4B LE [51~54]  ← 'I' (unsigned)
    'B'    # uart0_data_bits [55]
    'B'    # uart0_parity [56]
    'B'    # uart0_stop_bits [57]
    'B'    # uart0_flow_control [58]
    'I'    # uart1_baud_rate 4B LE [59~62]
    'B'    # uart1_data_bits [63]
    'B'    # uart1_parity [64]
    'B'    # uart1_stop_bits [65]
    'B'    # uart1_flow_control [66]
    '10s'  # pw_setting[10] [67~76]
    'B'    # dhcp_use [77]
    'B'    # dns_use [78]
    '4s'   # dns_server_ip[4] [79~82]
    '50s'  # dns_domain_name[50] [83~132]
)
WEB_SIZE = 133
assert struct.calcsize(WEB_FORMAT) == WEB_SIZE, \
    f"WEB struct 크기 오류: {struct.calcsize(WEB_FORMAT)} != {WEB_SIZE}"

# ─────────────────────────────────────────────────────────────
# WIZ550S2E 확장 포맷
# ─────────────────────────────────────────────────────────────
S2E_BASE_SIZE = SR_SIZE  # 기본 162B — SR과 동일 구조

MQTT_FORMAT   = '<10s10s25s25s'   # mqtt_user(10)+mqtt_pw(10)+pub_topic(25)+sub_topic(25)
MQTT_SIZE     = 70
assert struct.calcsize(MQTT_FORMAT) == MQTT_SIZE, \
    f"MQTT struct 크기 오류: {struct.calcsize(MQTT_FORMAT)} != {MQTT_SIZE}"

MODBUS_FORMAT = '<BB'              # modbus_use(1) + modbus_mode(1)
MODBUS_SIZE   = 2
assert struct.calcsize(MODBUS_FORMAT) == MODBUS_SIZE
```

**3. 유틸리티 함수:**
```python
# ─────────────────────────────────────────────────────────────
# 유틸리티
# ─────────────────────────────────────────────────────────────
def _ip_bytes_to_str(ip4: bytes) -> str:
    return '.'.join(str(b) for b in ip4)


def _ip_str_to_bytes(ip_str: str) -> bytes:
    try:
        return ipaddress.IPv4Address(ip_str.strip()).packed
    except ValueError:
        return b'\x00\x00\x00\x00'


def _cstr_to_str(raw: bytes) -> str:
    return raw.rstrip(b'\x00').decode('ascii', errors='replace')


def _str_to_cstr(s: str, length: int) -> bytes:
    return s.encode('ascii', errors='replace')[:length].ljust(length, b'\x00')
```

**4. _parse_base_162 내부 헬퍼 (D-02, SR/S2E 공유):**
```python
# ─────────────────────────────────────────────────────────────
# 내부 헬퍼 (D-02: SR과 S2E 공유)
# ─────────────────────────────────────────────────────────────
def _parse_base_162(data: bytes) -> dict:
    """
    SR/S2E 공유 162B 기본 구조 파싱 내부 헬퍼.
    data는 system_info (packet_size[2] 포함, 총 162B).
    len(data) < SR_SIZE → 빈 dict 반환 (예외 없음).
    """
    if len(data) < SR_SIZE:
        return {}
    try:
        fields = struct.unpack(SR_FORMAT, data[:SR_SIZE])
    except struct.error:
        return {}

    (packet_size, module_type, module_name, fw_ver,
     mac, local_ip, gateway, subnet,
     working_mode, state, remote_ip,
     local_port, remote_port, inactivity, reconnection,
     packing_time, packing_size, packing_delimiter,
     packing_delimiter_length, packing_data_appendix,
     baud_rate,
     data_bits, parity, stop_bits, flow_control,
     pw_setting, pw_connect,
     dhcp_use, dns_use, dns_server_ip, dns_domain_name,
     serial_command, serial_trigger) = fields

    return {
        'packet_size':              packet_size,
        'module_type':              module_type.hex(),
        'module_name':              _cstr_to_str(module_name),
        'fw_ver':                   fw_ver,
        'fw_str':                   f'{fw_ver[0]}.{fw_ver[1]}.{fw_ver[2]}',
        'mac':                      ':'.join(f'{b:02X}' for b in mac),
        'local_ip':                 _ip_bytes_to_str(local_ip),
        'gateway':                  _ip_bytes_to_str(gateway),
        'subnet':                   _ip_bytes_to_str(subnet),
        'working_mode':             working_mode,
        'state':                    state,
        'remote_ip':                _ip_bytes_to_str(remote_ip),
        'local_port':               local_port,
        'remote_port':              remote_port,
        'inactivity':               inactivity,
        'reconnection':             reconnection,
        'packing_time':             packing_time,
        'packing_size':             packing_size,
        'packing_delimiter':        packing_delimiter,
        'packing_delimiter_length': packing_delimiter_length,
        'packing_data_appendix':    packing_data_appendix,
        'baud_rate':                baud_rate,
        'data_bits':                data_bits,
        'parity':                   parity,
        'stop_bits':                stop_bits,
        'flow_control':             flow_control,
        'pw_setting':               _cstr_to_str(pw_setting),
        'pw_connect':               _cstr_to_str(pw_connect),
        'dhcp_use':                 dhcp_use,
        'dns_use':                  dns_use,
        'dns_server_ip':            _ip_bytes_to_str(dns_server_ip),
        'dns_domain_name':          _cstr_to_str(dns_domain_name),
        'serial_command':           serial_command,
        'serial_trigger':           serial_trigger,
        '_proto':                   'wiz550',
    }
```

**5. parse_sr / build_sr (PROF-01):**
```python
# ─────────────────────────────────────────────────────────────
# WIZ550SR — 162B
# ─────────────────────────────────────────────────────────────
def parse_sr(data: bytes) -> dict:
    """WIZ550SR 162B → dict. len 부족 시 빈 dict 반환."""
    d = _parse_base_162(data)
    if d:
        d['device_type'] = 'WIZ550SR'
    return d


def build_sr(d: dict) -> bytes:
    """dict → WIZ550SR 162B bytes. 왕복 검증: build_sr(parse_sr(data)) == data[:162]"""
    raw = struct.pack(
        SR_FORMAT,
        d.get('packet_size', SR_SIZE),
        bytes.fromhex(d.get('module_type', '020000'))[:3],
        _str_to_cstr(d.get('module_name', ''), 25),
        d.get('fw_ver', b'\x00\x00\x00')[:3],
        bytes(int(x, 16) for x in d.get('mac', '00:00:00:00:00:00').replace('-', ':').split(':'))[:6],
        _ip_str_to_bytes(d.get('local_ip', '0.0.0.0')),
        _ip_str_to_bytes(d.get('gateway', '0.0.0.0')),
        _ip_str_to_bytes(d.get('subnet', '255.255.255.0')),
        d.get('working_mode', 0),
        d.get('state', 0),
        _ip_str_to_bytes(d.get('remote_ip', '0.0.0.0')),
        d.get('local_port', 0),
        d.get('remote_port', 0),
        d.get('inactivity', 0),
        d.get('reconnection', 0),
        d.get('packing_time', 0),
        d.get('packing_size', 0),
        d.get('packing_delimiter', b'\x00\x00\x00\x00')[:4],
        d.get('packing_delimiter_length', 0),
        d.get('packing_data_appendix', 0),
        d.get('baud_rate', 115200),
        d.get('data_bits', 8),
        d.get('parity', 0),
        d.get('stop_bits', 1),
        d.get('flow_control', 0),
        _str_to_cstr(d.get('pw_setting', ''), 10),
        _str_to_cstr(d.get('pw_connect', ''), 10),
        d.get('dhcp_use', 0),
        d.get('dns_use', 0),
        _ip_str_to_bytes(d.get('dns_server_ip', '0.0.0.0')),
        _str_to_cstr(d.get('dns_domain_name', ''), 50),
        d.get('serial_command', 0),
        d.get('serial_trigger', b'\x00\x00\x00')[:3],
    )
    assert len(raw) == SR_SIZE
    return raw
```

**6. parse_web / build_web (PROF-03):**
```python
# ─────────────────────────────────────────────────────────────
# WIZ550WEB — 133B (pw_connect 없음 — Pitfall 6)
# ─────────────────────────────────────────────────────────────
def parse_web(data: bytes) -> dict:
    """WIZ550WEB 133B → dict. len 부족 시 빈 dict 반환."""
    if len(data) < WEB_SIZE:
        return {}
    try:
        fields = struct.unpack(WEB_FORMAT, data[:WEB_SIZE])
    except struct.error:
        return {}

    (packet_size, module_type, module_name, fw_ver,
     mac, local_ip, gateway, subnet,
     uart0_baud_rate, uart0_data_bits, uart0_parity, uart0_stop_bits, uart0_flow_control,
     uart1_baud_rate, uart1_data_bits, uart1_parity, uart1_stop_bits, uart1_flow_control,
     pw_setting, dhcp_use, dns_use, dns_server_ip, dns_domain_name) = fields

    return {
        'packet_size':          packet_size,
        'module_type':          module_type.hex(),
        'module_name':          _cstr_to_str(module_name),
        'fw_ver':               fw_ver,
        'fw_str':               f'{fw_ver[0]}.{fw_ver[1]}.{fw_ver[2]}',
        'mac':                  ':'.join(f'{b:02X}' for b in mac),
        'local_ip':             _ip_bytes_to_str(local_ip),
        'gateway':              _ip_bytes_to_str(gateway),
        'subnet':               _ip_bytes_to_str(subnet),
        'uart0_baud_rate':      uart0_baud_rate,
        'uart0_data_bits':      uart0_data_bits,
        'uart0_parity':         uart0_parity,
        'uart0_stop_bits':      uart0_stop_bits,
        'uart0_flow_control':   uart0_flow_control,
        'uart1_baud_rate':      uart1_baud_rate,
        'uart1_data_bits':      uart1_data_bits,
        'uart1_parity':         uart1_parity,
        'uart1_stop_bits':      uart1_stop_bits,
        'uart1_flow_control':   uart1_flow_control,
        'pw_setting':           _cstr_to_str(pw_setting),
        # pw_connect 없음 (WEB 구조체 미포함 — Pitfall 6)
        'dhcp_use':             dhcp_use,
        'dns_use':              dns_use,
        'dns_server_ip':        _ip_bytes_to_str(dns_server_ip),
        'dns_domain_name':      _cstr_to_str(dns_domain_name),
        'device_type':          'WIZ550WEB',
        '_proto':               'wiz550',
    }


def build_web(d: dict) -> bytes:
    """dict → WIZ550WEB 133B bytes. 왕복 검증: build_web(parse_web(data)) == data[:133]"""
    raw = struct.pack(
        WEB_FORMAT,
        d.get('packet_size', WEB_SIZE),
        bytes.fromhex(d.get('module_type', '010200'))[:3],
        _str_to_cstr(d.get('module_name', ''), 25),
        d.get('fw_ver', b'\x00\x00\x00')[:3],
        bytes(int(x, 16) for x in d.get('mac', '00:00:00:00:00:00').replace('-', ':').split(':'))[:6],
        _ip_str_to_bytes(d.get('local_ip', '0.0.0.0')),
        _ip_str_to_bytes(d.get('gateway', '0.0.0.0')),
        _ip_str_to_bytes(d.get('subnet', '255.255.255.0')),
        d.get('uart0_baud_rate', 9600),
        d.get('uart0_data_bits', 8),
        d.get('uart0_parity', 0),
        d.get('uart0_stop_bits', 1),
        d.get('uart0_flow_control', 0),
        d.get('uart1_baud_rate', 9600),
        d.get('uart1_data_bits', 8),
        d.get('uart1_parity', 0),
        d.get('uart1_stop_bits', 1),
        d.get('uart1_flow_control', 0),
        _str_to_cstr(d.get('pw_setting', ''), 10),
        d.get('dhcp_use', 0),
        d.get('dns_use', 0),
        _ip_str_to_bytes(d.get('dns_server_ip', '0.0.0.0')),
        _str_to_cstr(d.get('dns_domain_name', ''), 50),
    )
    assert len(raw) == WEB_SIZE
    return raw
```

TDD 순서:
1. 파일 생성 후: `uv run pytest tests/test_wiz550_profile.py::test_sr_parse_returns_dict tests/test_wiz550_profile.py::test_sr_roundtrip tests/test_wiz550_profile.py::test_sr_parse_too_short tests/test_wiz550_profile.py::test_web_parse_returns_dict tests/test_wiz550_profile.py::test_web_roundtrip -v`
2. 실패 시 수정하여 PASS
  </action>
  <verify>
    <automated>cd /d/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && uv run pytest tests/test_wiz550_profile.py::test_sr_parse_returns_dict tests/test_wiz550_profile.py::test_sr_roundtrip tests/test_wiz550_profile.py::test_sr_parse_too_short tests/test_wiz550_profile.py::test_web_parse_returns_dict tests/test_wiz550_profile.py::test_web_roundtrip -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - WIZ550Profile.py 파일 존재: `ls WIZ550Profile.py`
    - SR_FORMAT assert 통과: `uv run python -c "from WIZ550Profile import SR_SIZE; print(SR_SIZE)"` → 162
    - WEB_FORMAT assert 통과: `uv run python -c "from WIZ550Profile import WEB_SIZE; print(WEB_SIZE)"` → 133
    - MQTT_FORMAT assert 통과: `uv run python -c "from WIZ550Profile import MQTT_SIZE; print(MQTT_SIZE)"` → 70
    - parse_sr export: `uv run python -c "from WIZ550Profile import parse_sr, build_sr; print('OK')"` → OK
    - parse_web export: `uv run python -c "from WIZ550Profile import parse_web, build_web; print('OK')"` → OK
    - _parse_base_162 내부 헬퍼: `grep "def _parse_base_162" WIZ550Profile.py` 매칭
    - WEB에 pw_connect 없음: `grep "pw_connect" WIZ550Profile.py` → WEB 함수 내부에 없음(parse_web 반환 dict에 미포함)
    - SR 왕복 테스트: `uv run pytest tests/test_wiz550_profile.py::test_sr_roundtrip -v` → PASSED
    - WEB 왕복 테스트: `uv run pytest tests/test_wiz550_profile.py::test_web_roundtrip -v` → PASSED
  </acceptance_criteria>
  <done>
    - WIZ550Profile.py: SR_FORMAT(162B), WEB_FORMAT(133B), MQTT_FORMAT(70B) assert 통과
    - parse_sr/build_sr: 왕복 테스트 PASS
    - parse_web/build_web: 왕복 테스트 PASS
    - WEB dict에 pw_connect 없음 (Pitfall 6 준수)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: S2E 가변 구조 구현 + 전체 테스트 PASS</name>
  <files>WIZ550Profile.py, tests/test_wiz550_profile.py</files>
  <read_first>
    - WIZ550Profile.py (Task 1에서 생성한 현재 상태 — SR/WEB 함수들 확인)
    - .planning/phases/04-protocol-engine/04-RESEARCH.md (Pattern 6 parse_s2e D-04 이중 판별, Pitfall 5 fw_ver[0] 업데이트 금지)
    - tests/conftest.py (s2e_base_bytes, s2e_modbus_bytes, s2e_mqtt_bytes 픽스처 바이트 구조 확인)
    - tests/test_wiz550_profile.py (test_s2e_base_variant, test_s2e_modbus_variant, test_s2e_mqtt_variant 기대값)
  </read_first>
  <behavior>
    - test_s2e_base_variant(s2e_base_bytes): parse_s2e(162B) → {'s2e_variant':'base', 'device_type':'WIZ550S2E'}
    - test_s2e_modbus_variant(s2e_modbus_bytes): parse_s2e(164B, fw_ver[1]=0) → {'s2e_variant':'modbus', 'modbus_use':1}
    - test_s2e_mqtt_variant(s2e_mqtt_bytes): parse_s2e(232B, fw_ver[1]=1) → {'s2e_variant':'mqtt', 'mqtt_user':'mqttuser'}
    - build_s2e: fw_ver[0]는 원본값 유지 (Pitfall 5 — Java updateFromPanel 주석 처리 동작 재현)
  </behavior>
  <action>
WIZ550Profile.py에 parse_s2e / build_s2e 함수를 추가한다 (파일 끝에 이어서 작성).

**parse_s2e / build_s2e (PROF-02, D-04):**
```python
# ─────────────────────────────────────────────────────────────
# WIZ550S2E — 162~232B 가변 (D-04 이중 판별)
# ─────────────────────────────────────────────────────────────
def parse_s2e(data: bytes) -> dict:
    """
    WIZ550S2E 가변 구조 → dict.

    D-04 이중 판별 (데이터 길이 우선 + fw_ver[1] 홀짝 검증):
      len>=232 AND fw_ver[1]%2!=0 → MQTT 70B 확장 추가 (총 232B)
      len>=164 AND fw_ver[1]%2==0 → Modbus 2B 확장 추가 (총 164B)
      기본 → s2e_variant='base' (162B)

    주의: 데이터 길이가 주방어선 (트런케이션 오파싱 방지).
          fw_ver[1] 홀짝은 검증용 보조 조건.
    """
    d = _parse_base_162(data)
    if not d:
        return {}
    d['device_type'] = 'WIZ550S2E'

    fw_ver = d.get('fw_ver', b'\x00\x00\x00')

    # D-04: 데이터 길이 우선 → fw_ver[1] 홀짝 검증
    if len(data) >= S2E_BASE_SIZE + MQTT_SIZE and (fw_ver[1] % 2 != 0):
        # MQTT 70B 확장 파싱
        try:
            ext = data[S2E_BASE_SIZE:S2E_BASE_SIZE + MQTT_SIZE]
            mqtt_user, mqtt_pw, mqtt_pub, mqtt_sub = struct.unpack(MQTT_FORMAT, ext)
            d['mqtt_user']      = _cstr_to_str(mqtt_user)
            d['mqtt_pw']        = _cstr_to_str(mqtt_pw)
            d['mqtt_pub_topic'] = _cstr_to_str(mqtt_pub)
            d['mqtt_sub_topic'] = _cstr_to_str(mqtt_sub)
            d['s2e_variant']    = 'mqtt'
        except struct.error:
            d['s2e_variant'] = 'base'
    elif len(data) >= S2E_BASE_SIZE + MODBUS_SIZE and (fw_ver[1] % 2 == 0):
        # Modbus 2B 확장 파싱
        try:
            ext = data[S2E_BASE_SIZE:S2E_BASE_SIZE + MODBUS_SIZE]
            modbus_use, modbus_mode = struct.unpack(MODBUS_FORMAT, ext)
            d['modbus_use']  = modbus_use
            d['modbus_mode'] = modbus_mode
            d['s2e_variant'] = 'modbus'
        except struct.error:
            d['s2e_variant'] = 'base'
    else:
        d['s2e_variant'] = 'base'

    return d


def build_s2e(d: dict) -> bytes:
    """
    dict → WIZ550S2E bytes (가변 길이).
    s2e_variant에 따라 162B/164B/232B 반환.

    Pitfall 5: fw_ver[0]는 원본값 유지 (Java updateFromPanel에서 주석 처리됨).
    """
    # 기본 162B 빌드 (build_sr 코드 재사용 — module_type만 S2E로 변경)
    raw = struct.pack(
        SR_FORMAT,
        d.get('packet_size', S2E_BASE_SIZE),
        bytes.fromhex(d.get('module_type', '000000'))[:3],  # S2E: [0x00, 0x00, 0x00]
        _str_to_cstr(d.get('module_name', ''), 25),
        d.get('fw_ver', b'\x00\x00\x00')[:3],  # Pitfall 5: fw_ver[0] 원본값 유지
        bytes(int(x, 16) for x in d.get('mac', '00:00:00:00:00:00').replace('-', ':').split(':'))[:6],
        _ip_str_to_bytes(d.get('local_ip', '0.0.0.0')),
        _ip_str_to_bytes(d.get('gateway', '0.0.0.0')),
        _ip_str_to_bytes(d.get('subnet', '255.255.255.0')),
        d.get('working_mode', 0),
        d.get('state', 0),
        _ip_str_to_bytes(d.get('remote_ip', '0.0.0.0')),
        d.get('local_port', 0),
        d.get('remote_port', 0),
        d.get('inactivity', 0),
        d.get('reconnection', 0),
        d.get('packing_time', 0),
        d.get('packing_size', 0),
        d.get('packing_delimiter', b'\x00\x00\x00\x00')[:4],
        d.get('packing_delimiter_length', 0),
        d.get('packing_data_appendix', 0),
        d.get('baud_rate', 115200),
        d.get('data_bits', 8),
        d.get('parity', 0),
        d.get('stop_bits', 1),
        d.get('flow_control', 0),
        _str_to_cstr(d.get('pw_setting', ''), 10),
        _str_to_cstr(d.get('pw_connect', ''), 10),
        d.get('dhcp_use', 0),
        d.get('dns_use', 0),
        _ip_str_to_bytes(d.get('dns_server_ip', '0.0.0.0')),
        _str_to_cstr(d.get('dns_domain_name', ''), 50),
        d.get('serial_command', 0),
        d.get('serial_trigger', b'\x00\x00\x00')[:3],
    )
    assert len(raw) == S2E_BASE_SIZE

    variant = d.get('s2e_variant', 'base')
    if variant == 'mqtt':
        # MQTT 70B 확장 추가
        ext = struct.pack(
            MQTT_FORMAT,
            _str_to_cstr(d.get('mqtt_user', ''), 10),
            _str_to_cstr(d.get('mqtt_pw', ''), 10),
            _str_to_cstr(d.get('mqtt_pub_topic', ''), 25),
            _str_to_cstr(d.get('mqtt_sub_topic', ''), 25),
        )
        assert len(ext) == MQTT_SIZE
        return raw + ext  # 232B
    elif variant == 'modbus':
        # Modbus 2B 확장 추가
        ext = struct.pack(MODBUS_FORMAT,
                          d.get('modbus_use', 0),
                          d.get('modbus_mode', 0))
        return raw + ext  # 164B
    else:
        return raw  # 162B
```

TDD 순서:
1. parse_s2e/build_s2e 추가 후:
   `uv run pytest tests/test_wiz550_profile.py -v`
2. 전체 PASS 확인
3. 이어서 전체 테스트 스위트 실행:
   `uv run pytest tests/test_wiz550_profile.py tests/test_wiz550_handler.py -v`
4. test_get_info_length_parse 최종 확인 (WIZ550Profile 구현 완료로 full PASS):
   `uv run pytest tests/test_wiz550_handler.py::test_get_info_length_parse -v`
  </action>
  <verify>
    <automated>cd /d/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && uv run pytest tests/test_wiz550_profile.py tests/test_wiz550_handler.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - parse_s2e export: `uv run python -c "from WIZ550Profile import parse_s2e, build_s2e; print('OK')"` → OK
    - S2E base 테스트: `uv run pytest tests/test_wiz550_profile.py::test_s2e_base_variant -v` → PASSED
    - S2E modbus 테스트: `uv run pytest tests/test_wiz550_profile.py::test_s2e_modbus_variant -v` → PASSED
    - S2E mqtt 테스트: `uv run pytest tests/test_wiz550_profile.py::test_s2e_mqtt_variant -v` → PASSED
    - 전체 profile 테스트: `uv run pytest tests/test_wiz550_profile.py -v` → FAILED 0
    - 전체 handler+profile: `uv run pytest tests/ -v` → FAILED 0
    - fw_ver[0] 보존 확인: `grep "Pitfall 5\|fw_ver\[0\]" WIZ550Profile.py` → 주석 존재
    - S2E 162B 기본 변형 확인: `grep "s2e_variant.*base" WIZ550Profile.py` 매칭
  </acceptance_criteria>
  <done>
    - WIZ550Profile.py: parse_s2e/build_s2e 구현 완료
    - s2e_variant: base/modbus/mqtt 3종 판별 (D-04)
    - uv run pytest tests/ -v → 전체 FAILED 0
    - test_get_info_length_parse PASS (WIZ550Profile 연동 완성)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네트워크 bytes → parse_sr/s2e/web | 외부 장치 응답 바이트가 struct.unpack에 진입하는 지점 |
| UI dict → build_sr/s2e/web | 사용자 입력 dict가 struct.pack에 진입하는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | parse_sr/web — struct.unpack | mitigate | `len(data) < SR_SIZE/WEB_SIZE` 체크 후 빈 dict 반환. `try: struct.unpack ... except struct.error: return {}`. |
| T-04-02-02 | Tampering | parse_s2e — 가변 길이 분기 | mitigate | D-04: 데이터 길이가 주방어선. `len(data) >= SIZE + EXT_SIZE` 조건 먼저 체크 후 fw_ver[1] 홀짝 검증. 확장 파싱 실패 시 `s2e_variant='base'` fallback. |
| T-04-02-03 | Tampering | build_sr/web — struct.pack | mitigate | `assert len(raw) == SIZE` — import 시점 struct 크기 검증(assert calcsize) + 빌드 시점 크기 검증(assert len). |
| T-04-02-04 | Denial of Service | struct.unpack — 임의 길이 | mitigate | 최소 크기 사전 체크로 struct.unpack 호출 전 보호. struct.error는 except로 catch. |
| T-04-02-05 | Information Disclosure | pw_setting/pw_connect — ASCII decode | accept | `errors='replace'` 사용. 비정상 바이트는 `?`로 대체. 내부 네트워크 도구이므로 낮은 위협. |
</threat_model>

<verification>
Plan 02 완료 기준:

1. `uv run python -c "from WIZ550Profile import parse_sr, build_sr, parse_s2e, build_s2e, parse_web, build_web, SR_SIZE, WEB_SIZE; print('import OK')"` → import OK
2. struct assert 통과 (import 시점):
   - `uv run python -c "import WIZ550Profile; print('assert OK')"` → assert OK (크기 불일치 시 AssertionError)
3. `uv run pytest tests/test_wiz550_profile.py -v` → 6 PASSED, 0 FAILED
4. `uv run pytest tests/ -v` → 전체 FAILED 0
5. grep 검증:
   - `grep "def _parse_base_162" WIZ550Profile.py` → 매칭 (D-02)
   - `grep "s2e_variant" WIZ550Profile.py` → 3개 이상 (base/modbus/mqtt 분기)
</verification>

<success_criteria>
- WIZ550Profile.py 루트 디렉토리에 존재
- SR_FORMAT(162B), WEB_FORMAT(133B), MQTT_FORMAT(70B) assert 통과
- parse_sr/build_sr SR 162B 왕복 검증 통과 (PROF-01)
- parse_s2e: base(162B)/modbus(164B)/mqtt(232B) 3종 판별 (D-04, PROF-02)
- parse_web/build_web WEB 133B 왕복 검증 통과, pw_connect 없음 (PROF-03)
- _parse_base_162 내부 헬퍼로 SR/S2E 코드 공유 (D-02)
- uv run pytest tests/ -v → FAILED 0 (전체 Phase 4 테스트 통과)
</success_criteria>

<output>
완료 후 `.planning/phases/04-protocol-engine/04-02-SUMMARY.md` 생성
</output>
