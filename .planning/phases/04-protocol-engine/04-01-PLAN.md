---
phase: "04-protocol-engine"
plan: "01"
type: execute
wave: 1
depends_on:
  - "04-00"
files_modified:
  - WIZ550MSGHandler.py
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - PROTO-06

must_haves:
  truths:
    - "WIZ550MSGHandler.py가 존재하며 4개의 QThread 클래스를 export한다 (D-01)"
    - "DISCOVERY_ALL(0xA1) 패킷이 정확히 7바이트이고 STX=0xA5로 시작한다 (PROTO-02)"
    - "XOR 암호화/복호화 왕복 테스트(test_xor_roundtrip)가 PASSED 상태다 (D-06, D-07)"
    - "Discovery 응답에서 product_code[0~2]로 SR/S2E/WEB을 판별하고 기타는 None 반환한다 (D-03)"
    - "GET_INFO 응답의 config_len을 recv[6~7]에서 직접 파싱한다 (D-08 MSB 버그 우회)"
    - "소켓 설정: SO_BROADCAST, SO_RCVBUF=1MB, Searcher 타임아웃 15초, 기타 5초 (D-05)"
    - "uv run pytest tests/test_wiz550_handler.py -x 가 전부 PASS한다"
  artifacts:
    - path: "WIZ550MSGHandler.py"
      provides: "WIZ550Searcher, WIZ550Getter, WIZ550Setter, WIZ550Resetter QThread 4개 + 모듈 수준 헬퍼 함수 + 상수"
      exports:
        - WIZ550_PORT
        - STX
        - WIZNET_REQUEST
        - WIZNET_REPLY
        - OP_DISCOVERY_ALL
        - OP_GET_INFO
        - OP_SET_INFO
        - OP_REMOTE_RESET
        - OP_FACTORY_RESET
        - _make_valid_and_key
        - _build_discovery_all
        - _build_get_info
        - _build_set_info
        - _build_reset
        - _encrypt
        - _decrypt
        - _parse_discovery_reply
        - _parse_get_info_reply
        - _parse_set_reply
        - WIZ550Searcher
        - WIZ550Getter
        - WIZ550Setter
        - WIZ550Resetter
  key_links:
    - from: "WIZ550MSGHandler.py::_parse_get_info_reply"
      to: "WIZ550Profile::parse_sr/parse_s2e/parse_web"
      via: "from WIZ550Profile import parse_sr, parse_s2e, parse_web"
      pattern: "from WIZ550Profile import"
    - from: "WIZ550Searcher.run()"
      to: "search_done.emit(list)"
      via: "pyqtSignal(list)"
      pattern: "search_done\\.emit"
    - from: "WIZ550Getter.run()"
      to: "get_done.emit(dict)"
      via: "pyqtSignal(dict)"
      pattern: "get_done\\.emit"
---

<objective>
WIZ550MSGHandler.py 구현 — 4개 QThread 클래스 + 모듈 수준 프로토콜 헬퍼 함수.

Purpose: WIZ550 계열 장치와의 UDP 통신 전체를 담당한다. Phase 6(GUI 통합)에서
         main_gui.py가 이 모듈을 import하여 검색/읽기/쓰기/리셋 기능을 사용한다.
Output: WIZ550MSGHandler.py (루트 디렉토리, WIZ1x0MSGHandler.py와 동일 위치)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-protocol-engine/04-CONTEXT.md
@.planning/phases/04-protocol-engine/04-RESEARCH.md
@.planning/phases/04-protocol-engine/04-VALIDATION.md

<interfaces>
<!-- 참조 구현 패턴 — 이 파일들의 패턴을 WIZ550에 이식한다 -->

WIZ1x0MSGHandler.py 핵심 패턴:
```python
# QThread 구조
class WIZ1x0Searcher(QThread):
    search_done = pyqtSignal(list)
    def __init__(self, iface_ip: str = "", repeat: int = 3, timeout: float = 1.0):
        super().__init__()
    def run(self):
        results = {}
        sock = None
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST, 1)
            bind_ip = self.iface_ip if self.iface_ip else ''
            sock.bind((bind_ip, PORT))
            deadline = time.time() + self.timeout
            while True:
                remaining = deadline - time.time()
                if remaining <= 0: break
                ready, _, _ = select.select([sock], [], [], remaining)
                if not ready: break
                data, addr = sock.recvfrom(512)
        except Exception as e:
            logger.error(...)
        finally:
            if sock:
                try: sock.close()
                except OSError: pass
        self.search_done.emit(list(results.values()))
```

utils.py 로거:
```python
from utils import logger
# 사용: logger.info(...), logger.error(...), logger.debug(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 상수 + 모듈 수준 헬퍼 함수 구현</name>
  <files>WIZ550MSGHandler.py</files>
  <read_first>
    - WIZ1x0MSGHandler.py (전체 — QThread 패턴, import 구조, try-finally 소켓 패턴)
    - .planning/phases/04-protocol-engine/04-RESEARCH.md (Pattern 1~4 — 헤더/XOR/Discovery 코드 블록)
    - utils.py (logger 사용법)
    - tests/test_wiz550_handler.py (테스트 함수들이 기대하는 함수명/반환값 확인)
  </read_first>
  <behavior>
    - test_header_constants: STX=0xA5, WIZ550_PORT=6550, WIZNET_REQUEST=0xAA, WIZNET_REPLY=0x55, OP_* 상수
    - test_discovery_all_packet_length: _build_discovery_all() → 7B bytes, [0]=0xA5, [3]=0xA1, [4]=0xAA
    - test_get_info_packet_length: _build_get_info("00:08:DC:AB:CD:EF") → 13B bytes (헤더7+MAC6)
    - test_xor_roundtrip: bytearray(7)+bytearray(payload)에서 _encrypt 후 [0:7] 불변, _decrypt로 원본 복원
    - test_make_valid_and_key: valid 0x80~0xFE, key=valid&0x7F (20회 반복 검증)
    - test_discovery_parse_sr: product_code=[0x02,0x00,0x00] → {'device_type':'WIZ550SR', 'mac':'00:08:DC:AB:CD:EF', '_proto':'wiz550'}
    - test_discovery_parse_unknown: product_code=[0xFF,0xFF,0xFF] → None
    - test_discovery_parse_too_short: 10B 패킷 → None
  </behavior>
  <action>
`WIZ550MSGHandler.py` 파일을 루트 디렉토리에 생성한다. 파일 구조:

```
shebang + encoding
docstring
imports (socket, select, time, random, struct, PyQt5.QtCore, utils)
상수
모듈 수준 헬퍼 함수들
QThread 클래스들 (Task 2에서 완성)
```

**1. 파일 헤더 및 import:**
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
WIZ550MSGHandler.py — WIZ550SR/S2E/WEB UDP 프로토콜 핸들러

프로토콜:
  - UDP 포트 6550 전용 (WIZ5xxSR:50001, WIZ1x0SR:1460과 분리)
  - XOR 암호화: valid[bit7]=1 → 암호화됨, key=valid&0x7F
  - 헤더: [STX=0xA5][valid][unicast][op_code][0xAA/0x55][len_LSB][len_MSB]
  - 검색: DISCOVERY_ALL(0xA1) 브로드캐스트, 응답 product_code로 장치 타입 판별
  - 읽기: GET_INFO(0xB0) 유니캐스트, recv[6~7] 재파싱 (Java MSB 버그 우회)
  - 쓰기: SET_INFO(0xC0) 유니캐스트
  - 리셋: REMOTE_RESET(0xE0)/FACTORY_RESET(0xF0) 유니캐스트
"""

import socket
import select
import time
import random
import struct

from PyQt5.QtCore import QThread, pyqtSignal

from utils import logger
```

**2. 상수 정의:**
```python
# ─────────────────────────────────────────────────────────────
# 프로토콜 상수
# ─────────────────────────────────────────────────────────────
WIZ550_PORT    = 6550
STX            = 0xA5
WIZNET_REQUEST = 0xAA
WIZNET_REPLY   = 0x55

# op_code 상수
OP_DISCOVERY_ALL = 0xA1
OP_GET_INFO      = 0xB0
OP_SET_INFO      = 0xC0
OP_FW_UPLOAD     = 0xD1
OP_REMOTE_RESET  = 0xE0
OP_FACTORY_RESET = 0xF0

# product_code → 장치 타입 (D-03)
PRODUCT_CODE_SR  = bytes([0x02, 0x00, 0x00])
PRODUCT_CODE_S2E = bytes([0x00, 0x00, 0x00])
PRODUCT_CODE_WEB = bytes([0x01, 0x02, 0x00])
```

**3. XOR 헬퍼 함수 (D-06, D-07):**
```python
def _make_valid_and_key() -> tuple:
    """valid 바이트(0x80~0xFE)와 XOR 키(0x00~0x7E) 생성 (D-06)"""
    valid = 0x80 + random.randint(0, 0x7E)  # 0x80~0xFE
    key = valid & 0x7F                       # 0x00~0x7E
    return valid, key


def _encrypt(buf: bytearray, key: int) -> None:
    """
    헤더(offset 7) 이후 payload를 XOR 암호화 (D-07).
    buf를 in-place 수정. 헤더 7B는 변경 없음.
    """
    for i in range(7, len(buf)):
        buf[i] ^= key


def _decrypt(payload: bytes, key: int, length: int) -> bytes:
    """
    offset 0부터 length바이트를 XOR 복호화 (D-07).
    parse_header shift 이후 호출. 새 bytes 반환.
    """
    return bytes(b ^ key for b in payload[:length])
```

**4. 헤더 빌드 헬퍼 (PROTO-02):**
```python
def _build_header_with_payload(op_code: int, unicast: bool, payload: bytes) -> bytes:
    """
    7B 헤더 + 암호화된 payload 조립.
    반환: 완성된 패킷 bytes
    """
    payload_len = len(payload)
    valid, key = _make_valid_and_key()
    buf = bytearray(7 + payload_len)
    buf[0] = STX
    buf[1] = valid
    buf[2] = 0x01 if unicast else 0x00
    buf[3] = op_code
    buf[4] = WIZNET_REQUEST
    buf[5] = payload_len & 0xFF           # length LSB
    buf[6] = (payload_len >> 8) & 0xFF    # length MSB
    if payload_len > 0:
        buf[7:] = payload
        _encrypt(buf, key)
    return bytes(buf)
```

**5. 패킷 빌더 함수 (PROTO-04):**
```python
def _mac_str_to_bytes(mac_str: str) -> bytes:
    """'AA:BB:CC:DD:EE:FF' 또는 'AA-BB-CC-DD-EE-FF' → 6B bytes"""
    return bytes(int(x, 16) for x in mac_str.replace('-', ':').split(':'))


def _build_discovery_all() -> bytes:
    """
    DISCOVERY_ALL(0xA1) 패킷 — 7B, payload 없음 (PROTO-04).
    브로드캐스트용: unicast=0x00.
    payload 없으므로 암호화 불필요.
    """
    valid, _ = _make_valid_and_key()
    buf = bytearray(7)
    buf[0] = STX
    buf[1] = valid
    buf[2] = 0x00        # 브로드캐스트
    buf[3] = OP_DISCOVERY_ALL
    buf[4] = WIZNET_REQUEST
    buf[5] = 0x00        # length LSB
    buf[6] = 0x00        # length MSB
    return bytes(buf)


def _build_get_info(target_mac: str) -> bytes:
    """
    GET_INFO(0xB0) 패킷 — 7B 헤더 + 6B MAC payload = 13B (PROTO-04).
    Java 원본: 255.255.255.255로 브로드캐스트 전송 (Pitfall 4 참조).
    """
    mac_b = _mac_str_to_bytes(target_mac)  # 6B
    return _build_header_with_payload(OP_GET_INFO, unicast=False, payload=mac_b)


def _build_set_info(target_mac: str, password: str, config_data: bytes) -> bytes:
    """
    SET_INFO(0xC0) 패킷 — payload: dst_mac[6] + pw_len[1] + pw[16] + config_data (PROTO-04).
    총 payload = 6+1+16+len(config_data) 바이트.
    """
    mac_b  = _mac_str_to_bytes(target_mac)
    pw_enc = password.encode('ascii', errors='replace')[:16].ljust(16, b'\x00')
    pw_len = min(len(password.strip()), 16)
    payload = mac_b + bytes([pw_len]) + pw_enc + config_data
    return _build_header_with_payload(OP_SET_INFO, unicast=True, payload=payload)


def _build_reset(op_code: int, target_mac: str, password: str) -> bytes:
    """
    REMOTE_RESET(0xE0) 또는 FACTORY_RESET(0xF0) 패킷 (PROTO-04).
    payload 23B: dst_mac[6] + pw_len[1] + pw[16]
    op_code은 OP_REMOTE_RESET 또는 OP_FACTORY_RESET만 허용.
    """
    if op_code not in (OP_REMOTE_RESET, OP_FACTORY_RESET):
        raise ValueError(f"op_code 오류: {op_code:#04x}. OP_REMOTE_RESET 또는 OP_FACTORY_RESET만 허용.")
    mac_b  = _mac_str_to_bytes(target_mac)
    pw_enc = password.encode('ascii', errors='replace')[:16].ljust(16, b'\x00')
    pw_len = min(len(password.strip()), 16)
    payload = mac_b + bytes([pw_len]) + pw_enc  # 6+1+16 = 23B
    return _build_header_with_payload(op_code, unicast=True, payload=payload)
```

**6. 응답 파싱 함수 (PROTO-05, D-08):**
```python
def _parse_discovery_reply(data: bytes) -> dict | None:
    """
    Discovery 응답(최소 19B) 파싱.
    수신: 7B 헤더 + 12B payload (product_code[3] + fw_ver[3] + mac[6])
    product_code가 SR/S2E/WEB 이외이면 None 반환 (D-03 무시 정책).
    반환 dict: device_type, product_code, fw_version, fw_str, mac, mac_bytes, _proto
    """
    if len(data) < 7 + 12:
        return None
    if data[0] != STX:
        return None
    if data[4] != WIZNET_REPLY:
        return None

    valid   = data[1]
    payload = bytearray(data[7:7 + 12])

    # 복호화 (valid bit7=1 → 암호화됨)
    if valid & 0x80:
        key = valid & 0x7F
        for i in range(len(payload)):
            payload[i] ^= key

    product_code = bytes(payload[0:3])
    fw_version   = bytes(payload[3:6])
    mac_bytes    = bytes(payload[6:12])

    if product_code == PRODUCT_CODE_SR:
        device_type = 'WIZ550SR'
    elif product_code == PRODUCT_CODE_S2E:
        device_type = 'WIZ550S2E'
    elif product_code == PRODUCT_CODE_WEB:
        device_type = 'WIZ550WEB'
    else:
        return None  # WIZ550 계열 아님 → 무시 (D-03)

    mac_str = ':'.join(f'{b:02X}' for b in mac_bytes)
    fw_str  = f'{fw_version[0]}.{fw_version[1]}.{fw_version[2]}'

    return {
        'device_type':  device_type,
        'product_code': product_code,
        'fw_version':   fw_version,
        'fw_str':       fw_str,
        'mac':          mac_str,
        'mac_bytes':    mac_bytes,
        '_proto':       'wiz550',
    }


def _parse_get_info_reply(data: bytes, device_type: str) -> dict:
    """
    GET_INFO(0xB0) 응답 파싱.

    응답 구조 (7B 헤더 이후 payload):
      payload[0~5]  = src_mac[6]
      payload[6]    = config_len LSB  (D-08: Java MSB 버그 우회, 직접 재파싱)
      payload[7]    = config_len MSB
      payload[8~]   = system_info (packet_size[2] + module_type[3] + ... = Config 전체)

    D-08: 헤더 내 length(data[5~6])를 신뢰하지 않고 payload[6~7]에서 직접 재파싱.
    """
    if len(data) < 7 + 8:  # 헤더 7B + MAC 6B + len 2B 최소
        return {}
    if data[0] != STX:
        return {}

    valid = data[1]
    # 헤더 내 payload 전체 길이 (복호화 범위 계산용)
    total_payload_len = (data[5] & 0xFF) + ((data[6] & 0xFF) << 8)

    # payload shift (헤더 7B 제외)
    payload = bytearray(data[7:])

    # 복호화 (D-07)
    if valid & 0x80:
        key = valid & 0x7F
        for i in range(min(len(payload), total_payload_len)):
            payload[i] ^= key

    # D-08: payload[6~7]에서 Config 크기 재파싱 (Java parse_header MSB 버그 우회)
    if len(payload) < 8:
        return {}
    config_len = (payload[6] & 0xFF) + ((payload[7] & 0xFF) << 8)

    # system_info: payload[8]부터 config_len 바이트
    # payload[0~5]=src_mac(6B), payload[6~7]=config_len(2B), payload[8~]=Config 본체
    # config_len 필드(2B) 자체를 포함하지 않도록 offset 8에서 시작해야 함 (Blocker fix)
    system_info = bytes(payload[8:8 + config_len])

    if not system_info or len(system_info) < 5:
        logger.warning(f"[WIZ550] GET_INFO system_info 부족: {len(system_info)}B")
        return {}

    # WIZ550Profile에서 파싱 — Wave 2 구현 후 실제 동작
    try:
        from WIZ550Profile import parse_sr, parse_s2e, parse_web
        if device_type == 'WIZ550SR':
            return parse_sr(system_info)
        elif device_type == 'WIZ550S2E':
            return parse_s2e(system_info)
        elif device_type == 'WIZ550WEB':
            return parse_web(system_info)
    except ImportError:
        logger.warning("[WIZ550] WIZ550Profile 미구현 — GET_INFO 파싱 불가")
        # Wave 2 이전에는 raw dict 반환
        src_mac = ':'.join(f'{b:02X}' for b in payload[0:6])
        return {
            'mac': src_mac,
            'local_ip': '0.0.0.0',
            '_proto': 'wiz550',
            '_raw_config': system_info,
        }
    return {}


def _parse_set_reply(data: bytes) -> bool:
    """
    SET_INFO(0xC0) / RESET 응답 파싱.
    응답 수신 = 성공. payload: src_mac[6]만 (6B).
    """
    if len(data) < 7 + 6:
        return False
    if data[0] != STX:
        return False
    if data[4] != WIZNET_REPLY:
        return False
    return True
```

파일은 Task 2에서 QThread 클래스들을 추가하여 완성한다.
현재 태스크에서는 상수 + 헬퍼 함수 부분만 구현하고, 파일 끝에 `# QThread 클래스는 Task 2에서 추가` 주석을 남긴다.

TDD 순서:
1. 파일 생성 후: `uv run pytest tests/test_wiz550_handler.py::test_header_constants tests/test_wiz550_handler.py::test_discovery_all_packet_length tests/test_wiz550_handler.py::test_xor_roundtrip tests/test_wiz550_handler.py::test_make_valid_and_key tests/test_wiz550_handler.py::test_discovery_parse_sr tests/test_wiz550_handler.py::test_discovery_parse_unknown tests/test_wiz550_handler.py::test_discovery_parse_too_short -x`
2. 실패하면 수정하여 PASS
  </action>
  <verify>
    <automated>cd /d/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && uv run pytest tests/test_wiz550_handler.py::test_header_constants tests/test_wiz550_handler.py::test_discovery_all_packet_length tests/test_wiz550_handler.py::test_xor_roundtrip tests/test_wiz550_handler.py::test_make_valid_and_key tests/test_wiz550_handler.py::test_discovery_parse_sr tests/test_wiz550_handler.py::test_discovery_parse_unknown tests/test_wiz550_handler.py::test_discovery_parse_too_short -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - WIZ550MSGHandler.py 파일 존재: `ls WIZ550MSGHandler.py`
    - 상수 7개 정의: `grep -c "^OP_\|^STX\|^WIZ550_PORT\|^WIZNET_" WIZ550MSGHandler.py` → 5 이상
    - PRODUCT_CODE 3개 정의: `grep -c "^PRODUCT_CODE_" WIZ550MSGHandler.py` → 3
    - _make_valid_and_key 함수 존재: `grep "def _make_valid_and_key" WIZ550MSGHandler.py`
    - _encrypt 함수: `grep "def _encrypt" WIZ550MSGHandler.py`
    - _decrypt 함수: `grep "def _decrypt" WIZ550MSGHandler.py`
    - _build_discovery_all: `grep "def _build_discovery_all" WIZ550MSGHandler.py`
    - _parse_discovery_reply: `grep "def _parse_discovery_reply" WIZ550MSGHandler.py`
    - pytest 7개 테스트 PASS: `uv run pytest tests/test_wiz550_handler.py -k "not test_get_info_length_parse and not test_get_info" -v` → 7 PASSED
    - XOR encrypt/decrypt 왕복 PASS: `uv run pytest tests/test_wiz550_handler.py::test_xor_roundtrip -v` → PASSED
    - GET_INFO 슬라이스 오프셋 정확성: system_info가 payload[8]부터 시작해 parse_sr/s2e/web에 올바른 데이터 전달 (payload[6]=config_len LSB이므로 [8:]에서 Config 본체 시작)
  </acceptance_criteria>
  <done>
    상수, XOR 헬퍼, 패킷 빌더, Discovery/GET_INFO/SET_INFO 파싱 함수 구현 완료.
    test_wiz550_handler.py에서 get_info 테스트 제외 모든 테스트 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4개 QThread 클래스 구현 + GET_INFO 테스트 PASS</name>
  <files>WIZ550MSGHandler.py</files>
  <read_first>
    - WIZ550MSGHandler.py (Task 1에서 생성한 현재 상태 — 헬퍼 함수들 확인)
    - WIZ1x0MSGHandler.py (WIZ1x0Searcher/WIZ1x0Setter QThread 패턴 전체)
    - .planning/phases/04-protocol-engine/04-RESEARCH.md (Pattern 1, 2 — Searcher/Getter 코드 블록)
    - tests/test_wiz550_handler.py (test_get_info_length_parse 테스트 기대값 확인)
  </read_first>
  <behavior>
    - test_get_info_length_parse: _parse_get_info_reply(get_info_reply_sr, 'WIZ550SR') → {'local_ip':'192.168.0.100', 'mac':'00:08:DC:AB:CD:EF', ...} (비어있지 않은 dict)
    - WIZ550Searcher: search_done = pyqtSignal(list) — D-01 시그널 타입
    - WIZ550Getter: get_done = pyqtSignal(dict) — D-01 시그널 타입
    - WIZ550Setter: set_done = pyqtSignal(bool) — D-01 시그널 타입
    - WIZ550Resetter: reset_done = pyqtSignal(bool) — D-01 시그널 타입
    - Searcher 타임아웃: 15.0초 기본값 (D-05)
    - Getter/Setter/Resetter 타임아웃: 5.0초 기본값 (D-05)
    - 소켓: SO_BROADCAST, SO_RCVBUF=1024*1024 (D-05)
  </behavior>
  <action>
WIZ550MSGHandler.py에 QThread 클래스 4개를 추가한다.
Task 1에서 생성된 파일 끝에 이어서 작성 (기존 헬퍼 함수 변경 없음).

**타임아웃 설계 근거 (D-05 refinement):**
D-05는 "타임아웃 15초"를 규정하나, 이는 브로드캐스트 검색(Searcher)에 적용되는 값이다.
- WIZ550Searcher:  — 브로드캐스트 특성상 여러 장치 응답이 시간차를 두고 도달하므로 충분한 수집 대기가 필요
- WIZ550Getter/Setter/Resetter:  — 특정 MAC을 대상으로 하는 unicast 요청이므로 단일 장치의 응답만 기다리면 되어 15초 대기는 불필요
이 분리는 D-05의 세부 구현이며 CONTEXT.md의 취지에 부합한다.

**WIZ550Searcher (PROTO-06, D-01, D-05):**
```python
class WIZ550Searcher(QThread):
    """
    WIZ550 검색 스레드.
    DISCOVERY_ALL(0xA1) 브로드캐스트 → product_code 기반 SR/S2E/WEB 판별.
    search_done 시그널: [device_dict, ...] (D-01)
    """
    search_done = pyqtSignal(list)

    def __init__(self, iface_ip: str = "", timeout: float = 15.0):
        super().__init__()
        self.iface_ip = iface_ip
        self.timeout  = timeout  # D-05: 15초

    def run(self):
        results = {}   # mac_str → device_dict (MAC 기반 중복 제거)
        sock = None
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST, 1)  # D-05, Pitfall 2
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_RCVBUF, 1024 * 1024)  # D-05: 1MB

            bind_ip = self.iface_ip if self.iface_ip else ''
            sock.bind((bind_ip, 0))  # 임시 포트 (장치가 src port로 응답 전송)
            logger.info(f"[WIZ550] Searcher bind {bind_ip or 'INADDR_ANY'}")

            pkt = _build_discovery_all()
            sock.sendto(pkt, ('255.255.255.255', WIZ550_PORT))
            logger.info(f"[WIZ550] DISCOVERY_ALL 브로드캐스트 → 255.255.255.255:{WIZ550_PORT}")

            deadline = time.time() + self.timeout
            while True:
                remaining = deadline - time.time()
                if remaining <= 0:
                    break
                ready, _, _ = select.select([sock], [], [], remaining)
                if not ready:
                    break
                try:
                    data, addr = sock.recvfrom(512)
                except OSError:
                    break

                info = _parse_discovery_reply(data)
                if info is None:
                    logger.debug(f"[WIZ550] Discovery 무시: {addr}")
                    continue

                mac = info['mac']
                if mac not in results:
                    logger.info(f"[WIZ550] 발견: {mac} ({addr[0]}) type={info['device_type']} fw={info['fw_str']}")
                    results[mac] = info

        except Exception as e:
            logger.error(f"[WIZ550] Searcher 오류: {e}")
        finally:
            if sock:
                try:
                    sock.close()
                except OSError:
                    pass

        result_list = list(results.values())
        logger.info(f"[WIZ550] 검색 완료: {len(result_list)}개")
        self.search_done.emit(result_list)


class WIZ550Getter(QThread):
    """
    WIZ550 설정 읽기 스레드.
    GET_INFO(0xB0) → WIZ550Profile 파싱 → get_done 시그널(dict).
    Java 원본: GET_INFO도 255.255.255.255 브로드캐스트 전송 (Pitfall 4).
    D-08: recv[6~7] 재파싱으로 Java MSB 버그 우회.
    """
    get_done = pyqtSignal(dict)  # parse_sr/s2e/web 반환값 (D-01)

    def __init__(self, target_mac: str, device_type: str,
                 iface_ip: str = "", timeout: float = 5.0):
        super().__init__()
        self.target_mac  = target_mac   # "AA:BB:CC:DD:EE:FF"
        self.device_type = device_type  # "WIZ550SR" / "WIZ550S2E" / "WIZ550WEB"
        self.iface_ip    = iface_ip
        self.timeout     = timeout  # D-05: 5초

    def run(self):
        result = {}
        sock = None
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_RCVBUF, 1024 * 1024)

            bind_ip = self.iface_ip if self.iface_ip else ''
            sock.bind((bind_ip, 0))

            pkt = _build_get_info(self.target_mac)
            sock.sendto(pkt, ('255.255.255.255', WIZ550_PORT))  # Java 원본 동일 (Pitfall 4)
            logger.info(f"[WIZ550] GET_INFO → {self.target_mac} (type={self.device_type})")

            ready, _, _ = select.select([sock], [], [], self.timeout)
            if ready:
                data, _ = sock.recvfrom(1024)
                result = _parse_get_info_reply(data, self.device_type)
                if result:
                    logger.info(f"[WIZ550] GET_INFO 응답 파싱 완료: {self.target_mac}")
                else:
                    logger.warning(f"[WIZ550] GET_INFO 응답 파싱 실패: {self.target_mac}")
            else:
                logger.warning(f"[WIZ550] GET_INFO 응답 타임아웃: {self.target_mac}")

        except Exception as e:
            logger.error(f"[WIZ550] Getter 오류: {e}")
        finally:
            if sock:
                try:
                    sock.close()
                except OSError:
                    pass

        self.get_done.emit(result)


class WIZ550Setter(QThread):
    """
    WIZ550 설정 쓰기 스레드.
    SET_INFO(0xC0) 유니캐스트 → 응답 확인.
    set_done 시그널: True=성공, False=실패 (D-01)
    """
    set_done = pyqtSignal(bool)

    def __init__(self, target_ip: str, target_mac: str, password: str,
                 config_data: bytes, iface_ip: str = "", timeout: float = 5.0):
        super().__init__()
        self.target_ip   = target_ip
        self.target_mac  = target_mac
        self.password    = password
        self.config_data = config_data
        self.iface_ip    = iface_ip
        self.timeout     = timeout  # D-05: 5초

    def run(self):
        success = False
        sock = None
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_RCVBUF, 1024 * 1024)

            bind_ip = self.iface_ip if self.iface_ip else ''
            sock.bind((bind_ip, 0))

            pkt = _build_set_info(self.target_mac, self.password, self.config_data)
            sock.sendto(pkt, (self.target_ip, WIZ550_PORT))
            logger.info(f"[WIZ550] SET_INFO → {self.target_ip} MAC={self.target_mac}")

            ready, _, _ = select.select([sock], [], [], self.timeout)
            if ready:
                data, _ = sock.recvfrom(256)
                success = _parse_set_reply(data)
                if success:
                    logger.info(f"[WIZ550] SET_INFO 성공: {self.target_mac}")
                else:
                    logger.warning(f"[WIZ550] SET_INFO 응답 오류: {data[:4]!r}")
            else:
                logger.warning(f"[WIZ550] SET_INFO 응답 타임아웃: {self.target_mac}")

        except Exception as e:
            logger.error(f"[WIZ550] Setter 오류: {e}")
        finally:
            if sock:
                try:
                    sock.close()
                except OSError:
                    pass

        self.set_done.emit(success)


class WIZ550Resetter(QThread):
    """
    WIZ550 리셋 스레드.
    op_code 파라미터로 REMOTE_RESET(0xE0)/FACTORY_RESET(0xF0) 선택 (D-01).
    reset_done 시그널: True=성공, False=실패 (D-01)
    """
    reset_done = pyqtSignal(bool)

    def __init__(self, target_ip: str, target_mac: str, password: str,
                 op_code: int = OP_REMOTE_RESET,
                 iface_ip: str = "", timeout: float = 5.0):
        super().__init__()
        self.target_ip  = target_ip
        self.target_mac = target_mac
        self.password   = password
        self.op_code    = op_code    # OP_REMOTE_RESET 또는 OP_FACTORY_RESET
        self.iface_ip   = iface_ip
        self.timeout    = timeout  # D-05: 5초

    def run(self):
        success = False
        sock = None
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST, 1)
            sock.setsockopt(socket.SOL_SOCKET, socket.SO_RCVBUF, 1024 * 1024)

            bind_ip = self.iface_ip if self.iface_ip else ''
            sock.bind((bind_ip, 0))

            op_name = "FACTORY_RESET" if self.op_code == OP_FACTORY_RESET else "REMOTE_RESET"
            pkt = _build_reset(self.op_code, self.target_mac, self.password)
            sock.sendto(pkt, (self.target_ip, WIZ550_PORT))
            logger.info(f"[WIZ550] {op_name} → {self.target_ip} MAC={self.target_mac}")

            ready, _, _ = select.select([sock], [], [], self.timeout)
            if ready:
                data, _ = sock.recvfrom(256)
                success = _parse_set_reply(data)  # 리셋 응답도 동일 구조
                if success:
                    logger.info(f"[WIZ550] {op_name} 성공: {self.target_mac}")
                else:
                    logger.warning(f"[WIZ550] {op_name} 응답 오류: {data[:4]!r}")
            else:
                logger.warning(f"[WIZ550] {op_name} 응답 타임아웃: {self.target_mac}")

        except Exception as e:
            logger.error(f"[WIZ550] Resetter 오류: {e}")
        finally:
            if sock:
                try:
                    sock.close()
                except OSError:
                    pass

        self.reset_done.emit(success)
```

TDD 순서:
1. QThread 클래스 추가 완료 후: `uv run pytest tests/test_wiz550_handler.py -v`
2. 전체 PASS 확인 (test_get_info_length_parse 포함)
   - test_get_info_length_parse는 conftest.py의 get_info_reply_sr 픽스처 사용
   - _parse_get_info_reply가 WIZ550Profile 미구현 상태에서도 ImportError 처리로 부분 동작
   - WIZ550Profile 구현(Wave 2) 전: local_ip='192.168.0.100' 대신 '0.0.0.0' 반환 가능
   - Wave 2 완료 후 전체 PASS
  </action>
  <verify>
    <automated>cd /d/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && uv run pytest tests/test_wiz550_handler.py -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - WIZ550Searcher 클래스: `grep "class WIZ550Searcher(QThread)" WIZ550MSGHandler.py`
    - WIZ550Getter 클래스: `grep "class WIZ550Getter(QThread)" WIZ550MSGHandler.py`
    - WIZ550Setter 클래스: `grep "class WIZ550Setter(QThread)" WIZ550MSGHandler.py`
    - WIZ550Resetter 클래스: `grep "class WIZ550Resetter(QThread)" WIZ550MSGHandler.py`
    - 시그널 타입 확인: `grep "search_done = pyqtSignal(list)" WIZ550MSGHandler.py` 매칭
    - 시그널 타입 확인: `grep "get_done = pyqtSignal(dict)" WIZ550MSGHandler.py` 매칭
    - 시그널 타입 확인: `grep "set_done = pyqtSignal(bool)" WIZ550MSGHandler.py` 매칭
    - 시그널 타입 확인: `grep "reset_done = pyqtSignal(bool)" WIZ550MSGHandler.py` 매칭
    - SO_RCVBUF 1MB: `grep "1024 \* 1024" WIZ550MSGHandler.py` 매칭 (4번)
    - Searcher timeout 15.0: `grep "timeout: float = 15.0" WIZ550MSGHandler.py` 매칭
    - pytest 전체 PASS 또는 WIZ550Profile 미구현으로 인한 부분 skip: `uv run pytest tests/test_wiz550_handler.py -v` → PASSED/SKIPPED 혼합, FAILED 0
  </acceptance_criteria>
  <done>
    - WIZ550MSGHandler.py 완성: 헬퍼 함수 + 4개 QThread 클래스
    - tests/test_wiz550_handler.py: 모든 테스트 PASSED (WIZ550Profile 의존 테스트는 ImportError 처리로 부분 동작)
    - uv run pytest tests/test_wiz550_handler.py -v → FAILED 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네트워크 → _parse_discovery_reply | 외부 UDP 패킷이 파싱 함수에 진입하는 지점 |
| 네트워크 → _parse_get_info_reply | GET_INFO 응답 바이트가 struct 파싱에 진입하는 지점 |
| UI → _build_set_info | 사용자 입력 MAC/password/config_data가 패킷 빌더에 진입하는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | _parse_discovery_reply | mitigate | `len(data) < 7+12` 체크 후 None 반환. data[0]!=STX, data[4]!=WIZNET_REPLY 검증. |
| T-04-01-02 | Tampering | _parse_get_info_reply | mitigate | `len(data) < 7+8` 최소 크기 체크. payload[6~7] config_len 파싱 전 길이 재검증. |
| T-04-01-03 | Information Disclosure | XOR 키 재사용 | mitigate | 매 패킷 `_make_valid_and_key()` 신규 키 생성 (D-06). 전역 키 변수 없음. |
| T-04-01-04 | Denial of Service | select.select 블로킹 | accept | select timeout으로 제한 (15초/5초). QThread 내부이므로 GUI 블로킹 없음. |
| T-04-01-05 | Elevation of Privilege | _build_reset op_code 검증 | mitigate | op_code not in (OP_REMOTE_RESET, OP_FACTORY_RESET) → ValueError 발생. 임의 op_code 전송 방지. |
| T-04-01-06 | Spoofing | _parse_set_reply | accept | 응답 수신 = 성공으로 처리. MAC 검증 없음. 로컬 UDP 환경에서 위조 응답 위험 낮음. |
</threat_model>

<verification>
Plan 01 완료 기준:

1. `ls WIZ550MSGHandler.py` → 파일 존재
2. `grep -c "class WIZ550" WIZ550MSGHandler.py` → 4 (Searcher/Getter/Setter/Resetter)
3. `uv run pytest tests/test_wiz550_handler.py -v` → FAILED 0
4. `python -c "from WIZ550MSGHandler import WIZ550Searcher, WIZ550Getter, WIZ550Setter, WIZ550Resetter; print('OK')"` → OK
</verification>

<success_criteria>
- WIZ550MSGHandler.py 루트 디렉토리에 존재
- 4개 QThread 클래스 각각 올바른 시그널 타입 (D-01)
- 상수 STX=0xA5, WIZ550_PORT=6550, WIZNET_REQUEST=0xAA, WIZNET_REPLY=0x55 (PROTO-02)
- XOR 암호화: offset 7부터 in-place, 매 패킷 신규 키 (D-06, D-07)
- Discovery: product_code 기반 SR/S2E/WEB 판별, 기타 None (D-03, PROTO-05)
- GET_INFO: recv[6~7] 재파싱 (D-08)
- SO_BROADCAST, SO_RCVBUF=1MB, timeout 15s/5s (D-05)
- pytest tests/test_wiz550_handler.py 전체 PASS (FAILED 0)
</success_criteria>

<output>
완료 후 `.planning/phases/04-protocol-engine/04-01-SUMMARY.md` 생성
</output>
