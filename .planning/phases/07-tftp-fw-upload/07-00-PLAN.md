---
phase: 07-tftp-fw-upload
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_wiz550_fw.py
autonomous: true
requirements:
  - FW-01
  - FW-02
  - FW-03
  - FW-04

must_haves:
  truths:
    - "tests/test_wiz550_fw.py 파일이 존재하며 pytest 수집에 성공한다"
    - "5개 테스트 함수가 모두 FAILED (not ERRORED) 상태로 실행된다 (RED)"
  artifacts:
    - path: "tests/test_wiz550_fw.py"
      provides: "FW-01~04 테스트 스텁 5개"
      exports:
        - test_build_fw_upload_pkt
        - test_tftp_server_tempdir
        - test_parse_fw_done_reply
        - test_dialog_tabs
        - test_boot_file_rejected
  key_links:
    - from: "tests/test_wiz550_fw.py"
      to: "WIZ550MSGHandler.build_fw_upload_pkt"
      via: "import + call"
      pattern: "from WIZ550MSGHandler import build_fw_upload_pkt"
    - from: "tests/test_wiz550_fw.py"
      to: "wiz550_fw_dialog.WIZ550FWDialog"
      via: "import + instantiation"
      pattern: "from wiz550_fw_dialog import WIZ550FWDialog"
---

<objective>
Wave 0: FW-01~04 테스트 스텁 생성 — RED 상태 확립

Purpose: Nyquist 원칙 준수. 후속 구현 태스크가 GREEN 검증 명령을 즉시 실행 가능하게 한다.
Output: tests/test_wiz550_fw.py — 5개 테스트 함수 (FAILED 상태, 수입 오류 아님)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-tftp-fw-upload/07-VALIDATION.md
@.planning/phases/07-tftp-fw-upload/07-RESEARCH.md
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_wiz550_fw.py 테스트 스텁 작성</name>
  <files>tests/test_wiz550_fw.py</files>

  <read_first>
    - tests/conftest.py — qapp 픽스처 패턴 확인 (WIZ550FWDialog 테스트에 재사용)
    - tests/test_wiz550_gui.py — Phase 6 GUI 테스트 스텁 구조 참조
    - .planning/phases/07-tftp-fw-upload/07-VALIDATION.md — 5개 테스트 함수 명세
    - WIZ550MSGHandler.py — _build_header_with_payload, _mac_str_to_bytes, OP_FW_UPLOAD 시그니처 확인
  </read_first>

  <behavior>
    - test_build_fw_upload_pkt: `build_fw_upload_pkt("00:08:DC:AB:CD:EF", "192.168.0.100", 69, "app.bin", "")` 호출 → pytest.raises(ImportError) 또는 AssertionError (함수 미구현 상태). 실제 구현 후: len(pkt)==86, pkt[0]==0xA5, pkt[3]==0xD1, struct.unpack_from('<H', pkt, 34)[0]==69
    - test_tftp_server_tempdir: tempfile.mkdtemp()으로 디렉토리 생성 후 `tftpy.TftpServer(tftproot=tmpdir)` 생성 성공. listen() 호출 없이 인스턴스만 검증. Wave 0에서는 import만 확인 (tftpy 이미 설치됨)
    - test_parse_fw_done_reply: 7B 헤더 `bytes([0xA5, 0x00, 0x01, 0xD2, 0x55, 0x06, 0x00])` + 6B MAC `bytes([0x00,0x08,0xDC,0xAB,0xCD,0xEF])` = 13B 패킷 생성. `_is_fw_done_reply(pkt)` 호출 → pytest.raises(ImportError) 또는 True
    - test_dialog_tabs: qapp 픽스처 사용. `WIZ550FWDialog(localip_addr="192.168.0.100")` 생성 → pytest.raises(ImportError) 또는 tabWidget.count()==2
    - test_boot_file_rejected: `_is_boot_file("WIZ550SR_BOOT_v1.bin")` 호출 → pytest.raises(ImportError) 또는 True
  </behavior>

  <action>
    tests/test_wiz550_fw.py 신규 파일 생성. 아래 정확한 구조로 작성한다.

    파일 전체 내용:

    ```python
    #!/usr/bin/env python3
    # -*- coding: utf-8 -*-
    """
    tests/test_wiz550_fw.py — Phase 7 TFTP FW Upload 테스트 스텁

    Wave 0: RED 상태. 후속 구현 완료 후 GREEN 전환.
    """

    import struct
    import tempfile
    import os
    import pytest

    import tftpy  # 설치 확인 (pip show tftpy → 0.8.7)


    # ─────────────────────────────────────────────────────────────────
    # FW-02: 0xD1 패킷 빌더 — 86바이트 레이아웃 + server_port LE 검증
    # ─────────────────────────────────────────────────────────────────

    def test_build_fw_upload_pkt():
        """
        build_fw_upload_pkt() → 86B, offset[3]=0xD1, offset[34:36]=LE(69).
        RED: WIZ550MSGHandler에 함수 미구현 상태.
        """
        from WIZ550MSGHandler import build_fw_upload_pkt

        pkt = build_fw_upload_pkt(
            target_mac="00:08:DC:AB:CD:EF",
            server_ip="192.168.0.100",
            server_port=69,
            file_name="app.bin",
            password="",
        )
        assert len(pkt) == 86, f"Expected 86 bytes, got {len(pkt)}"
        assert pkt[0] == 0xA5, "STX mismatch"
        assert pkt[3] == 0xD1, "op_code must be 0xD1 (FW_UPLOAD)"
        port = struct.unpack_from('<H', pkt, 34)[0]
        assert port == 69, f"server_port LE 오류: expected 69, got {port}"


    # ─────────────────────────────────────────────────────────────────
    # FW-01: tftpy TftpServer 초기화 — tmpdir + 인스턴스 생성
    # ─────────────────────────────────────────────────────────────────

    def test_tftp_server_tempdir():
        """
        tempfile.mkdtemp() + TftpServer(tftproot=tmpdir) 생성 성공.
        listen() 호출 없이 객체 생성만 검증.
        """
        tmpdir = tempfile.mkdtemp(prefix='wiz550fw_test_')
        try:
            server = tftpy.TftpServer(tftproot=tmpdir)
            assert server is not None
        finally:
            import shutil
            shutil.rmtree(tmpdir, ignore_errors=True)


    # ─────────────────────────────────────────────────────────────────
    # FW-03: 0xD2 완료 응답 파싱
    # ─────────────────────────────────────────────────────────────────

    def test_parse_fw_done_reply():
        """
        _is_fw_done_reply(data) → True (0xD2/0x55 헤더 인식).
        RED: WIZ550FWUploadThread에 함수 미구현 상태.
        """
        from WIZ550FWUploadThread import _is_fw_done_reply

        # 7B 헤더 (0xD2 응답) + 6B src_mac = 13B
        header = bytes([0xA5, 0x00, 0x01, 0xD2, 0x55, 0x06, 0x00])
        src_mac = bytes([0x00, 0x08, 0xDC, 0xAB, 0xCD, 0xEF])
        pkt = header + src_mac
        assert _is_fw_done_reply(pkt) is True

        # 다른 op_code → False
        not_done = bytes([0xA5, 0x00, 0x01, 0xB0, 0x55, 0x06, 0x00]) + src_mac
        assert _is_fw_done_reply(not_done) is False


    # ─────────────────────────────────────────────────────────────────
    # FW-04: 다이얼로그 탭 2개 존재
    # ─────────────────────────────────────────────────────────────────

    def test_dialog_tabs(qapp):
        """
        WIZ550FWDialog(localip_addr) 생성 → QTabWidget.count() == 2.
        RED: wiz550_fw_dialog.py 미구현 상태.
        """
        from wiz550_fw_dialog import WIZ550FWDialog

        dlg = WIZ550FWDialog(localip_addr="192.168.0.100")
        assert dlg.tab_widget.count() == 2, "탭 2개 필요 (자동/수동)"
        dlg.close()


    # ─────────────────────────────────────────────────────────────────
    # FW-02 보안: BOOT 파일 업로드 거부
    # ─────────────────────────────────────────────────────────────────

    def test_boot_file_rejected():
        """
        파일명에 'BOOT' 포함 시 _is_boot_file() → True (업로드 거부 대상).
        RED: wiz550_fw_dialog.py 미구현 상태.
        """
        from wiz550_fw_dialog import _is_boot_file

        assert _is_boot_file("WIZ550SR_BOOT_v1.bin") is True
        assert _is_boot_file("WIZ550SR_APP_v1.bin") is False
        assert _is_boot_file("boot_upgrade.bin") is True   # 대소문자 무관
        assert _is_boot_file("app.bin") is False
    ```

    테스트 실행하여 RED 확인:
    `uv run pytest tests/test_wiz550_fw.py -x -q 2>&1 | head -30`

    ImportError (모듈 미구현)가 나타나는 것이 정상. ERRORED 상태가 아닌 ImportError는 허용.
    test_tftp_server_tempdir만 PASSED여야 한다 (tftpy 이미 설치됨).
  </action>

  <verify>
    <automated>uv run pytest tests/test_wiz550_fw.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_wiz550_fw.py 파일 존재: `test -f tests/test_wiz550_fw.py`
    - 5개 테스트 함수 정의됨: `grep -c "^def test_" tests/test_wiz550_fw.py` → 5
    - test_tftp_server_tempdir PASSED: `uv run pytest tests/test_wiz550_fw.py::test_tftp_server_tempdir -v` → PASSED
    - 나머지 4개 ImportError (구현 전 예상): FAILED with ImportError 허용
    - tftpy import 성공: 파일 상단 `import tftpy` 라인 존재
  </acceptance_criteria>

  <done>tests/test_wiz550_fw.py 파일이 존재하고, test_tftp_server_tempdir 1개는 PASSED, 나머지 4개는 ImportError로 FAILED. pytest 수집 오류 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 코드 → 구현 모듈 | ImportError는 Wave 0 정상 동작 — 구현 완료 전 미구현 모듈 참조 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-00-01 | Tampering | tests/test_wiz550_fw.py | accept | 테스트 파일. 프로덕션 코드 아님. 위협 없음. |
</threat_model>

<verification>
`uv run pytest tests/test_wiz550_fw.py -v` 실행 후:
- test_tftp_server_tempdir: PASSED
- 나머지 4개: FAILED (ImportError) — 구현 전 정상
- 수집 오류(ERRORS) 0개
</verification>

<success_criteria>
tests/test_wiz550_fw.py 파일 생성 완료. pytest 수집 성공. test_tftp_server_tempdir PASSED, 나머지 4개 FAILED (ImportError).
</success_criteria>

<output>
완료 후 `.planning/phases/07-tftp-fw-upload/07-00-SUMMARY.md` 생성.
</output>
