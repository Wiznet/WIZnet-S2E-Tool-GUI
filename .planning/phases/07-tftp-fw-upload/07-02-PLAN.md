---
phase: 07-tftp-fw-upload
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - wiz550_fw_dialog.py
autonomous: true
requirements:
  - FW-04

must_haves:
  truths:
    - "WIZ550FWDialog 인스턴스 생성 시 QTabWidget이 탭 2개를 갖는다"
    - "Tab 0 텍스트가 '자동 (내장 TFTP)', Tab 1 텍스트가 '수동 (외부 TFTP)'"
    - "수동 탭 Server IP 필드가 localip_addr 값으로 초기화된다"
    - "Upload 버튼이 #cc785c 배경색 스타일을 갖는다"
    - "BOOT 파일 선택 시 _is_boot_file() → True"
    - "btn_cancel 레이블이 idle='Close', 진행 중='Stop Upload', 완료='Close'로 전환된다"
  artifacts:
    - path: "wiz550_fw_dialog.py"
      provides: "WIZ550FWDialog QDialog 클래스 + _is_boot_file 헬퍼"
      exports:
        - WIZ550FWDialog
        - _is_boot_file
  key_links:
    - from: "wiz550_fw_dialog.WIZ550FWDialog._on_upload"
      to: "WIZ550FWUploadThread"
      via: "import + instantiation"
      pattern: "WIZ550FWUploadThread.*mode.*fw_path"
    - from: "WIZ550FWDialog.lbl_status"
      to: "WIZ550FWUploadThread.progress"
      via: "pyqtSignal → setText"
      pattern: "progress.connect.*lbl_status"
---

<objective>
Wave 2: UI 다이얼로그 — wiz550_fw_dialog.py 신규 생성

Purpose: FW-04 (FW 업로드 UI). D-01 2탭 구조, D-03 서버 IP 자동채움, D-04 pw 입력,
          D-07 QProgressBar indeterminate, 07-UI-SPEC.md 전체 레이아웃 준수.
Output: wiz550_fw_dialog.py — WIZ550FWDialog 클래스 + _is_boot_file 헬퍼
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-tftp-fw-upload/07-CONTEXT.md
@.planning/phases/07-tftp-fw-upload/07-UI-SPEC.md
@.planning/phases/07-tftp-fw-upload/07-RESEARCH.md
@fw_git_dialog.py
@tests/test_wiz550_fw.py

<interfaces>
<!-- WIZ550FWUploadThread 시그니처 (07-01-PLAN.md에서 생성) -->
```python
class WIZ550FWUploadThread(QThread):
    progress = pyqtSignal(str)   # lbl_status.setText 연결
    finished = pyqtSignal(bool)  # True=성공, False=실패
    error = pyqtSignal(str)      # lbl_status 오류 색상 표시

    def __init__(self, mode: str, fw_path: str, target_ip: str, target_mac: str,
                 server_ip: str, server_port: int, password: str = "",
                 iface_ip: str = ""):
        ...

    def stop(self):
        """외부에서 호출 가능한 중단 메서드"""
        ...
```

<!-- UI-SPEC 다이얼로그 구조 요약 -->
```
WIZ550FWDialog(QDialog)
  setWindowTitle("WIZ550 FW Upload")
  setFixedWidth(480)
  setModal(True)
  root: QVBoxLayout(spacing=8, margins=12,12,12,12)
    tab_widget: QTabWidget
      Tab 0 "자동 (내장 TFTP)":
        auto_tab_layout: QVBoxLayout(spacing=8, margins=12,12,12,12)
          file_row: QHBoxLayout
            lbl_file: QLabel("FW File:") [fixedWidth=80]
            edit_file: QLineEdit [Expanding, readOnly=True, placeholder="No file selected"]
            btn_browse: QPushButton("Browse...") [fixedWidth=70]
          pw_row: QHBoxLayout
            lbl_pw: QLabel("Password:") [fixedWidth=80]
            edit_pw: QLineEdit [Expanding, EchoMode=Password, placeholder="Optional"]
          sep1: QFrame(HLine/Sunken)
          pgbar_auto: QProgressBar [visible=False, range=(0,100), value=0]
      Tab 1 "수동 (외부 TFTP)":
        manual_tab_layout: QVBoxLayout(spacing=8, margins=12,12,12,12)
          server_ip_row: QHBoxLayout
            lbl_ip: QLabel("Server IP:") [fixedWidth=80]
            edit_ip: QLineEdit [Expanding, pre-filled=localip_addr]
          port_row: QHBoxLayout
            lbl_port: QLabel("Port:") [fixedWidth=80]
            edit_port: QLineEdit [Expanding, text="69"]
          fname_row: QHBoxLayout
            lbl_fname: QLabel("File Name:") [fixedWidth=80]
            edit_fname: QLineEdit [Expanding]
          pw_row: QHBoxLayout
            lbl_pw: QLabel("Password:") [fixedWidth=80]
            edit_pw_manual: QLineEdit [Expanding, EchoMode=Password, placeholder="Optional"]
    lbl_status: QLabel("") [fixedHeight=20]
    btn_row: QHBoxLayout
      addStretch()
      btn_upload: QPushButton("Upload") [default=True, fixedWidth=90, primary style]
      btn_cancel: QPushButton("Close") [fixedWidth=90]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wiz550_fw_dialog.py 다이얼로그 클래스 생성</name>
  <files>wiz550_fw_dialog.py</files>

  <read_first>
    - fw_git_dialog.py — 전체 파일 읽기. QVBoxLayout spacing/margins 패턴, QProgressBar 초기화, btn_row addStretch() → 기능버튼 → Cancel 패턴, QFrame(HLine/Sunken) 구분선 패턴 확인
    - .planning/phases/07-tftp-fw-upload/07-UI-SPEC.md — 전체 파일. 특히: Spacing Scale, Component Inventory, Interaction States, Copywriting Contract, Qt-Specific Implementation Notes
    - .planning/phases/07-tftp-fw-upload/07-CONTEXT.md — D-01~D-04, D-07 결정 사항
    - tests/test_wiz550_fw.py — test_dialog_tabs, test_boot_file_rejected 테스트 시그니처 확인
  </read_first>

  <behavior>
    - WIZ550FWDialog(localip_addr="192.168.0.100") 생성 성공
    - dlg.tab_widget.count() == 2
    - dlg.tab_widget.tabText(0) == "자동 (내장 TFTP)"
    - dlg.tab_widget.tabText(1) == "수동 (외부 TFTP)"
    - dlg.edit_ip.text() == "192.168.0.100"
    - dlg.edit_port.text() == "69"
    - _is_boot_file("WIZ550SR_BOOT_v1.bin") is True
    - _is_boot_file("app.bin") is False
  </behavior>

  <action>
    wiz550_fw_dialog.py 신규 파일 생성. 아래 구조를 정확히 구현한다.

    **파일 헤더:**
    ```python
    #!/usr/bin/env python3
    # -*- coding: utf-8 -*-
    """
    wiz550_fw_dialog.py — WIZ550 TFTP FW 업로드 다이얼로그

    D-01: 탭 2개 구조 (자동/수동)
    D-02: 포트 69 바인딩 실패 시 오류 메시지 후 중단
    D-03: 수동 탭 서버 IP = NIC IP 자동채움
    D-04: pw 입력 필드 optional
    D-07: QProgressBar indeterminate → 완료 100%
    UI-SPEC: fw_git_dialog.py 패턴 계승
    """

    import os
    import socket

    from PyQt5.QtCore import Qt
    from PyQt5.QtWidgets import (
        QDialog, QVBoxLayout, QHBoxLayout,
        QTabWidget, QWidget, QLabel, QLineEdit,
        QPushButton, QProgressBar, QFrame,
        QFileDialog, QMessageBox,
    )

    from WIZ550FWUploadThread import WIZ550FWUploadThread
    from utils import logger
    ```

    **_is_boot_file 헬퍼 (모듈 수준):**
    ```python
    def _is_boot_file(filename: str) -> bool:
        """파일명에 'BOOT' 포함 시 True (대소문자 무관). 기존 firmware_file_open() 패턴 준용."""
        return 'BOOT' in os.path.basename(filename).upper()
    ```

    **WIZ550FWDialog 클래스 — __init__ 파라미터:**
    ```python
    class WIZ550FWDialog(QDialog):
        def __init__(self, localip_addr: str = "",
                     target_ip: str = "",
                     target_mac: str = "",
                     parent=None):
    ```
    - localip_addr: 수동 탭 Server IP 기본값 (D-03)
    - target_ip: 0xD1 전송 목적지 (main_gui.py에서 전달)
    - target_mac: WIZ550 MAC (0xD1 payload)

    **인스턴스 변수 (주요):**
    ```python
    self._localip_addr = localip_addr
    self._target_ip = target_ip
    self._target_mac = target_mac
    self._upload_thread = None  # WIZ550FWUploadThread 인스턴스 보관
    self._fw_path = ""          # 선택된 FW 파일 경로 (자동 탭)
    ```

    **_build_ui() 메서드 — 07-UI-SPEC.md 완전 준수:**

    1. 다이얼로그 기본 설정:
    ```python
    self.setWindowTitle("WIZ550 FW Upload")
    self.setFixedWidth(480)
    self.setModal(True)
    ```

    2. root layout:
    ```python
    root = QVBoxLayout(self)
    root.setSpacing(8)
    root.setContentsMargins(12, 12, 12, 12)
    ```

    3. tab_widget = QTabWidget():
    ```python
    self.tab_widget = QTabWidget()
    root.addWidget(self.tab_widget)
    ```

    4. Tab 0 — 자동 탭:
    - auto_widget = QWidget(), auto_layout = QVBoxLayout(auto_widget), setSpacing(8), setContentsMargins(12,12,12,12)
    - file_row: lbl_file=QLabel("FW File:") setFixedWidth(80), edit_file readOnly=True placeholder="No file selected", btn_browse setFixedWidth(70) text="Browse..."
    - pw_row: lbl_pw=QLabel("Password:") setFixedWidth(80), edit_pw EchoMode=Password placeholder="Optional"
    - sep = QFrame(), sep.setFrameShape(QFrame.HLine), sep.setFrameShadow(QFrame.Sunken)
    - pgbar_auto = QProgressBar(), setRange(0,100), setValue(0), setVisible(False)
    - 위젯 이름: self.edit_file, self.btn_browse, self.edit_pw, self.pgbar_auto
    - self.tab_widget.addTab(auto_widget, "자동 (내장 TFTP)")

    5. Tab 1 — 수동 탭:
    - manual_widget = QWidget(), manual_layout = QVBoxLayout(manual_widget), setSpacing(8), setContentsMargins(12,12,12,12)
    - server_ip_row: lbl_ip=QLabel("Server IP:") setFixedWidth(80), edit_ip text=localip_addr
    - port_row: lbl_port=QLabel("Port:") setFixedWidth(80), edit_port text="69"
    - fname_row: lbl_fname=QLabel("File Name:") setFixedWidth(80), edit_fname
    - pw_row: lbl_pw_m=QLabel("Password:") setFixedWidth(80), edit_pw_manual EchoMode=Password placeholder="Optional"
    - 위젯 이름: self.edit_ip, self.edit_port, self.edit_fname, self.edit_pw_manual
    - self.tab_widget.addTab(manual_widget, "수동 (외부 TFTP)")

    6. lbl_status (탭 외부):
    ```python
    self.lbl_status = QLabel("")
    self.lbl_status.setFixedHeight(20)
    root.addWidget(self.lbl_status)
    ```

    7. btn_row:
    ```python
    btn_row = QHBoxLayout()
    btn_row.addStretch()
    self.btn_upload = QPushButton("Upload")
    self.btn_upload.setDefault(True)
    self.btn_upload.setFixedWidth(90)
    self.btn_upload.setStyleSheet("""
        QPushButton {
            background-color: #cc785c;
            color: white;
            border-radius: 6px;
            padding: 6px 16px;
            font-weight: 500;
        }
        QPushButton:hover {
            background-color: #a9583e;
        }
        QPushButton:disabled {
            background-color: #e6dfd8;
            color: #6c6a64;
        }
    """)
    self.btn_cancel = QPushButton("Close")
    self.btn_cancel.setFixedWidth(90)
    btn_row.addWidget(self.btn_upload)
    btn_row.addWidget(self.btn_cancel)
    root.addLayout(btn_row)
    ```

    **시그널 연결 (_connect_signals 메서드):**
    ```python
    def _connect_signals(self):
        self.btn_browse.clicked.connect(self._on_browse)
        self.btn_upload.clicked.connect(self._on_upload)
        self.btn_cancel.clicked.connect(self._on_cancel)
    ```

    **_on_browse:**
    ```python
    def _on_browse(self):
        fname, _ = QFileDialog.getOpenFileName(
            self, "펌웨어 파일 선택", "", "Binary Files (*.bin);;All Files (*)"
        )
        if not fname:
            return
        if _is_boot_file(fname):
            QMessageBox.warning(
                self, "파일 오류",
                "Cannot upload BOOT firmware file. Please select an APP firmware file only."
            )
            return
        self._fw_path = fname
        self.edit_file.setText(os.path.basename(fname))
    ```

    **_validate_inputs() → (bool, str) — 입력 유효성 검증:**
    ```python
    def _validate_inputs(self) -> tuple:
        """반환: (유효 여부, 오류 메시지)"""
        tab = self.tab_widget.currentIndex()
        if tab == 0:  # 자동 탭
            if not self._fw_path:
                return False, "Please select a firmware file."
            basename = os.path.basename(self._fw_path)
            if len(basename) > 50:
                return False, "File name is too long (max 50 characters)."
            return True, ""
        else:  # 수동 탭
            ip_text = self.edit_ip.text().strip()
            port_text = self.edit_port.text().strip()
            fname_text = self.edit_fname.text().strip()
            try:
                socket.inet_aton(ip_text)
            except OSError:
                return False, "Invalid server IP address."
            try:
                port = int(port_text)
                if not (1 <= port <= 65535):
                    raise ValueError
            except ValueError:
                return False, "Port must be between 1 and 65535."
            if not fname_text:
                return False, "Please enter the firmware file name on the TFTP server."
            if len(fname_text) > 50:
                return False, "File name is too long (max 50 characters)."
            return True, ""
    ```

    **_on_upload:**
    ```python
    def _on_upload(self):
        valid, err_msg = self._validate_inputs()
        if not valid:
            self._set_status(err_msg, error=True)
            return

        tab = self.tab_widget.currentIndex()
        if tab == 0:  # 자동 탭
            mode = 'auto'
            fw_path = self._fw_path
            server_ip = self._localip_addr or "0.0.0.0"
            server_port = 69
            file_name = os.path.basename(fw_path)
            password = self.edit_pw.text()
        else:  # 수동 탭
            mode = 'manual'
            fw_path = ""
            server_ip = self.edit_ip.text().strip()
            server_port = int(self.edit_port.text().strip())
            file_name = self.edit_fname.text().strip()
            password = self.edit_pw_manual.text()

        self._set_uploading_state(True)

        self._upload_thread = WIZ550FWUploadThread(
            mode=mode,
            fw_path=fw_path,
            target_ip=self._target_ip,
            target_mac=self._target_mac,
            server_ip=server_ip,
            server_port=server_port,
            password=password,
            iface_ip=self._localip_addr or "",
        )
        self._upload_thread.progress.connect(self._on_progress)
        self._upload_thread.finished.connect(self._on_finished)
        self._upload_thread.error.connect(self._on_error)
        self._upload_thread.start()
    ```

    **_set_uploading_state(uploading: bool):**
    ```python
    def _set_uploading_state(self, uploading: bool):
        """D-07 + UI-SPEC Interaction States"""
        self.btn_upload.setEnabled(not uploading)
        if uploading:
            self.btn_cancel.setText("Stop Upload")
            self.pgbar_auto.setRange(0, 0)   # indeterminate
            self.pgbar_auto.setValue(0)
            self.pgbar_auto.setVisible(True)
        else:
            self.btn_cancel.setText("Close")
            self.pgbar_auto.setRange(0, 100)
            self.pgbar_auto.setValue(100)
    ```

    **_on_progress, _on_finished, _on_error:**
    ```python
    def _on_progress(self, msg: str):
        self._set_status(msg, muted=True)

    def _on_finished(self, success: bool):
        self._set_uploading_state(False)
        if success:
            self._set_status("Upload complete!", success=True)
        else:
            # 실패 메시지는 _on_error에서 이미 표시됨
            pass

    def _on_error(self, msg: str):
        self._set_uploading_state(False)
        if "timed out" in msg.lower() or "no response" in msg.lower():
            self._set_status(
                "Upload timed out. No response from device. "
                "Please retry or check the device connection.",
                error=True
            )
        else:
            self._set_status(msg, error=True)
    ```

    **_set_status(msg, *, error=False, success=False, muted=False):**
    ```python
    def _set_status(self, msg: str, *, error: bool = False,
                    success: bool = False, muted: bool = False):
        self.lbl_status.setText(msg)
        if error:
            self.lbl_status.setStyleSheet("color: #c64545;")
        elif success:
            self.lbl_status.setStyleSheet("color: #5db872;")
        elif muted:
            self.lbl_status.setStyleSheet("color: #6c6a64;")
        else:
            self.lbl_status.setStyleSheet("")
    ```

    **_on_cancel:**
    ```python
    def _on_cancel(self):
        if self._upload_thread is not None and self._upload_thread.isRunning():
            self._upload_thread.stop()
            self._upload_thread.wait(3000)
            self._upload_thread = None
        self.reject()
    ```

    **closeEvent 오버라이드 (안전 정리):**
    ```python
    def closeEvent(self, event):
        if self._upload_thread is not None and self._upload_thread.isRunning():
            self._upload_thread.stop()
            self._upload_thread.wait(3000)
        event.accept()
    ```

    **__init__ 본문:**
    ```python
    def __init__(self, localip_addr="", target_ip="", target_mac="", parent=None):
        super().__init__(parent)
        self._localip_addr = localip_addr
        self._target_ip = target_ip
        self._target_mac = target_mac
        self._upload_thread = None
        self._fw_path = ""
        self._build_ui()
        self._connect_signals()
    ```
  </action>

  <verify>
    <automated>uv run pytest tests/test_wiz550_fw.py::test_dialog_tabs tests/test_wiz550_fw.py::test_boot_file_rejected -xvs</automated>
  </verify>

  <acceptance_criteria>
    - wiz550_fw_dialog.py 파일 존재: `grep -n "class WIZ550FWDialog" wiz550_fw_dialog.py` → 1줄
    - _is_boot_file 함수 존재: `grep -n "def _is_boot_file" wiz550_fw_dialog.py` → 1줄
    - test_dialog_tabs PASSED: `uv run pytest tests/test_wiz550_fw.py::test_dialog_tabs -v` → PASSED
    - test_boot_file_rejected PASSED: `uv run pytest tests/test_wiz550_fw.py::test_boot_file_rejected -v` → PASSED
    - Upload 버튼 스타일 #cc785c 포함: `grep "cc785c" wiz550_fw_dialog.py` → 1줄 이상
    - btn_cancel "Close" 초기값: `grep '"Close"' wiz550_fw_dialog.py` → 1줄 이상
    - btn_cancel "Stop Upload" 전환: `grep '"Stop Upload"' wiz550_fw_dialog.py` → 1줄 이상
    - pgbar_auto setRange(0,0) indeterminate: `grep "setRange(0, 0)" wiz550_fw_dialog.py` → 1줄 이상
    - 입력 검증 D-02 문자열: `grep "Administrator privileges" wiz550_fw_dialog.py` → 0줄 (D-02는 WIZ550FWUploadThread에서 emit. 다이얼로그는 _on_error로 수신 표시)
    - 다이얼로그 폭 480: `grep "setFixedWidth(480)" wiz550_fw_dialog.py` → 1줄
    - 탭 텍스트 검증: `grep "자동 (내장 TFTP)" wiz550_fw_dialog.py` → 1줄
  </acceptance_criteria>

  <done>wiz550_fw_dialog.py 생성 완료. test_dialog_tabs + test_boot_file_rejected PASSED. 전체 5개 테스트 중 4개 PASSED (test_build_fw_upload_pkt 포함).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력 → _validate_inputs | edit_ip, edit_port, edit_fname 필드가 다이얼로그에서 직접 수집됨 |
| 파일 선택 → _is_boot_file | 사용자가 임의 파일 선택. BOOT 파일 차단 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02-01 | Tampering | _validate_inputs server IP | mitigate | `socket.inet_aton(ip_text)` 호출로 4옥텟 IP 형식 검증. 실패 시 오류 메시지 표시, 업로드 미진행 |
| T-7-02-02 | Tampering | _validate_inputs port range | mitigate | `1 <= port <= 65535` 범위 체크. ValueError 시 오류 메시지 표시 |
| T-7-02-03 | Tampering | _on_browse BOOT 파일 | mitigate | `_is_boot_file()` → True 시 QMessageBox.warning + return. 업로드 진행 불가 |
| T-7-02-04 | Tampering | edit_fname 파일명 50자 초과 | mitigate | `len(fname_text) > 50` 체크 → 오류 메시지. build_fw_upload_pkt에서도 [:50] 슬라이싱 이중 방어 |
| T-7-02-05 | Denial of Service | closeEvent QThread 미정리 | mitigate | closeEvent 오버라이드: stop() + wait(3000) 후 accept. QThread 좀비 프로세스 방지 |
</threat_model>

<verification>
1. `uv run pytest tests/test_wiz550_fw.py -v` → 5개 중 4개 PASSED (test_dialog_tabs, test_boot_file_rejected, test_build_fw_upload_pkt, test_parse_fw_done_reply)
2. test_tftp_server_tempdir은 Wave 0에서 이미 PASSED
3. 모든 5개 PASSED 확인
</verification>

<success_criteria>
- wiz550_fw_dialog.py 신규 파일 생성 완료
- test_dialog_tabs PASSED
- test_boot_file_rejected PASSED
- 전체 tests/test_wiz550_fw.py 5개 모두 PASSED
</success_criteria>

<output>
완료 후 `.planning/phases/07-tftp-fw-upload/07-02-SUMMARY.md` 생성.
</output>
