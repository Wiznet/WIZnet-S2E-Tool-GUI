---
phase: 07-tftp-fw-upload
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - main_gui.py
autonomous: false
requirements:
  - FW-01
  - FW-02
  - FW-03
  - FW-04

must_haves:
  truths:
    - "WIZ550 장치가 선택된 상태에서 FW Upload 버튼 클릭 시 WIZ550FWDialog가 열린다"
    - "WIZ1x0SR 또는 기타 장치는 기존 업로드 경로가 그대로 동작한다 (회귀 없음)"
    - "다이얼로그에 target_ip, target_mac, localip_addr가 올바르게 전달된다"
  artifacts:
    - path: "main_gui.py"
      provides: "event_upload_clicked() WIZ550 분기 + upload_wiz550() 메서드"
      contains: "def upload_wiz550"
  key_links:
    - from: "main_gui.py event_upload_clicked"
      to: "main_gui.upload_wiz550"
      via: "_proto == 'wiz550' 체크"
      pattern: "_proto.*wiz550.*upload_wiz550"
    - from: "main_gui.upload_wiz550"
      to: "wiz550_fw_dialog.WIZ550FWDialog"
      via: "import + exec_()"
      pattern: "WIZ550FWDialog.*exec_"
---

<objective>
Wave 3: main_gui.py 진입점 연결 — event_upload_clicked() WIZ550 분기 추가

Purpose: FW-06 분기 (D-06). WIZ550 장치 선택 시 WIZ550FWDialog를 열어 업로드 흐름 완성.
          기존 WIZ5xxSR / WIZ1x0SR 업로드 경로에 회귀 없음 확인 포함.
Output: main_gui.py에 upload_wiz550() 메서드 추가 + event_upload_clicked() 분기 삽입
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-tftp-fw-upload/07-CONTEXT.md
@.planning/phases/07-tftp-fw-upload/07-RESEARCH.md
@main_gui.py
@wiz550_fw_dialog.py

<interfaces>
<!-- main_gui.py 기존 event_upload_clicked() (line 1250) — 현재 코드 -->
```python
def event_upload_clicked(self):
    if self.curr_dev == 'WIZ1x0SR':
        self.show_msgbox("Info", "WIZ1x0SR 펌웨어 업로드는 지원되지 않습니다.", QMessageBox.Information)
        return
    if self.localip_addr is not None:
        self.update_btn_clicked()
    else:
        self.show_msgbox(
            "Warning",
            "Local IP information could not be found. Check the Network configuration.",
            QMessageBox.Warning,
        )
```

<!-- 기존 WIZ550 분기 패턴 (Phase 6에서 확립) — event_setting_clicked, event_reset_clicked 동일 패턴 사용 -->
```python
# Phase 6 패턴 (line 1208~1220):
if (hasattr(self, 'curr_mac') and self.curr_mac
        and self.dev_profile.get(self.curr_mac, {}).get('_proto') == 'wiz550'):
    self.upload_wiz550()
    return
```

<!-- WIZ550FWDialog 시그니처 (wiz550_fw_dialog.py) -->
```python
class WIZ550FWDialog(QDialog):
    def __init__(self, localip_addr: str = "",
                 target_ip: str = "",
                 target_mac: str = "",
                 parent=None):
```

<!-- main_gui.py 내 WIZ550 장치 데이터 접근 패턴 -->
```python
self.curr_mac          # 현재 선택 장치 MAC
self.localip_addr      # 현재 NIC IP (수동 탭 Server IP 자동채움)
self.dev_profile[self.curr_mac].get('_proto')   # 'wiz550'
self.dev_profile[self.curr_mac].get('IP')       # 장치 IP (target_ip)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main_gui.py — event_upload_clicked() WIZ550 분기 + upload_wiz550() 추가</name>
  <files>main_gui.py</files>

  <read_first>
    - main_gui.py lines 1~30 — import 섹션 전체. wiz550_fw_dialog 임포트 추가 위치 확인
    - main_gui.py lines 1248~1265 — event_upload_clicked() 현재 전체 코드 확인
    - main_gui.py lines 1205~1250 — event_setting_clicked(), event_reset_clicked() WIZ550 분기 패턴 확인 (upload_wiz550와 동일 위치/패턴으로 작성)
    - main_gui.py line 544 — self.curr_mac = None 초기화 확인
    - main_gui.py line 473 — self.dev_profile = {} 초기화 확인
    - wiz550_fw_dialog.py — WIZ550FWDialog __init__ 파라미터 재확인
  </read_first>

  <action>
    **Step 1: import 섹션에 wiz550_fw_dialog 추가**

    main_gui.py 상단 import 섹션에서 기존 fw_git_dialog 임포트 라인을 찾는다:
    `from fw_git_dialog import FWGitDialog`

    그 바로 다음 줄에 추가:
    `from wiz550_fw_dialog import WIZ550FWDialog`

    **Step 2: event_upload_clicked() 수정**

    기존 함수:
    ```python
    def event_upload_clicked(self):
        if self.curr_dev == 'WIZ1x0SR':
            self.show_msgbox("Info", "WIZ1x0SR 펌웨어 업로드는 지원되지 않습니다.", QMessageBox.Information)
            return
        if self.localip_addr is not None:
            self.update_btn_clicked()
        else:
            self.show_msgbox(
                "Warning",
                "Local IP information could not be found. Check the Network configuration.",
                QMessageBox.Warning,
            )
    ```

    다음으로 교체:
    ```python
    def event_upload_clicked(self):
        # WIZ550 장치 전용 다이얼로그 (D-06)
        if (hasattr(self, 'curr_mac') and self.curr_mac
                and self.dev_profile.get(self.curr_mac, {}).get('_proto') == 'wiz550'):
            self.upload_wiz550()
            return
        # 기존 WIZ1x0SR 처리 (미지원)
        if self.curr_dev == 'WIZ1x0SR':
            self.show_msgbox("Info", "WIZ1x0SR 펌웨어 업로드는 지원되지 않습니다.", QMessageBox.Information)
            return
        # 기존 WIZ5xxSR 처리
        if self.localip_addr is not None:
            self.update_btn_clicked()
        else:
            self.show_msgbox(
                "Warning",
                "Local IP information could not be found. Check the Network configuration.",
                QMessageBox.Warning,
            )
    ```

    **Step 3: upload_wiz550() 메서드 추가**

    event_upload_clicked() 함수 다음 줄(또는 event_setting_clicked 등 WIZ550 전용 함수들과 가까운 위치)에 추가:

    ```python
    def upload_wiz550(self):
        """WIZ550 장치 TFTP FW 업로드 다이얼로그 실행 (D-06)."""
        if not self.curr_mac or self.curr_mac not in self.dev_profile:
            self.show_msgbox("Warning", "장치 정보를 불러올 수 없습니다. 장치를 다시 선택하세요.", QMessageBox.Warning)
            return

        dev_data = self.dev_profile[self.curr_mac]
        target_ip = dev_data.get('IP', '')
        target_mac = self.curr_mac
        localip = self.localip_addr or ''

        if not target_ip:
            self.show_msgbox("Warning", "장치 IP 정보가 없습니다. 장치를 다시 검색하세요.", QMessageBox.Warning)
            return

        dlg = WIZ550FWDialog(
            localip_addr=localip,
            target_ip=target_ip,
            target_mac=target_mac,
            parent=self,
        )
        dlg.exec_()
    ```

    **주의사항:**
    - main_gui.py 초기화 순서 확인 필수: `self.curr_mac`은 __init__ line 527, `self.dev_profile`은 line 473에서 초기화됨. event_upload_clicked()는 UI 이벤트 핸들러이므로 __init__ 완료 후 호출 → 안전
    - 기존 WIZ1x0SR, WIZ5xxSR 경로를 삭제하거나 변경하지 말 것. 분기 추가만.
    - `from wiz550_fw_dialog import WIZ550FWDialog` 중복 임포트 주의 (grep으로 이미 존재 확인 후 스킵)
  </action>

  <verify>
    <automated>uv run pytest tests/test_wiz550_fw.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - WIZ550FWDialog import 추가: `grep "from wiz550_fw_dialog import WIZ550FWDialog" main_gui.py` → 1줄
    - WIZ550 분기 추가: `grep "_proto.*wiz550\|upload_wiz550" main_gui.py | head -5` → 2줄 이상 (분기 + 함수 정의)
    - upload_wiz550 함수 정의: `grep "def upload_wiz550" main_gui.py` → 1줄
    - WIZ550FWDialog exec_ 호출: `grep "WIZ550FWDialog.*\|dlg\.exec_" main_gui.py` → 존재
    - 기존 WIZ1x0SR 분기 유지: `grep "WIZ1x0SR 펌웨어 업로드" main_gui.py` → 1줄 (삭제 금지)
    - 기존 update_btn_clicked 경로 유지: `grep "update_btn_clicked" main_gui.py` → 기존 줄 유지
    - 전체 테스트 통과: `uv run pytest tests/test_wiz550_fw.py -q` → 5 passed
  </acceptance_criteria>

  <done>main_gui.py에 WIZ550 업로드 분기 추가 완료. 기존 WIZ5xxSR/WIZ1x0SR 경로 회귀 없음 확인. 전체 5개 테스트 PASSED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 통합 동작 확인 (체크포인트)</name>

  <what-built>
    Phase 7 전체 구현:
    - tests/test_wiz550_fw.py — 5개 테스트 (Wave 0)
    - WIZ550MSGHandler.py — build_fw_upload_pkt() 추가 (Wave 1)
    - WIZ550FWUploadThread.py — tftpy QThread + 0xD2 수신 (Wave 1)
    - wiz550_fw_dialog.py — 2탭 다이얼로그 (Wave 2)
    - main_gui.py — event_upload_clicked() WIZ550 분기 (Wave 3)
  </what-built>

  <how-to-verify>
    **1. 자동 테스트 확인 (30초)**
    ```
    uv run pytest tests/test_wiz550_fw.py -v
    ```
    → 5개 모두 PASSED 확인

    **2. 전체 회귀 테스트 확인 (1분)**
    ```
    uv run pytest tests/ -q
    ```
    → 기존 테스트 포함 모두 PASSED 확인

    **3. 다이얼로그 시각 확인 (2분)**

    임시 스크립트로 다이얼로그 열기:
    ```python
    # %LOCALAPPDATA%\Temp\claude\test_fw_dialog.py
    import sys
    from PyQt5.QtWidgets import QApplication
    from wiz550_fw_dialog import WIZ550FWDialog

    app = QApplication(sys.argv)
    dlg = WIZ550FWDialog(
        localip_addr="192.168.0.100",
        target_ip="192.168.0.200",
        target_mac="00:08:DC:AB:CD:EF"
    )
    dlg.show()
    sys.exit(app.exec_())
    ```
    실행: `cd D:\user\Documents\GitHub\WIZnet-S2E-Tool-GUI && uv run python %LOCALAPPDATA%\Temp\claude\test_fw_dialog.py`

    확인 사항:
    - [ ] 다이얼로그 폭 480px, 타이틀 "WIZ550 FW Upload"
    - [ ] Tab 0 텍스트 "자동 (내장 TFTP)", Tab 1 텍스트 "수동 (외부 TFTP)"
    - [ ] 수동 탭 Server IP 필드에 "192.168.0.100" 자동 채워짐, Port 필드 "69"
    - [ ] Upload 버튼 갈색(#cc785c) 배경색
    - [ ] Close 버튼 오른쪽 정렬
    - [ ] 자동 탭 Browse... 버튼 클릭 → 파일 선택 다이얼로그 열림

    **4. main_gui.py 기존 기능 확인 (선택)**
    GUI 실행 후 WIZ5xxSR 장치 선택 → FW Upload 버튼 → 기존 업로드 흐름 동작 확인 (회귀 없음)
  </how-to-verify>

  <resume-signal>확인 완료 시 "approved" 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event_upload_clicked → upload_wiz550 | _proto 체크 후 진입. curr_mac/dev_profile 접근 시점에 __init__ 완료 보장됨 |
| upload_wiz550 → WIZ550FWDialog | target_ip, target_mac을 dev_profile에서 읽어 전달. None/빈 문자열 방어 처리 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-03-01 | Tampering | upload_wiz550 target_ip 미설정 | mitigate | `if not target_ip: show_msgbox` → 다이얼로그 미열림. 장치 재검색 안내 |
| T-7-03-02 | Tampering | event_upload_clicked 분기 순서 | mitigate | WIZ550 체크를 WIZ1x0SR 체크보다 먼저 배치. 순서 전환 시 WIZ550이 WIZ1x0SR 경로로 빠질 위험 없음 (MAC 기반 체크) |
| T-7-03-03 | Spoofing | _proto 조작 가능성 | accept | dev_profile은 내부 dict. 외부 입력 아님. 네트워크에서 받은 응답으로 채워지나 로컬 GUI 도구 특성상 낮은 위협 |
</threat_model>

<verification>
1. `uv run pytest tests/test_wiz550_fw.py -v` → 5 passed
2. `uv run pytest tests/ -q` → 전체 통과 (기존 회귀 없음)
3. `grep "def upload_wiz550" main_gui.py` → 존재
4. `grep "from wiz550_fw_dialog import" main_gui.py` → 존재
5. checkpoint:human-verify 통과
</verification>

<success_criteria>
- main_gui.py에 WIZ550 업로드 분기 추가 완료
- 기존 WIZ5xxSR/WIZ1x0SR 업로드 경로 회귀 없음
- 전체 tests/ 통과
- 다이얼로그 시각 확인 체크포인트 통과
</success_criteria>

<output>
완료 후 `.planning/phases/07-tftp-fw-upload/07-03-SUMMARY.md` 생성.
</output>
