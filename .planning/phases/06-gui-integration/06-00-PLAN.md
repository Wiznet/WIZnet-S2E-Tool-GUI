---
phase: 06-gui-integration
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_wiz550_gui.py
  - tests/conftest.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "tests/test_wiz550_gui.py 파일이 존재하고 모든 테스트 함수가 xfail 또는 skip 상태로 수집된다"
    - "QApplication 픽스처(qapp)가 conftest.py에 존재하여 PyQt5 위젯 테스트가 import 오류 없이 로드된다"
    - "uv run pytest tests/test_wiz550_gui.py --co -q 가 오류 없이 테스트 목록을 출력한다"
  artifacts:
    - path: "tests/test_wiz550_gui.py"
      provides: "UI-01~04 단위 테스트 스텁 6개"
      exports:
        - test_merge_wiz550_results
        - test_search_each_dev_filters_wiz550
        - test_build_panel_sections
        - test_disabled_field_widget
        - test_setinfo_roundtrip
        - test_wiz550_resetter_opcodes
    - path: "tests/conftest.py"
      provides: "qapp 픽스처 (PyQt5 QApplication 세션 스코프)"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_wiz550_gui.py"
      via: "qapp fixture"
      pattern: "def qapp"
---

<objective>
Wave 0: Phase 6 테스트 인프라 구축

기존 tests/conftest.py에 PyQt5 QApplication 픽스처를 추가하고,
UI-01~04 요구사항을 커버하는 단위 테스트 스텁 6개를 tests/test_wiz550_gui.py에 작성한다.
모든 스텁은 pytest.mark.xfail(strict=False) 또는 pytest.mark.skip으로 마킹하여
Wave 1~3 구현 전에도 수집(collect)은 성공한다.

Purpose: Nyquist 규칙 — Wave 1~3의 모든 태스크가 자동화된 verify 커맨드를 가지려면
테스트 파일이 먼저 존재해야 한다.

Output: tests/test_wiz550_gui.py (6 stub tests), tests/conftest.py (qapp fixture 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-gui-integration/06-CONTEXT.md
@.planning/phases/06-gui-integration/06-VALIDATION.md

<interfaces>
<!-- tests/conftest.py 현재 구조 (Phase 4 픽스처) -->
<!-- conftest.py는 STX/WIZ550_PORT 등 프로토콜 상수와 패킷 빌더 헬퍼만 정의 -->
<!-- QApplication 픽스처가 없음 → Wave 0에서 추가 필요 -->

기존 conftest.py 에서 추출:
```python
import struct, pytest
STX = 0xA5; WIZNET_REQUEST = 0xAA; WIZNET_REPLY = 0x55
OP_DISCOVERY = 0xA1; OP_GET_INFO = 0xB0; WIZ550_PORT = 6550

def _make_header(op_code, direction, payload_len, unicast=False) -> bytes: ...
def make_discovery_reply(mac_bytes, fw_version, product_code) -> bytes: ...
def make_get_info_reply(config_bytes, mac_bytes) -> bytes: ...
```

WIZ550Profile 공개 함수:
```python
from WIZ550Profile import parse_sr, parse_s2e, parse_web, build_sr, build_s2e, build_web
# parse_sr(data: bytes) -> dict   (162B)
# parse_web(data: bytes) -> dict  (133B)
# build_sr(d: dict) -> bytes
```

WIZ550MSGHandler QThread 시그널:
```python
from WIZ550MSGHandler import WIZ550Searcher, WIZ550Getter, WIZ550Setter, WIZ550Resetter
from WIZ550MSGHandler import OP_REMOTE_RESET, OP_FACTORY_RESET
# WIZ550Searcher.search_done = pyqtSignal(list)
# WIZ550Getter.get_done      = pyqtSignal(dict)
# WIZ550Setter.set_done      = pyqtSignal(bool)
# WIZ550Resetter.reset_done  = pyqtSignal(bool)
# WIZ550Resetter.__init__(target_ip, target_mac, password, op_code, iface_ip, timeout)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest.py에 qapp 픽스처 추가</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (기존 내용 확인 후 추가)
  </read_first>
  <action>
tests/conftest.py 파일을 읽고, 파일 끝에 다음 내용을 추가한다.
기존 내용은 유지한다 — 덮어쓰기 금지.

추가 내용:

```python
# ─────────────────────────────────────────────────────────────────
# PyQt5 GUI 테스트용 QApplication 픽스처 (Phase 6 — Wave 0)
# ─────────────────────────────────────────────────────────────────
import sys

@pytest.fixture(scope="session")
def qapp():
    """
    세션 전체에서 재사용하는 QApplication 인스턴스.
    pytest-qt 없이 순수 PyQt5로 구성.
    이미 QApplication이 존재하면 재사용한다.
    """
    from PyQt5.QtWidgets import QApplication
    app = QApplication.instance()
    if app is None:
        app = QApplication(sys.argv)
    yield app
    # 세션 종료 시 app을 명시적으로 종료하지 않음 — pytest 종료 시 자동 해제
```

추가 위치: 파일 맨 끝 (기존 픽스처/헬퍼 아래).
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && grep -n "def qapp" tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py에 `def qapp():` 함수가 존재한다
    - `@pytest.fixture(scope="session")` 데코레이터가 qapp 위에 있다
    - `QApplication.instance()` 패턴이 포함되어 있다 (재사용 guard)
    - 기존 _make_header / make_discovery_reply 등 Phase 4 헬퍼가 여전히 존재한다
  </acceptance_criteria>
  <done>conftest.py에 qapp 픽스처가 추가되고 기존 내용이 보존됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_wiz550_gui.py — UI-01~04 테스트 스텁 6개 작성</name>
  <files>tests/test_wiz550_gui.py</files>
  <read_first>
    - tests/conftest.py (qapp 픽스처 이름 확인)
    - WIZ550MSGHandler.py (OP_REMOTE_RESET, OP_FACTORY_RESET 상수 확인)
    - specs/devices/WIZ550SR.yaml (ui.sections 구조 확인)
    - specs/devices/WIZ550WEB.yaml (disabled 필드 목록 확인)
  </read_first>
  <behavior>
    - test_merge_wiz550_results: XFAIL — _proto='wiz550' 장치가 dev_profile에 추가되는지 (UI-01)
    - test_search_each_dev_filters_wiz550: XFAIL — wiz550 장치가 search_each_dev 파이프라인에서 필터링되는지 (UI-01)
    - test_build_panel_sections: XFAIL — SR=3섹션, S2E=3~5섹션, WEB=4섹션 반환하는지 (UI-02)
    - test_disabled_field_widget: XFAIL — WEB disabled 필드 위젯이 isEnabled()==False인지 (UI-02, WR-01)
    - test_setinfo_roundtrip: XFAIL — fill_setinfo_wiz550 → build_sr 결과가 원본 bytes와 일치하는지 (UI-03)
    - test_wiz550_resetter_opcodes: PASS — WIZ550Resetter가 OP_REMOTE_RESET/FACTORY_RESET 상수를 올바르게 보유하는지 (UI-04, 구현 불필요)
  </behavior>
  <action>
tests/test_wiz550_gui.py 파일을 새로 생성한다 (없는 경우).
내용:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
tests/test_wiz550_gui.py — Phase 6 GUI Integration 단위 테스트

Wave 0: 모든 GUI 연동 테스트는 xfail(strict=False) 스텁으로 시작.
        Wave 1~3 구현 완료 시 마커를 제거하여 GREEN으로 전환.

Req Coverage:
  UI-01: test_merge_wiz550_results, test_search_each_dev_filters_wiz550
  UI-02: test_build_panel_sections, test_disabled_field_widget
  UI-03: test_setinfo_roundtrip
  UI-04: test_wiz550_resetter_opcodes
"""

import pytest
import struct

# ─────────────────────────────────────────────────────────────────
# UI-01: 검색 결과 병합 + search_each_dev 필터
# ─────────────────────────────────────────────────────────────────

@pytest.mark.xfail(strict=False, reason="Wave 1 구현 전 스텁")
def test_merge_wiz550_results(qapp):
    """
    _merge_wiz550_results 호출 후 dev_profile에 _proto='wiz550' 장치가 추가된다.
    Wave 1 구현 후: MainWindow 인스턴스를 생성하고 _merge_wiz550_results를 직접 호출.
    """
    # TODO (Wave 1): from main_gui import WIZwindow 후 실제 테스트 구현
    pytest.xfail("Wave 1 구현 전")


@pytest.mark.xfail(strict=False, reason="Wave 1 구현 전 스텁")
def test_search_each_dev_filters_wiz550(qapp):
    """
    search_each_dev()가 _proto='wiz550' 장치를 dev_info_list에서 제외한다.
    Wave 1 구현 후: dev_profile에 wiz550 장치 추가 → search_each_dev 호출 → 해당 MAC 제외 확인.
    """
    pytest.xfail("Wave 1 구현 전")


# ─────────────────────────────────────────────────────────────────
# UI-02: 패널 빌드 + disabled 필드
# ─────────────────────────────────────────────────────────────────

@pytest.mark.xfail(strict=False, reason="Wave 2 구현 전 스텁")
def test_build_panel_sections(qapp):
    """
    _build_wiz550_panel('WIZ550SR') → 3개 QGroupBox
    _build_wiz550_panel('WIZ550WEB') → 4개 QGroupBox (network/uart0/uart1/options)
    Wave 2 구현 후: QGroupBox 자식 위젯 수 검증.
    """
    pytest.xfail("Wave 2 구현 전")


@pytest.mark.xfail(strict=False, reason="Wave 2 구현 전 스텁")
def test_disabled_field_widget(qapp):
    """
    WIZ550WEB disabled:true 필드(working_mode, remote_ip 등)의 위젯이
    isEnabled() == False 인지 확인 (WR-01 해소).
    Wave 2 구현 후: _make_wiz550_field_widget({'id': 'working_mode', 'type': 'dropdown', 'disabled': True, 'choices': {}}) → isEnabled() == False.
    """
    pytest.xfail("Wave 2 구현 전")


# ─────────────────────────────────────────────────────────────────
# UI-03: 설정 쓰기 왕복 테스트
# ─────────────────────────────────────────────────────────────────

@pytest.mark.xfail(strict=False, reason="Wave 3 구현 전 스텁")
def test_setinfo_roundtrip(qapp):
    """
    parse_sr(raw_bytes) → fill_devinfo_wiz550() → fill_setinfo_wiz550() → build_sr(d)
    결과가 원본 raw_bytes와 동일한지 확인 (왕복 테스트).
    Wave 3 구현 후: WIZ550SR 162B 더미 패킷으로 검증.
    """
    pytest.xfail("Wave 3 구현 전")


# ─────────────────────────────────────────────────────────────────
# UI-04: Resetter op_code 상수 검증 (즉시 PASS 가능)
# ─────────────────────────────────────────────────────────────────

def test_wiz550_resetter_opcodes():
    """
    WIZ550Resetter가 OP_REMOTE_RESET(0xE0) / OP_FACTORY_RESET(0xF0) 상수를 보유한다.
    GUI 없이 import만으로 검증 가능 — xfail 없이 즉시 PASS.
    """
    from WIZ550MSGHandler import OP_REMOTE_RESET, OP_FACTORY_RESET, WIZ550Resetter
    assert OP_REMOTE_RESET  == 0xE0, f"OP_REMOTE_RESET 오류: {hex(OP_REMOTE_RESET)}"
    assert OP_FACTORY_RESET == 0xF0, f"OP_FACTORY_RESET 오류: {hex(OP_FACTORY_RESET)}"
    # WIZ550Resetter 기본 op_code 확인
    import inspect
    sig = inspect.signature(WIZ550Resetter.__init__)
    default_op = sig.parameters['op_code'].default
    assert default_op == OP_REMOTE_RESET, \
        f"WIZ550Resetter 기본 op_code 오류: {hex(default_op)}"
```

파일 경로: tests/test_wiz550_gui.py
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && uv run pytest tests/test_wiz550_gui.py --co -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_wiz550_gui.py 파일이 존재한다
    - `uv run pytest tests/test_wiz550_gui.py --co -q` 가 6개 테스트를 수집한다 (오류 없이)
    - `uv run pytest tests/test_wiz550_gui.py -x` 실행 시 test_wiz550_resetter_opcodes가 PASS한다
    - xfail 5개 테스트는 XFAIL 또는 SKIP 상태 (FAILED가 아님)
    - grep으로 확인: `grep -c "def test_" tests/test_wiz550_gui.py` 결과 = 6
  </acceptance_criteria>
  <done>6개 테스트 스텁이 수집되고, test_wiz550_resetter_opcodes가 PASS함</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → import | 테스트 코드가 WIZ550MSGHandler, WIZ550Profile을 import — 이 단계는 pure Python, 네트워크 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Information Disclosure | tests/test_wiz550_gui.py | accept | 테스트 코드 — 실제 장치 접근 없음, 더미 데이터만 사용 |
</threat_model>

<verification>
```bash
cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI

# 테스트 수집 확인
uv run pytest tests/test_wiz550_gui.py --co -q

# 실행 확인 (xfail 5개 + pass 1개)
uv run pytest tests/test_wiz550_gui.py -v

# 기존 Phase 4 테스트 회귀 확인
uv run pytest tests/ -x
```
</verification>

<success_criteria>
- tests/test_wiz550_gui.py 존재, 6개 테스트 수집됨
- tests/conftest.py에 qapp 픽스처 추가됨 (기존 내용 보존)
- test_wiz550_resetter_opcodes: PASS
- xfail 5개: XFAIL 상태 (strict=False — PASSED여도 오류 아님)
- uv run pytest tests/ -x: 전체 통과
</success_criteria>

<output>
완료 후 .planning/phases/06-gui-integration/06-00-SUMMARY.md 생성.

포함 내용:
- 추가된 픽스처 이름 및 위치
- 6개 테스트 함수명과 커버하는 요구사항 ID
- pytest 실행 결과 요약
</output>
