---
phase: 06-gui-integration
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - main_gui.py
autonomous: true
requirements:
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Apply 버튼 클릭 시 WIZ550 장치이면 apply_wiz550()가 호출된다"
    - "apply_wiz550()가 비밀번호를 입력받아 WIZ550Setter를 시작하고 SET_INFO를 전송한다"
    - "SET_INFO 응답 수신 시 성공 메시지(statusbar + QMessageBox)가 표시된다 (D-05: #5db872)"
    - "Reset 버튼 클릭 시 REMOTE_RESET(0xE0) 패킷이 전송된다"
    - "Factory Reset 액션 클릭 시 FACTORY_RESET(0xF0) 패킷이 전송된다"
    - "fill_setinfo_wiz550()이 _wiz550_field_widgets에서 올바른 타입으로 값을 수집한다"
    - "WIZ550 설정 쓰기 결과가 기존 WIZ5xxSR / WIZ1x0SR Apply에 회귀를 주지 않는다"
  artifacts:
    - path: "main_gui.py"
      provides: "WIZ550 Apply/Reset/FactoryReset 흐름"
      contains: "apply_wiz550"
  key_links:
    - from: "apply_wiz550()"
      to: "WIZ550Setter"
      via: "WIZ550Setter.set_done 시그널"
      pattern: "set_done.connect.*_on_wiz550_set_done"
    - from: "reset_wiz550(op_code)"
      to: "WIZ550Resetter"
      via: "WIZ550Resetter.reset_done 시그널"
      pattern: "reset_done.connect.*_on_wiz550_reset_done"
    - from: "fill_setinfo_wiz550()"
      to: "_wiz550_field_widgets"
      via: "위젯 값 읽기"
      pattern: "_wiz550_field_widgets"
---

<objective>
Wave 3: WIZ550 설정 쓰기(Apply) + Reset / Factory Reset 구현

main_gui.py에 WIZ550 설정 쓰기 흐름을 구현한다.
- fill_setinfo_wiz550() — 위젯에서 값 수집 → dict 반환
- apply_wiz550() — 비밀번호 입력 → Profile 빌드 → WIZ550Setter 시작
- _on_wiz550_set_done() — 성공/실패 메시지 표시 (D-05 컬러)
- reset_wiz550(op_code) — REMOTE_RESET(0xE0) 또는 FACTORY_RESET(0xF0) 전송
- _on_wiz550_reset_done() — 결과 표시
- Apply/Reset 버튼을 wiz550 장치일 때 wiz550 함수로 라우팅

Purpose: UI-03, UI-04 요구사항 충족.
Output: main_gui.py 수정 (5개 메서드 추가 + Apply/Reset 버튼 라우팅)

D-05 컬러: Apply 버튼 #cc785c, 성공 #5db872, 오류 #c64545
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-gui-integration/06-CONTEXT.md
@.planning/phases/06-gui-integration/06-RESEARCH.md
@.planning/phases/06-gui-integration/06-02-SUMMARY.md

<interfaces>
<!-- WIZ550Setter / WIZ550Resetter 인터페이스 -->
```python
# WIZ550Setter
WIZ550Setter(
    target_ip: str,   # local_ip 값
    target_mac: str,  # curr_mac
    password: str,    # pw_setting 값 (또는 사용자 입력)
    config_data: bytes,  # build_sr/s2e/web 반환값
    iface_ip: str = "",
    timeout: float = 5.0,
)
# set_done = pyqtSignal(bool)  — True=성공, False=실패

# WIZ550Resetter
WIZ550Resetter(
    target_ip: str,
    target_mac: str,
    password: str,
    op_code: int = OP_REMOTE_RESET,  # 0xE0 또는 0xF0
    iface_ip: str = "",
    timeout: float = 5.0,
)
# reset_done = pyqtSignal(bool)

from WIZ550MSGHandler import OP_REMOTE_RESET, OP_FACTORY_RESET
# OP_REMOTE_RESET  = 0xE0
# OP_FACTORY_RESET = 0xF0
```

WIZ550Profile 빌더 인터페이스:
```python
from WIZ550Profile import build_sr, build_s2e, build_web
# build_sr(d: dict)  -> bytes (162B)
# build_s2e(d: dict) -> bytes (162~232B 가변)
# build_web(d: dict) -> bytes (133B)
```

fill_setinfo_wiz550 위젯 값 읽기 패턴:
```python
# fill_devinfo_wiz550의 역방향
# QLabel → 건너뜀 (readonly)
# QCheckBox → .isChecked() → bool
# QComboBox → .currentData() → userData(int 또는 str)
# QLineEdit → .text() → str (ip/text) 또는 int (uint16)
```

dev_profile 구조 (GET_INFO 후):
```python
dev_profile[mac] = {
    '_proto': 'wiz550',
    'device_type': 'WIZ550SR',
    'local_ip': '192.168.0.100',
    'pw_setting': '',      # 현재 비밀번호 (빈 문자열 = 비밀번호 없음)
    ...
}
```

기존 QInputDialog 패턴 (main_gui.py line 5249):
```python
inputdlg = QInputDialog(self)
# QInputDialog.getText(parent, title, label, QLineEdit.Password)
pw, ok = QInputDialog.getText(
    self, "WIZ550 비밀번호", "설정 비밀번호 입력:", QLineEdit.Password, ""
)
```

<!-- B-04: Apply/Reset 함수명 확정 (grep 직접 확인) -->
Apply/Reset 라우팅 대상 함수 (main_gui.py 직접 확인된 실제 이름):
  - Apply:        event_setting_clicked (line 1180) → self.do_setting() 호출
  - Reset:        event_reset_clicked   (line 1186) → self.do_reset() 호출
  - FactoryReset: event_factory_setting (line 1192) → self.msg_factory_setting() → do_factory_reset("setting") 호출
                  event_factory_firmware(line 1195) → self.msg_factory_firmware() → do_factory_reset("firmware") 호출

WIZ550 분기는 event_setting_clicked / event_reset_clicked / event_factory_option_clicked 각 함수 진입부에 삽입.
Factory Reset의 경우 event_factory_option_clicked(line 1200)에서 "settings" 분기 진입부에 삽입.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fill_setinfo_wiz550 + apply_wiz550 + _on_wiz550_set_done + reset_wiz550 + _on_wiz550_reset_done 구현</name>
  <files>main_gui.py</files>
  <read_first>
    - main_gui.py (line 5160~5220 — 기존 Apply 버튼 처리 함수 확인, 비밀번호 다이얼로그 패턴)
    - main_gui.py (_on_wiz550_get_done 위치 확인 — 그 직후에 삽입)
  </read_first>
  <action>
`_on_wiz550_get_done()` 메서드 직후에 아래 5개 메서드를 삽입한다 (들여쓰기 4 스페이스).

**메서드 1: fill_setinfo_wiz550()**

```python
    def fill_setinfo_wiz550(self) -> dict:
        """
        _wiz550_field_widgets에서 사용자 입력값을 읽어 dict로 반환 (UI-03).
        readonly(QLabel) 위젯은 건너뜀.
        uint16 타입은 int 변환, ip 타입은 str 유지.
        """
        from PyQt5.QtWidgets import QLabel, QCheckBox, QComboBox, QLineEdit
        result = {}
        if not hasattr(self, '_wiz550_field_widgets'):
            return result

        for field_id, widget in self._wiz550_field_widgets.items():
            if isinstance(widget, QLabel):
                continue  # readonly — 값 수집 제외
            elif isinstance(widget, QCheckBox):
                result[field_id] = 1 if widget.isChecked() else 0
            elif isinstance(widget, QComboBox):
                # userData는 YAML choices 키 (bps int 또는 working_mode int)
                result[field_id] = widget.currentData()
            elif isinstance(widget, QLineEdit):
                text = widget.text().strip()
                # uint16 필드 (local_port, remote_port, inactivity, reconnection)
                # ip/text 필드는 str 유지
                try:
                    result[field_id] = int(text)
                except ValueError:
                    result[field_id] = text
        return result
```

**메서드 2: apply_wiz550()**

```python
    def apply_wiz550(self):
        """
        WIZ550 설정 Apply — 비밀번호 입력 → Profile 빌드 → WIZ550Setter 시작 (UI-03).
        D-05: Apply 버튼 #cc785c (신규 WIZ550 UI에만 적용, 기존 UI 불변).
        """
        if not hasattr(self, 'curr_mac') or not self.curr_mac:
            return
        d_profile = self.dev_profile.get(self.curr_mac, {})
        device_type = d_profile.get('device_type', 'WIZ550SR')

        # 비밀번호 입력 다이얼로그
        from PyQt5.QtWidgets import QInputDialog, QLineEdit
        pw, ok = QInputDialog.getText(
            self,
            "WIZ550 설정 비밀번호",
            "설정 비밀번호 (없으면 빈 칸):",
            QLineEdit.Password,
            d_profile.get('pw_setting', ''),
        )
        if not ok:
            return  # 취소

        # 위젯에서 값 수집
        d = self.fill_setinfo_wiz550()
        d['pw_setting'] = pw  # 입력한 비밀번호로 덮어쓰기

        # dev_profile의 Discovery/GET_INFO 정보 병합 (mac_bytes 등 필수 필드)
        d.update({k: v for k, v in d_profile.items() if k not in d})

        # Profile bytes 빌드
        try:
            from WIZ550Profile import build_sr, build_s2e, build_web
            builders = {
                'WIZ550SR':  build_sr,
                'WIZ550S2E': build_s2e,
                'WIZ550WEB': build_web,
            }
            builder = builders.get(device_type, build_sr)
            config_bytes = builder(d)
        except Exception as e:
            self.logger.error(f"[WIZ550] Profile 빌드 오류: {e}")
            self.statusbar.showMessage(f" WIZ550 Apply 오류: {e}")
            return

        target_ip = d.get('local_ip', '')
        if not target_ip:
            self.statusbar.showMessage(" WIZ550 Apply 오류: IP 주소 없음")
            return

        setter = WIZ550Setter(
            target_ip=target_ip,
            target_mac=self.curr_mac,
            password=pw,
            config_data=config_bytes,
            iface_ip=self.selected_eth or "",
        )
        setter.set_done.connect(self._on_wiz550_set_done)
        setter.start()
        self.statusbar.showMessage(f" WIZ550 설정 전송 중... ({self.curr_mac})")
```

**메서드 3: _on_wiz550_set_done()**

```python
    def _on_wiz550_set_done(self, success: bool):
        """WIZ550Setter 완료 콜백 — 성공/실패 메시지 표시 (D-05 컬러)."""
        from PyQt5.QtWidgets import QMessageBox
        if success:
            # D-05: 성공 색상 #5db872
            self.statusbar.showMessage(" WIZ550 설정 저장 완료")
            self.statusbar.setStyleSheet("QStatusBar { color: #5db872; }")
            QMessageBox.information(
                self, "WIZ550 Apply", "설정이 성공적으로 저장되었습니다."
            )
        else:
            # D-05: 오류 색상 #c64545
            self.statusbar.showMessage(" WIZ550 설정 저장 실패 (응답 없음 또는 비밀번호 오류)")
            self.statusbar.setStyleSheet("QStatusBar { color: #c64545; }")
            QMessageBox.warning(
                self, "WIZ550 Apply 실패",
                "설정 저장에 실패했습니다.\n비밀번호를 확인하거나 장치 연결을 확인하세요."
            )
        # statusbar 색상을 3초 후 원상복구
        from PyQt5.QtCore import QTimer
        QTimer.singleShot(3000, lambda: self.statusbar.setStyleSheet(""))
```

**메서드 4: reset_wiz550()**

```python
    def reset_wiz550(self, op_code: int = None):
        """
        WIZ550 Reset 전송 — REMOTE_RESET(0xE0) 또는 FACTORY_RESET(0xF0) (UI-04).
        op_code 기본값: OP_REMOTE_RESET
        """
        from WIZ550MSGHandler import OP_REMOTE_RESET, OP_FACTORY_RESET
        if op_code is None:
            op_code = OP_REMOTE_RESET

        if not hasattr(self, 'curr_mac') or not self.curr_mac:
            return

        d_profile = self.dev_profile.get(self.curr_mac, {})
        target_ip = d_profile.get('local_ip', '')
        if not target_ip:
            self.statusbar.showMessage(" WIZ550 Reset 오류: IP 주소 없음")
            return

        # 비밀번호 입력
        from PyQt5.QtWidgets import QInputDialog, QLineEdit
        op_name = "Factory Reset" if op_code == OP_FACTORY_RESET else "Reset"
        pw, ok = QInputDialog.getText(
            self,
            f"WIZ550 {op_name}",
            f"{op_name} 비밀번호 (없으면 빈 칸):",
            QLineEdit.Password,
            d_profile.get('pw_setting', ''),
        )
        if not ok:
            return

        resetter = WIZ550Resetter(
            target_ip=target_ip,
            target_mac=self.curr_mac,
            password=pw,
            op_code=op_code,
            iface_ip=self.selected_eth or "",
        )
        resetter.reset_done.connect(
            lambda success, name=op_name: self._on_wiz550_reset_done(success, name)
        )
        resetter.start()
        self.statusbar.showMessage(f" WIZ550 {op_name} 전송 중... ({self.curr_mac})")
```

**메서드 5: _on_wiz550_reset_done()**

```python
    def _on_wiz550_reset_done(self, success: bool, op_name: str = "Reset"):
        """WIZ550Resetter 완료 콜백 — 결과 표시 (UI-04)."""
        from PyQt5.QtWidgets import QMessageBox
        if success:
            self.statusbar.showMessage(f" WIZ550 {op_name} 완료")
            QMessageBox.information(
                self, f"WIZ550 {op_name}", f"{op_name}이 완료되었습니다."
            )
        else:
            self.statusbar.showMessage(f" WIZ550 {op_name} 실패")
            QMessageBox.warning(
                self, f"WIZ550 {op_name} 실패",
                f"{op_name}에 실패했습니다.\n장치 연결을 확인하세요."
            )
```

삽입 위치: `_on_wiz550_get_done()` 메서드 직후. 들여쓰기 4 스페이스.
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && grep -n "def fill_setinfo_wiz550\|def apply_wiz550\|def _on_wiz550_set_done\|def reset_wiz550\|def _on_wiz550_reset_done" main_gui.py</automated>
  </verify>
  <acceptance_criteria>
    - 5개 메서드 정의가 모두 존재한다
    - `grep "WIZ550Setter(" main_gui.py | grep -v "import"` 결과가 apply_wiz550 내에 존재
    - `grep "WIZ550Resetter(" main_gui.py | grep -v "import"` 결과가 reset_wiz550 내에 존재
    - `grep "set_done.connect" main_gui.py` 결과가 존재
    - `grep "reset_done.connect" main_gui.py` 결과가 존재
    - `grep "OP_FACTORY_RESET\|OP_REMOTE_RESET" main_gui.py` 결과가 존재 (reset_wiz550 내)
    - `grep "#5db872\|#c64545" main_gui.py` 결과가 _on_wiz550_set_done 내에 존재 (D-05)
  </acceptance_criteria>
  <done>5개 메서드가 main_gui.py에 추가됨. Apply/Reset/FactoryReset 흐름 완성.</done>
</task>

<task type="auto">
  <name>Task 2: Apply/Reset 버튼에 WIZ550 라우팅 연결</name>
  <files>main_gui.py</files>
  <read_first>
    - main_gui.py (line 1180~1210 — event_setting_clicked, event_reset_clicked, event_factory_option_clicked 전체 확인)
  </read_first>
  <action>
<!-- B-04: 실제 함수명 확정 (interfaces 블록에서 grep으로 미리 확인) -->

아래 3개 함수의 진입부에 WIZ550 분기를 삽입한다.
각 수정 전에 해당 라인을 Read로 확인하여 정확한 삽입 위치를 파악한다.

**수정 A: event_setting_clicked (line 1180) — Apply 라우팅**

현재 코드:
```python
def event_setting_clicked(self):
    if self.curr_dev == 'WIZ1x0SR':
        self.apply_1x0()
        return
    self.do_setting()
```

`if self.curr_dev == 'WIZ1x0SR':` 블록 직전에 wiz550 분기 삽입:
```python
def event_setting_clicked(self):
    # WIZ550 장치이면 전용 Apply 흐름으로 라우팅 (UI-03, Phase 6)
    if (hasattr(self, 'curr_mac') and self.curr_mac
            and self.dev_profile.get(self.curr_mac, {}).get('_proto') == 'wiz550'):
        self.apply_wiz550()
        return
    if self.curr_dev == 'WIZ1x0SR':
        self.apply_1x0()
        return
    self.do_setting()
```

**수정 B: event_reset_clicked (line 1186) — Reset 라우팅**

현재 코드:
```python
def event_reset_clicked(self):
    if self.curr_dev == 'WIZ1x0SR':
        self.show_msgbox("Info", "WIZ1x0SR은 Apply 시 자동으로 재시작됩니다.", QMessageBox.Information)
        return
    self.do_reset()
```

`if self.curr_dev == 'WIZ1x0SR':` 블록 직전에 wiz550 분기 삽입:
```python
def event_reset_clicked(self):
    # WIZ550 장치이면 전용 Reset 흐름으로 라우팅 (UI-04, Phase 6)
    if (hasattr(self, 'curr_mac') and self.curr_mac
            and self.dev_profile.get(self.curr_mac, {}).get('_proto') == 'wiz550'):
        from WIZ550MSGHandler import OP_REMOTE_RESET
        self.reset_wiz550(op_code=OP_REMOTE_RESET)
        return
    if self.curr_dev == 'WIZ1x0SR':
        self.show_msgbox("Info", "WIZ1x0SR은 Apply 시 자동으로 재시작됩니다.", QMessageBox.Information)
        return
    self.do_reset()
```

**수정 C: event_factory_option_clicked (line 1200) — Factory Reset 라우팅**

현재 코드:
```python
def event_factory_option_clicked(self, option):
    self.logger.info(option.text())
    opt = option.text()

    if "settings" in opt:
        self.event_factory_setting()
    elif "firmware" in opt:
        self.event_factory_firmware()
```

`if "settings" in opt:` 블록 내부를 다음과 같이 수정:
```python
def event_factory_option_clicked(self, option):
    self.logger.info(option.text())
    opt = option.text()

    if "settings" in opt:
        # WIZ550 장치이면 전용 FactoryReset 흐름으로 라우팅 (UI-04, Phase 6)
        if (hasattr(self, 'curr_mac') and self.curr_mac
                and self.dev_profile.get(self.curr_mac, {}).get('_proto') == 'wiz550'):
            from WIZ550MSGHandler import OP_FACTORY_RESET
            self.reset_wiz550(op_code=OP_FACTORY_RESET)
        else:
            self.event_factory_setting()
    elif "firmware" in opt:
        self.event_factory_firmware()
```

주의: 기존 Apply/Reset 흐름 코드는 절대 수정하지 않는다.
WIZ550 분기는 앞에 삽입하거나(A/B) 내부에서 조건 분기(C)하고 `return`으로 빠져나온다.
기존 WIZ5xxSR / WIZ1x0SR Apply/Reset에 회귀가 없어야 한다.
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && grep -n "apply_wiz550()\|reset_wiz550(" main_gui.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "apply_wiz550()" main_gui.py` 결과가 정확히 2개 존재 (정의 1 + 라우팅 호출 1)
    - `grep "reset_wiz550(" main_gui.py` 결과가 정확히 3개 존재:
        정의 1 (def reset_wiz550) + Reset 라우팅 1 (event_reset_clicked) + FactoryReset 라우팅 1 (event_factory_option_clicked)
    - Apply/Reset 라우팅 코드에 `_proto.*wiz550` 조건 체크가 존재한다
    - 각 라우팅 분기 직후 `return` 문이 존재하여 기존 코드가 실행되지 않는다 (A/B 수정)
    - `uv run pytest tests/test_wiz550_gui.py -v` 실행 시 test_setinfo_roundtrip이 XFAIL 또는 PASS
    - `uv run pytest tests/ -x` 전체 통과 (기존 테스트 회귀 없음)
  </acceptance_criteria>
  <done>
    Apply/Reset/FactoryReset 버튼이 WIZ550 장치일 때 wiz550 전용 메서드로 라우팅된다.
    reset_wiz550() 호출이 정확히 3개: 정의 1 + Reset 라우팅 1 + FactoryReset 라우팅 1 (W-2).
    기존 WIZ5xxSR / WIZ1x0SR 흐름에 회귀 없음.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI 위젯 → fill_setinfo_wiz550 → WIZ550Setter | 사용자 입력이 바이너리 패킷으로 변환되어 장치로 전송됨 |
| QInputDialog → password | 비밀번호가 plaintext로 UDP 패킷에 포함됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | QInputDialog.getText(Password mode) | accept | 네트워크 내부 도구 — 비밀번호가 UDP로 전송되나 로컬 네트워크 내부 통신 (WIZ550 원본 프로토콜 설계) |
| T-06-03-02 | Tampering | fill_setinfo_wiz550 → build_sr/s2e/web | mitigate | try/except로 build 오류 캐치 → 사용자에게 오류 메시지 표시, 전송 차단 |
| T-06-03-03 | Elevation of Privilege | Factory Reset | mitigate | 비밀번호 다이얼로그로 사용자 확인 필요. 취소 시 전송 안 함. |
| T-06-03-04 | Regression | Apply 라우팅 분기 | mitigate | `_proto == 'wiz550'` 명시적 조건 + `return` 으로 기존 코드 완전 격리 |
</threat_model>

<verification>
```bash
cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI

# 5개 메서드 존재 확인
grep -n "def fill_setinfo_wiz550\|def apply_wiz550\|def _on_wiz550_set_done\|def reset_wiz550\|def _on_wiz550_reset_done" main_gui.py

# 라우팅 분기 확인 (B-04: 실제 함수명 event_setting_clicked / event_reset_clicked / event_factory_option_clicked)
grep -n "apply_wiz550()\|reset_wiz550(" main_gui.py

# W-2: reset_wiz550 호출 수 정확히 3개 확인
# 정의 1 (def reset_wiz550) + Reset 라우팅 1 (event_reset_clicked) + FactoryReset 라우팅 1 (event_factory_option_clicked)
grep -c "reset_wiz550" main_gui.py

# D-05 컬러 토큰 확인
grep -n "#5db872\|#c64545\|#cc785c" main_gui.py

# Wave 3 테스트
uv run pytest tests/test_wiz550_gui.py::test_setinfo_roundtrip -v

# 전체 회귀 확인
uv run pytest tests/ -x
```
</verification>

<success_criteria>
- 5개 메서드 정의: fill_setinfo_wiz550, apply_wiz550, _on_wiz550_set_done, reset_wiz550, _on_wiz550_reset_done
- Apply 라우팅: event_setting_clicked에 wiz550 분기 추가 (B-04 확정)
- Reset 라우팅: event_reset_clicked에 wiz550 분기 추가 (B-04 확정)
- FactoryReset 라우팅: event_factory_option_clicked에 wiz550 분기 추가 (B-04 확정)
- reset_wiz550() 참조 정확히 3개: 정의 1 + Reset 라우팅 1 + FactoryReset 라우팅 1 (W-2 구체화)
- D-05 컬러 (#5db872, #c64545) 가 _on_wiz550_set_done 내에 존재
- uv run pytest tests/test_wiz550_gui.py -v: PASS 또는 XFAIL (FAILED 없음)
- uv run pytest tests/ -x: 전체 통과 (WIZ5xxSR / WIZ1x0SR 회귀 없음)
</success_criteria>

<output>
완료 후 .planning/phases/06-gui-integration/06-03-SUMMARY.md 생성.

포함 내용:
- 추가된 5개 메서드명과 라인 번호
- Apply/Reset/FactoryReset 라우팅 삽입 위치:
    Apply → event_setting_clicked (line 1180)
    Reset → event_reset_clicked (line 1186)
    FactoryReset → event_factory_option_clicked (line 1200, "settings" 분기)
- reset_wiz550() 호출 3개 확인 (정의 1 + Reset 1 + FactoryReset 1)
- D-05 컬러 적용 확인
- 전체 테스트 결과 요약
- 기존 WIZ5xxSR / WIZ1x0SR 회귀 없음 확인
</output>
