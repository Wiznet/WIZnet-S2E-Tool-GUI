---
phase: 06-gui-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - main_gui.py
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "WIZ550 장치 클릭 시 get_clicked_devinfo()가 wiz550 분기로 진입하여 GET_INFO를 전송한다"
    - "_build_wiz550_panel('WIZ550SR')이 QGroupBox 3개를 포함한 QWidget을 반환한다"
    - "_build_wiz550_panel('WIZ550WEB')이 QGroupBox 4개를 포함하고, disabled 필드 위젯이 isEnabled()==False이다"
    - "fill_devinfo_wiz550()이 disabled 필드(serial_command 등)에서 KeyError를 발생시키지 않는다 (WR-01 해소)"
    - "_show_wiz550_panel(True)가 wiz550_container를 표시하고 generalTab/channel_tab을 숨긴다"
    - "동일 장치 재선택 시 이전 패널이 해제되고 메모리 누수가 발생하지 않는다"
  artifacts:
    - path: "main_gui.py"
      provides: "WIZ550 UI 패널 빌드 시스템"
      contains: "_build_wiz550_panel"
  key_links:
    - from: "get_clicked_devinfo()"
      to: "_on_wiz550_get_done()"
      via: "WIZ550Getter.get_done 시그널"
      pattern: "_on_wiz550_get_done"
    - from: "_build_wiz550_panel()"
      to: "specs/devices/{device_type}.yaml"
      via: "yaml.safe_load()"
      pattern: "yaml.safe_load"
    - from: "fill_devinfo_wiz550()"
      to: "_wiz550_field_widgets"
      via: "field_id lookup"
      pattern: "_wiz550_field_widgets"
---

<objective>
Wave 2: WIZ550 설정 패널 동적 빌드 + 장치 선택 흐름 연결

main_gui.py에 WIZ550 전용 UI 패널 빌드 시스템을 구현한다.
- get_clicked_devinfo()에 wiz550 분기 추가 → WIZ550Getter 시작
- _show_wiz550_panel() 구현 → generalTab/channel_tab 교체
- _build_wiz550_panel(device_type) 구현 → YAML sections → QGroupBox → rows
- _make_wiz550_field_widget(field) 구현 → 타입별 위젯 생성
- fill_devinfo_wiz550(d) 구현 → dict → 위젯 값 채우기 (WR-01 가드)
- _on_wiz550_get_done() 구현 → GET_INFO 완료 콜백

Purpose: UI-02 요구사항 충족 — 장치 타입별 설정 패널이 동적 생성되고 값이 채워짐.
Output: main_gui.py 수정 (6개 메서드 추가 + get_clicked_devinfo 확장)

D-01: .ui 파일 수정 없이 Python 코드로 동적 생성
D-02: QVBoxLayout + QHBoxLayout 계층
D-03: spacing.xs=8px 행 간격, spacing.md=16px 패널 여백
D-04: 레이블 Fixed 폭 140px, 입력 위젯 Expanding
D-06: _build_wiz550_panel(device_type) 패턴
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-gui-integration/06-CONTEXT.md
@.planning/phases/06-gui-integration/06-RESEARCH.md
@.planning/phases/06-gui-integration/06-01-SUMMARY.md

<interfaces>
<!-- main_gui.py get_clicked_devinfo (line 3179) — wiz1x0 분기 이후에 wiz550 분기 삽입 -->
```python
def get_clicked_devinfo(self, macaddr, call_from=None):
    if macaddr not in self.dev_profile:
        ...
        return

    # WIZ1x0SR 전용 (line 3186)
    if self.dev_profile.get(macaddr, {}).get('_proto') == 'wiz1x0':
        self.curr_mac = macaddr
        self.curr_dev = 'WIZ1x0SR'
        self._show_wiz1x0_panel(True)
        self.fill_devinfo_1x0(self.dev_profile[macaddr])
        return

    # WIZ550 분기를 여기에 추가 (line 3192 앞)
    # 표준 장치: wiz1x0 패널 숨기고 기존 UI 복원 (line 3193)
    self._show_wiz1x0_panel(False)
```

_show_wiz1x0_panel 패턴 (line 3295~3300):
```python
def _show_wiz1x0_panel(self, show: bool):
    self.wiz1x0_tab.setVisible(show)
    self.generalTab.setVisible(not show)
    self.channel_tab.setVisible(not show)
    self.btn_setting.setEnabled(show)
```

dev_profile dict 구조 (WIZ550 Discovery 직후):
```python
dev_profile[mac] = {
    'device_type': 'WIZ550SR',   # 'WIZ550SR' | 'WIZ550S2E' | 'WIZ550WEB'
    'fw_version': b'\x01\x00\x00',  # fw_ver bytes (3B) — condition 판별용
    'fw_str': '1.0.0',
    'mac': 'AA:BB:CC:DD:EE:FF',
    'mac_bytes': b'\xaa\xbb\xcc\xdd\xee\xff',
    '_proto': 'wiz550',
}
```

WIZ550Getter 인터페이스:
```python
WIZ550Getter(target_mac: str, device_type: str, iface_ip: str = "", timeout: float = 5.0)
# get_done = pyqtSignal(dict)  — parse_sr/s2e/web 반환값
```

YAML section 구조 (WIZ550SR.yaml):
```
ui:
  sections:
    - id: network
      label: "Network"
      fields:
        - {id: mac, label: "MAC Address", type: readonly}
        - {id: dhcp_use, label: "DHCP", type: checkbox}
        - {id: local_ip, label: "IP Address", type: ip}
        ...
    - id: serial
      label: "Serial"
      fields:
        - id: baud_rate
          label: "Baud Rate"
          type: dropdown
          choices:
            300: "300 bps"
            ...
```

WIZ550WEB disabled 필드 (parse_web() 반환 dict에 없는 키):
  working_mode, remote_ip, remote_port, local_port, serial_command
→ fill_devinfo_wiz550에서 if field_id not in d: continue 가드 필요 (WR-01)

WIZ550S2E condition 섹션:
```yaml
- id: mqtt
  condition: mqtt     # fw_ver[1] % 2 == 1 (홀수)
- id: modbus
  condition: modbus   # fw_ver[1] % 2 == 0 and fw_ver[1] != 0 (짝수 非0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _build_wiz550_panel + _make_wiz550_field_widget + _show_wiz550_panel + fill_devinfo_wiz550 구현</name>
  <files>main_gui.py</files>
  <read_first>
    - main_gui.py (line 3295~3340 — _show_wiz1x0_panel 패턴 확인)
    - main_gui.py (line 3340~3380 — _apply_wiz1x0_compact_layout 위치 확인, 삽입 위치 파악)
    - specs/devices/WIZ550SR.yaml (전체 — sections/fields 구조)
    - specs/devices/WIZ550S2E.yaml (전체 — condition 섹션)
    - specs/devices/WIZ550WEB.yaml (전체 — disabled 필드)
  </read_first>
  <action>
`_show_wiz1x0_panel()` 메서드 (line 3295) 직전에 아래 4개 메서드를 삽입한다.
들여쓰기: 4 스페이스 (클래스 메서드 레벨).

**메서드 1: _show_wiz550_panel()**

```python
    def _show_wiz550_panel(self, show: bool):
        """WIZ550 전용 패널 ↔ 기존 generalTab 전환 (D-01, D-07).
        wiz550_container: 런타임 생성 QWidget (Wave 2에서 동적 삽입).
        """
        if hasattr(self, '_wiz550_container') and self._wiz550_container is not None:
            self._wiz550_container.setVisible(show)
        self.generalTab.setVisible(not show)
        self.channel_tab.setVisible(not show)
        # WIZ1x0SR 패널이 열려있으면 함께 닫기
        if show and hasattr(self, 'wiz1x0_tab'):
            self.wiz1x0_tab.setVisible(False)
```

**메서드 2: _build_wiz550_panel()**

```python
    def _build_wiz550_panel(self, device_type: str):
        """
        DeviceSpec YAML sections → QGroupBox → 위젯 rows 동적 빌드 (D-01, D-06).
        device_type: 'WIZ550SR' | 'WIZ550S2E' | 'WIZ550WEB'
        반환: QWidget (scroll 가능 컨테이너)
        """
        import yaml
        from pathlib import Path
        from PyQt5.QtWidgets import (
            QWidget, QVBoxLayout, QHBoxLayout, QGroupBox,
            QLabel, QLineEdit, QCheckBox, QComboBox, QScrollArea, QSizePolicy,
        )

        yaml_path = Path(__file__).parent / 'specs' / 'devices' / f'{device_type}.yaml'
        spec = yaml.safe_load(yaml_path.read_text(encoding='utf-8'))

        # condition 판별 (WIZ550S2E mqtt/modbus)
        d = self.dev_profile.get(getattr(self, 'curr_mac', ''), {})
        fw_ver = d.get('fw_version', b'\x00\x00\x00')
        if len(fw_ver) >= 2 and fw_ver[1] != 0:
            has_mqtt   = (fw_ver[1] % 2 == 1)
            has_modbus = (fw_ver[1] % 2 == 0)
        else:
            has_mqtt = has_modbus = False

        # 루트 스크롤 영역 (WEB 4섹션 대응)
        scroll = QScrollArea()
        scroll.setWidgetResizable(True)
        scroll.setFrameShape(QScrollArea.NoFrame)

        inner = QWidget()
        scroll.setWidget(inner)

        root_layout = QVBoxLayout(inner)
        root_layout.setSpacing(8)                      # D-03: spacing.xs
        root_layout.setContentsMargins(16, 16, 16, 16) # D-03: spacing.md

        self._wiz550_field_widgets = {}  # field_id → QWidget

        for section in spec.get('ui', {}).get('sections', []):
            condition = section.get('condition')
            if condition == 'mqtt'   and not has_mqtt:
                continue
            if condition == 'modbus' and not has_modbus:
                continue

            grp = QGroupBox(section.get('label', ''))
            grp_layout = QVBoxLayout(grp)
            grp_layout.setSpacing(8)          # D-03: spacing.xs

            for field in section.get('fields', []):
                widget = self._make_wiz550_field_widget(field)
                self._wiz550_field_widgets[field['id']] = widget

                row = QHBoxLayout()
                row.setSpacing(8)             # D-03: spacing.xs
                lbl = QLabel(field.get('label', field['id']))
                lbl.setFixedWidth(140)        # D-04: 레이블 고정폭
                lbl.setSizePolicy(QSizePolicy.Fixed, QSizePolicy.Preferred)
                row.addWidget(lbl)
                row.addWidget(widget)
                grp_layout.addLayout(row)

            root_layout.addWidget(grp)

        root_layout.addStretch()
        return scroll
```

**메서드 3: _make_wiz550_field_widget()**

```python
    def _make_wiz550_field_widget(self, field: dict):
        """
        field dict → 타입별 QWidget 생성 (D-04, RESEARCH Pattern 5).
        disabled:true 필드는 setEnabled(False) (WR-01 해소).
        """
        from PyQt5.QtWidgets import (
            QLabel, QLineEdit, QCheckBox, QComboBox, QSizePolicy,
        )
        ftype    = field.get('type', 'text')
        disabled = field.get('disabled', False)

        if ftype == 'readonly':
            w = QLabel(str(field.get('value', '')))
            w.setEnabled(False)
            return w

        if ftype == 'checkbox':
            w = QCheckBox()
        elif ftype == 'dropdown':
            w = QComboBox()
            for k, v in field.get('choices', {}).items():
                w.addItem(str(v), userData=k)
        elif ftype in ('ip', 'text', 'uint16'):
            w = QLineEdit()
            if ftype == 'ip':
                w.setPlaceholderText('0.0.0.0')
            elif ftype == 'uint16':
                w.setPlaceholderText('0')
        else:
            w = QLineEdit()

        if disabled:
            w.setEnabled(False)

        # D-04: 입력 위젯 Expanding (Fill)
        w.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Preferred)
        return w
```

**메서드 4: fill_devinfo_wiz550()**

```python
    def fill_devinfo_wiz550(self, d: dict):
        """
        parse_sr/s2e/web 반환 dict → _wiz550_field_widgets에 값 채우기.
        disabled 필드(serial_command 등)는 dict에 없으므로 continue 가드 (WR-01).
        """
        from PyQt5.QtWidgets import QLabel, QCheckBox, QComboBox, QLineEdit
        if not hasattr(self, '_wiz550_field_widgets'):
            return

        for field_id, widget in self._wiz550_field_widgets.items():
            if field_id not in d:
                continue  # WR-01: disabled 필드 또는 WEB 미지원 필드 — KeyError 방지

            val = d[field_id]

            if isinstance(widget, QLabel):
                widget.setText(str(val))
            elif isinstance(widget, QCheckBox):
                widget.setChecked(bool(val))
            elif isinstance(widget, QComboBox):
                # choices 키는 bps int 또는 working_mode int — userData(int)와 str 변환 비교
                matched = False
                for i in range(widget.count()):
                    stored_key = widget.itemData(i)
                    if str(stored_key) == str(val):
                        widget.setCurrentIndex(i)
                        matched = True
                        break
                if not matched:
                    self.logger.debug(f"fill_devinfo_wiz550: {field_id}={val} → choices 매칭 실패")
            elif isinstance(widget, QLineEdit):
                widget.setText(str(val))
```

삽입 위치: `_show_wiz1x0_panel()` 정의 직전 (line 3295 앞).
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && grep -n "def _show_wiz550_panel\|def _build_wiz550_panel\|def _make_wiz550_field_widget\|def fill_devinfo_wiz550" main_gui.py</automated>
  </verify>
  <acceptance_criteria>
    - 4개 메서드 정의가 모두 존재한다: _show_wiz550_panel, _build_wiz550_panel, _make_wiz550_field_widget, fill_devinfo_wiz550
    - `grep "if field_id not in d:" main_gui.py` 결과 존재 (WR-01 가드)
    - `grep "yaml.safe_load" main_gui.py` 결과 존재 (_build_wiz550_panel 내부)
    - `grep "disabled.*field.get" main_gui.py` 또는 `grep "disabled = field.get" main_gui.py` 결과 존재
    - `grep "setSizePolicy.*Expanding" main_gui.py | grep -i wiz550` 또는 메서드 내부에 Expanding 존재
    - `grep "condition.*mqtt\|condition.*modbus" main_gui.py` 결과 존재
  </acceptance_criteria>
  <done>4개 메서드가 main_gui.py에 추가됨. WR-01 가드 포함.</done>
</task>

<task type="auto">
  <name>Task 2: get_clicked_devinfo()에 wiz550 분기 추가 + _on_wiz550_get_done 구현 + 컨테이너 초기화</name>
  <files>main_gui.py</files>
  <read_first>
    - main_gui.py (line 3179~3200 — get_clicked_devinfo 전체 흐름)
    - main_gui.py (line 870~890 — __init__ 초기화 블록)
  </read_first>
  <action>
세 위치를 수정한다.

**수정 A: __init__ 초기화에 _wiz550_container 속성 추가**

line 879~882 근처의 wiz550 초기화 블록 (Task 1-01에서 추가한 블록) 직후에 삽입:
```python
        # WIZ550 동적 패널 컨테이너 (런타임 생성, 초기 None)
        self._wiz550_container = None
        self._wiz550_field_widgets = {}
```

**수정 B: get_clicked_devinfo()에 wiz550 분기 추가**

line 3186~3191 (wiz1x0 분기 블록) 직후, line 3193 (`# 표준 장치`) 직전에 삽입:

```python
        # WIZ550 전용 UI 패널 (Phase 6 — UI-02)
        if self.dev_profile.get(macaddr, {}).get('_proto') == 'wiz550':
            self.curr_mac = macaddr
            d = self.dev_profile[macaddr]
            device_type = d.get('device_type', 'WIZ550SR')

            # 패널 컨테이너 최초 생성 또는 장치 타입 변경 시 재빌드
            if (self._wiz550_container is None
                    or getattr(self, '_wiz550_last_type', None) != device_type):
                new_panel = self._build_wiz550_panel(device_type)
                # 기존 패널 해제 (Pitfall 4: 메모리 누수 방지)
                if self._wiz550_container is not None:
                    self._wiz550_container.setParent(None)
                # 컨테이너를 generalTab과 동일한 parent layout에 삽입
                parent_widget = self.generalTab.parentWidget()
                if parent_widget is not None and parent_widget.layout() is not None:
                    parent_widget.layout().addWidget(new_panel)
                else:
                    # fallback: centralWidget layout
                    self.centralWidget().layout().addWidget(new_panel)
                self._wiz550_container = new_panel
                self._wiz550_last_type = device_type
                self._wiz550_container.setVisible(False)

            self._show_wiz550_panel(True)

            # GET_INFO로 최신 설정 읽기 (Pitfall 5: 패널 빌드 후 GET_INFO)
            getter = WIZ550Getter(
                target_mac=macaddr,
                device_type=device_type,
                iface_ip=self.selected_eth or "",
            )
            getter.get_done.connect(
                lambda cfg, mac=macaddr, dtype=device_type:
                    self._on_wiz550_get_done(cfg, mac, dtype)
            )
            getter.start()
            self.statusbar.showMessage(f" WIZ550 설정 읽는 중... ({macaddr})")
            return
```

**수정 C: _on_wiz550_get_done() 메서드 추가**

`fill_devinfo_wiz550()` 메서드 직후에 삽입:

```python
    def _on_wiz550_get_done(self, cfg: dict, macaddr: str, device_type: str):
        """WIZ550Getter 완료 콜백 — GET_INFO 응답을 dev_profile에 merge하고 UI에 채운다 (UI-02)."""
        if not cfg:
            self.statusbar.showMessage(f" WIZ550 설정 읽기 실패: {macaddr}")
            self.logger.warning(f"[WIZ550] GET_INFO 응답 없음: {macaddr}")
            return

        # dev_profile에 병합 (Discovery 정보 보존)
        self.dev_profile.setdefault(macaddr, {}).update(cfg)
        self.logger.info(f"[WIZ550] GET_INFO 완료: {macaddr} ({device_type})")

        # UI 채우기
        self.fill_devinfo_wiz550(cfg)
        self.statusbar.showMessage(f" WIZ550 설정 로드 완료: {macaddr}")
```
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && grep -n "def _on_wiz550_get_done\|_proto.*wiz550\|_wiz550_container\|_wiz550_last_type" main_gui.py | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def _on_wiz550_get_done" main_gui.py` 결과 1개 존재
    - `grep "_proto.*==.*wiz550" main_gui.py` 결과가 get_clicked_devinfo 내에 존재
    - `grep "_wiz550_container = None" main_gui.py` 결과가 __init__ 내에 존재
    - `grep "WIZ550Getter(" main_gui.py` 결과가 get_clicked_devinfo 내에 존재
    - `grep "getter.get_done.connect" main_gui.py` 결과 존재
    - `grep "setter.setParent(None)\|_wiz550_container.*setParent" main_gui.py` 결과 존재 (메모리 누수 방지)
    - `uv run pytest tests/test_wiz550_gui.py -x` 실행 시 기존 상태 유지 (XFAIL/PASS)
  </acceptance_criteria>
  <done>
    get_clicked_devinfo()에 wiz550 분기가 추가되고,
    WIZ550Getter가 시작되어 설정을 읽고,
    _on_wiz550_get_done()이 UI를 채우는 흐름이 완성됨
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET_INFO 응답 dict → fill_devinfo_wiz550 | 장치가 보내온 설정값이 UI 위젯에 직접 표시됨 |
| YAML file → _build_wiz550_panel | 로컬 YAML 파일을 safe_load로 파싱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | fill_devinfo_wiz550 QLineEdit.setText() | accept | 네트워크 내부 도구 — 악의적 장치가 보낸 값이 UI에 표시될 수 있지만 사용자가 확인 후 Apply |
| T-06-02-02 | Information Disclosure | yaml.safe_load(local file) | accept | 로컬 YAML — 외부 입력 아님, safe_load 사용으로 코드 실행 위험 없음 |
| T-06-02-03 | DoS | _build_wiz550_panel 재진입 | mitigate | `_wiz550_last_type` 캐시로 동일 타입 재선택 시 재빌드 스킵. setParent(None)으로 이전 패널 해제 |
</threat_model>

<verification>
```bash
cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI

# 핵심 메서드 존재 확인
grep -n "def _show_wiz550_panel\|def _build_wiz550_panel\|def _make_wiz550_field_widget\|def fill_devinfo_wiz550\|def _on_wiz550_get_done" main_gui.py

# WR-01 가드 확인
grep -n "if field_id not in d:" main_gui.py

# 분기 확인
grep -n "_proto.*wiz550" main_gui.py

# Wave 2 테스트 (xfail → green 전환 시도)
uv run pytest tests/test_wiz550_gui.py::test_build_panel_sections -v
uv run pytest tests/test_wiz550_gui.py::test_disabled_field_widget -v

# 전체 회귀
uv run pytest tests/ -x
```
</verification>

<success_criteria>
- 5개 메서드 정의 존재: _show_wiz550_panel, _build_wiz550_panel, _make_wiz550_field_widget, fill_devinfo_wiz550, _on_wiz550_get_done
- get_clicked_devinfo()에 wiz550 분기 존재
- WR-01 가드 (`if field_id not in d: continue`) 존재
- test_build_panel_sections, test_disabled_field_widget: PASS 또는 XFAIL
- uv run pytest tests/ -x: 전체 통과
</success_criteria>

<output>
완료 후 .planning/phases/06-gui-integration/06-02-SUMMARY.md 생성.

포함 내용:
- 추가된 5개 메서드명과 라인 번호
- wiz550 분기 삽입 위치 (line 번호)
- WR-01 가드 구현 확인
- condition 처리 방식 (mqtt/modbus)
- 컨테이너 캐싱 전략 (_wiz550_last_type)
</output>
