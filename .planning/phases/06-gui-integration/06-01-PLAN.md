---
phase: 06-gui-integration
plan: "01"
type: execute
wave: 1
depends_on:
  - "06-00"
files_modified:
  - main_gui.py
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Search 버튼 클릭 시 WIZ550Searcher가 항상 병행 시작된다 (체크박스 불필요)"
    - "_merge_wiz550_results가 호출되면 WIZ550 장치가 기존 list_device 테이블에 연두색 배경으로 추가된다"
    - "search_each_dev()가 _proto='wiz550' 장치를 WIZ5xxSR 텍스트 커맨드 파이프라인에서 제외한다"
    - "WIZ550 장치가 dev_profile에 _proto='wiz550' 키와 함께 저장된다"
  artifacts:
    - path: "main_gui.py"
      provides: "WIZ550 검색 통합 — search_pre() 확장 + _merge_wiz550_results + search_each_dev 필터"
      contains: "_merge_wiz550_results"
  key_links:
    - from: "main_gui.py search_pre()"
      to: "WIZ550Searcher"
      via: "wiz550_searcher.start()"
      pattern: "WIZ550Searcher"
    - from: "WIZ550Searcher.search_done"
      to: "_merge_wiz550_results"
      via: "connect()"
      pattern: "search_done.connect.*_merge_wiz550_results"
    - from: "search_each_dev"
      to: "dev_profile._proto"
      via: "필터 조건"
      pattern: "_proto.*wiz550"
---

<objective>
Wave 1: WIZ550 검색 통합

main_gui.py search_pre()에 WIZ550Searcher 병행 시작 코드를 추가하고,
_merge_wiz550_results() 메서드를 구현하여 검색 결과를 기존 장치 목록에 병합한다.
search_each_dev()에 wiz550 필터를 추가하여 잘못된 WIZ5xxSR 커맨드 전송을 방지한다.

Purpose: UI-01 요구사항 충족 — WIZ550 장치가 기존 검색 UI에 통합 표시됨.
Output: main_gui.py 수정 (search_pre + _merge_wiz550_results + search_each_dev 필터)

D-07 결정 구현:
  - search_pre() → WIZ550Searcher 항상 병행 시작 (체크박스 불필요)
  - wiz550_search_done 시그널 = WIZ550Searcher.search_done
  - search_each_dev() WIZ550 분기 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-gui-integration/06-CONTEXT.md
@.planning/phases/06-gui-integration/06-RESEARCH.md

<interfaces>
<!-- main_gui.py 핵심 참조 지점 (직접 확인된 라인 번호) -->

WIZ1x0Searcher 시작 블록 (line 2322~2340, 직접 복제 모델):
```python
# line 2322~2336 — WIZ1x0SR 검색 시작 (WIZ550도 동일 패턴)
self._search_phase3_done = False
if self.chk_wiz1x0_search.isChecked():
    if self.wiz1x0_searcher is None or not self.wiz1x0_searcher.isRunning():
        self._wiz1x0_search_pending = True
        self.wiz1x0_searcher = WIZ1x0Searcher(
            iface_ip=self.selected_eth if self.selected_eth else "",
            repeat=3,
            timeout=self.search_pre_wait_time,
        )
        self.wiz1x0_searcher.search_done.connect(self._merge_wiz1x0_results)
        self.wiz1x0_searcher.start()
```

_merge_wiz1x0_results 패턴 (line 2342~2386, 직접 복제 모델):
```python
def _merge_wiz1x0_results(self, results: list):
    self._wiz1x0_search_pending = False
    existing_macs = self.mac_list_str()
    new_results = [(mac, d) for mac, d in results if mac not in existing_macs]
    if new_results:
        for mac_str, board_dict in new_results:
            self.mac_list.append(mac_str.encode())
            self.mn_list.append("WIZ1x0SR")
            ver = board_dict.get('appver_str', '0.0')
            self.vr_list.append(ver.encode())
            self.st_list.append(b'normal')
            self.mode_list.append(b'0')
            self.detected_list.append(True)
            self.dev_profile[mac_str] = board_dict
        self.searched_devnum = len(self.mac_list)
        self.searched_num.setText(str(self.searched_devnum))
        _wiz1x0_bg = QtGui.QColor(0xE0, 0xF4, 0xFF)
        for mac_str, board_dict in new_results:
            row = self.list_device.rowCount()
            self.list_device.insertRow(row)
            for col, text in [(0, mac_str), (1, "WIZ1x0SR"), (2, "✓")]:
                item = QTableWidgetItem(text)
                item.setBackground(_wiz1x0_bg)
                self.list_device.setItem(row, col, item)
        self.list_device.resizeRowsToContents()
        QApplication.processEvents()
```

search_each_dev 필터 (line 2422~2425):
```python
dev_info_list = [
    d for d in dev_info_list
    if self.dev_profile.get(d[0], {}).get('_proto') != 'wiz1x0'
]
```

__init__ 초기화 블록 (line 874~882):
```python
# line 874~882
self.wiz1x0_searcher = None
self._wiz1x0_search_pending = False
self._search_phase3_done = False
self.wiz1x0_tab.setVisible(False)
self._connect_wiz1x0_signals()
```

mac_list_str() (line 2388~2396):
```python
def mac_list_str(self):
    result = set()
    for m in self.mac_list:
        try:
            result.add(m.decode('utf-8', errors='replace'))
        except Exception:
            pass
    return result
```

WIZ550Searcher 시그널:
```python
from WIZ550MSGHandler import WIZ550Searcher
# WIZ550Searcher(iface_ip: str = "", timeout: float = 15.0)
# search_done = pyqtSignal(list)
# device_dict 키: device_type, fw_str, mac, mac_bytes, _proto='wiz550'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: __init__ 초기화 블록에 WIZ550 searcher 속성 추가</name>
  <files>main_gui.py</files>
  <read_first>
    - main_gui.py (line 870~890 — WIZ1x0SR 초기화 블록 확인)
    - main_gui.py (line 1~80 — import 블록 확인)
  </read_first>
  <action>
두 가지 수정이 필요하다:

**수정 1: import 추가 (line 1~80 import 블록 끝 근처)**

main_gui.py 상단 import 블록에서 WIZ550MSGHandler 관련 import가 없으면 추가한다.
기존에 있으면 스킵.

추가 위치: `from WIZ1x0MSGHandler import ...` 라인 직후 (혹은 import 블록 끝):
```python
from WIZ550MSGHandler import (
    WIZ550Searcher,
    WIZ550Getter,
    WIZ550Setter,
    WIZ550Resetter,
    OP_REMOTE_RESET,
    OP_FACTORY_RESET,
)
```

**수정 2: __init__ 초기화 블록 (line 879 `self.wiz1x0_tab.setVisible(False)` 직후)**

line 880 `self._connect_wiz1x0_signals()` 전후에 아래 코드를 삽입한다:
```python
        # WIZ550 검색 스레드 초기화 (Phase 6 — UI-01, D-07)
        self.wiz550_searcher = None
        self._wiz550_search_pending = False
```

삽입 위치 기준: `self.wiz1x0_tab.setVisible(False)` 라인 바로 다음 줄.
들여쓰기: 8 스페이스 (기존 __init__ 블록과 동일).
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && grep -n "wiz550_searcher\|WIZ550Searcher\|WIZ550Getter\|WIZ550Setter\|WIZ550Resetter" main_gui.py | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from WIZ550MSGHandler import" main_gui.py` 결과에 WIZ550Searcher가 포함된다
    - `grep "self.wiz550_searcher = None" main_gui.py` 결과가 1개 이상 존재한다
    - `grep "self._wiz550_search_pending = False" main_gui.py` 결과가 1개 이상 존재한다
    - 추가 위치가 `wiz1x0_tab.setVisible(False)` 이후, `_connect_wiz1x0_signals()` 이전이다
  </acceptance_criteria>
  <done>WIZ550MSGHandler import와 wiz550_searcher 초기화 속성이 main_gui.py에 추가됨</done>
</task>

<task type="auto">
  <name>Task 2: search_pre()에 WIZ550Searcher 병행 시작 + _merge_wiz550_results 구현 + search_each_dev 필터</name>
  <files>main_gui.py</files>
  <read_first>
    - main_gui.py (line 2320~2345 — WIZ1x0Searcher 시작 블록 + _merge_wiz1x0_results 시작)
    - main_gui.py (line 2414~2430 — search_each_dev 필터 블록)
    - main_gui.py (line 2374~2390 — _merge_wiz1x0_results 끝 + mac_list_str)
  </read_first>
  <action>
세 위치에 코드를 추가/수정한다. 각 수정은 독립적이다.

**수정 A: search_pre() — WIZ550Searcher 병행 시작**

line 2340 (`self._wiz1x0_search_pending = False`) 직후에 삽입:

```python
                # WIZ550 검색 — 항상 병행 시작 (체크박스 없이, D-07)
                # 이전 searcher 실행 중이면 새로 시작하지 않음 (WinError 10048 방지)
                if self.wiz550_searcher is None or not self.wiz550_searcher.isRunning():
                    self._wiz550_search_pending = True
                    self.wiz550_searcher = WIZ550Searcher(
                        iface_ip=self.selected_eth if self.selected_eth else "",
                        timeout=self.search_pre_wait_time,
                    )
                    self.wiz550_searcher.search_done.connect(self._merge_wiz550_results)
                    self.wiz550_searcher.start()
                    self.logger.info(f"[TIMING] {self._T()} WIZ550Searcher.start() 완료")
                else:
                    self.logger.info(f"[TIMING] {self._T()} WIZ550Searcher 이미 실행 중 — skip")
```

들여쓰기: 16 스페이스 (기존 `self._wiz1x0_search_pending = False`와 동일한 들여쓰기 레벨).
search_pre_wait_time은 기존 WIZ1x0Searcher의 timeout과 동일하게 사용.

**수정 B: _merge_wiz550_results() 새 메서드 추가**

`_merge_wiz1x0_results()` 메서드 (line 2342) 직후, `mac_list_str()` (line 2388) 이전에
다음 메서드를 삽입한다 (들여쓰기 4 스페이스, 클래스 메서드 레벨):

```python
    def _merge_wiz550_results(self, results: list):
        """WIZ550Searcher 완료 콜백 — 검색 결과를 기존 장치 목록에 병합 (UI-01, D-07)."""
        self._wiz550_search_pending = False
        self.logger.info(f"[WIZ550] 검색 완료: {len(results)}개")

        existing_macs = self.mac_list_str()
        new_results = [d for d in results if d['mac'] not in existing_macs]
        if new_results:
            _wiz550_bg = QtGui.QColor(0xD0, 0xFF, 0xD0)  # 연한 녹색 배경 (WIZ1x0 하늘색과 구분)
            for device_dict in new_results:
                mac_str = device_dict['mac']
                self.mac_list.append(mac_str.encode())
                self.mn_list.append(device_dict.get('device_type', 'WIZ550SR'))
                self.vr_list.append(device_dict.get('fw_str', '').encode())
                self.st_list.append(b'normal')
                self.mode_list.append(b'0')
                self.detected_list.append(True)
                self.dev_profile[mac_str] = device_dict  # _proto='wiz550' 포함

            self.searched_devnum = len(self.mac_list)
            self.searched_num.setText(str(self.searched_devnum))

            for device_dict in new_results:
                mac_str = device_dict['mac']
                row = self.list_device.rowCount()
                self.list_device.insertRow(row)
                for col, text in [
                    (0, mac_str),
                    (1, device_dict.get('device_type', 'WIZ550SR')),
                    (2, device_dict.get('fw_str', '')),
                ]:
                    item = QTableWidgetItem(text)
                    item.setBackground(_wiz550_bg)
                    self.list_device.setItem(row, col, item)
            self.list_device.resizeRowsToContents()
            QApplication.processEvents()
        else:
            self.logger.debug("[WIZ550] 신규 장치 없음 (모두 중복 또는 결과 없음)")
```

**수정 C: search_each_dev() — wiz550 필터 추가**

line 2422~2425의 기존 wiz1x0 필터:
```python
        dev_info_list = [
            d for d in dev_info_list
            if self.dev_profile.get(d[0], {}).get('_proto') != 'wiz1x0'
        ]
```

이 코드를 다음과 같이 교체한다 (wiz550도 함께 제외):
```python
        # WIZ1x0SR + WIZ550: 바이너리 프로토콜 장치 → WIZ5xxSR 텍스트 커맨드 제외 (UI-01, Pitfall 3)
        _binary_proto = ('wiz1x0', 'wiz550')
        dev_info_list = [
            d for d in dev_info_list
            if self.dev_profile.get(d[0], {}).get('_proto') not in _binary_proto
        ]
```
  </action>
  <verify>
    <automated>cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI && grep -n "def _merge_wiz550_results\|WIZ550Searcher.*start\|wiz550.*wiz1x0\|_binary_proto" main_gui.py | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _merge_wiz550_results" main_gui.py` = 1
    - `grep "WIZ550Searcher" main_gui.py | grep "start()"` 결과가 1개 이상 존재한다
    - `grep "_binary_proto" main_gui.py` 결과가 존재한다 (search_each_dev 필터 교체 확인)
    - `grep "search_done.connect.*_merge_wiz550_results" main_gui.py` 결과가 존재한다
    - `grep "self.dev_profile\[mac_str\] = device_dict" main_gui.py` 결과가 존재한다 (_proto 포함 dict 저장)
    - `uv run pytest tests/test_wiz550_gui.py -x` 실행 시 기존 PASS/XFAIL 상태 유지
  </acceptance_criteria>
  <done>
    search_pre()에 WIZ550Searcher 시작 코드가 추가되고,
    _merge_wiz550_results()가 클래스 메서드로 구현되고,
    search_each_dev()가 wiz550 장치를 필터링한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WIZ550Searcher → main_gui | QThread에서 시그널로 결과 전달 — GUI 스레드 직접 접근 없음 |
| results list → dev_profile | 장치가 보내온 mac/device_type을 신뢰 없이 dict에 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Spoofing | _merge_wiz550_results | accept | 네트워크 내부 도구 — 위조 MAC 장치 표시는 실운영 위험 없음 (현장 엔지니어 전용) |
| T-06-01-02 | Tampering | search_each_dev 필터 | mitigate | `_binary_proto` 튜플로 wiz1x0 + wiz550 모두 명시적 제외. 새 프로토콜 추가 시 튜플에 추가 필요 |
| T-06-01-03 | Denial of Service | WIZ550Searcher 중복 실행 | mitigate | `isRunning()` 체크로 중복 bind(6550) 방지 — WinError 10048 예방 |
</threat_model>

<verification>
```bash
cd D:/user/Documents/GitHub/WIZnet-S2E-Tool-GUI

# 핵심 함수 존재 확인
grep -n "def _merge_wiz550_results\|WIZ550Searcher\|_binary_proto" main_gui.py

# 테스트 실행 (xfail 포함 전체 통과 확인)
uv run pytest tests/test_wiz550_gui.py -v

# 전체 테스트 회귀 확인
uv run pytest tests/ -x
```
</verification>

<success_criteria>
- main_gui.py에 _merge_wiz550_results() 메서드가 존재한다
- search_pre() 내에 WIZ550Searcher 시작 코드가 존재한다
- search_each_dev() 필터가 wiz550을 제외한다
- uv run pytest tests/test_wiz550_gui.py -x: PASS (xfail 포함)
- uv run pytest tests/ -x: 전체 통과 (Phase 4 테스트 회귀 없음)
</success_criteria>

<output>
완료 후 .planning/phases/06-gui-integration/06-01-SUMMARY.md 생성.

포함 내용:
- 수정된 함수명과 라인 번호
- WIZ550Searcher 시작 조건 (항상 병행)
- _merge_wiz550_results 구현 내용 요약
- search_each_dev 필터 변경 내용
</output>
