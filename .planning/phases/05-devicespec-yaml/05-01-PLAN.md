---
phase: 05-devicespec-yaml
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-00"
files_modified:
  - specs/devices/WIZ550SR.yaml
  - specs/devices/WIZ550S2E.yaml
  - specs/devices/WIZ550WEB.yaml
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04

must_haves:
  truths:
    - "uv run python validate_schemas.py 가 WIZ550SR/S2E/WEB.yaml 3개 포함 후 종료 코드 0"
    - "WIZ550SR.yaml 의 baud_rate choices 키가 실제 bps 정수값 (115200, 460800 등)"
    - "WIZ550S2E.yaml 에 condition:mqtt 섹션(mqtt_pub_topic/mqtt_sub_topic)과 condition:modbus 섹션이 존재"
    - "WIZ550WEB.yaml 에 working_mode/remote_ip/remote_port/local_port/serial_command 가 disabled:true 로 포함"
    - "WIZ550WEB.yaml 에 pw_connect 필드가 없음 (WEB 구조체 미포함)"
    - "uv run pytest tests/test_wiz550_spec.py -v 가 전체 PASS"
  artifacts:
    - path: "specs/devices/WIZ550SR.yaml"
      provides: "WIZ550SR DeviceSpec (SPEC-01)"
      contains: "family: wiz550"
    - path: "specs/devices/WIZ550S2E.yaml"
      provides: "WIZ550S2E DeviceSpec with MQTT/Modbus conditions (SPEC-02)"
      contains: "condition: mqtt"
    - path: "specs/devices/WIZ550WEB.yaml"
      provides: "WIZ550WEB DeviceSpec with disabled fields (SPEC-03)"
      contains: "disabled: true"
  key_links:
    - from: "specs/devices/WIZ550SR.yaml"
      to: "WIZ550Profile.parse_sr()"
      via: "field.id 와 dict 키 1:1 매핑"
      pattern: "id: baud_rate|local_ip|working_mode|serial_command"
    - from: "specs/devices/WIZ550S2E.yaml"
      to: "WIZ550Profile.parse_s2e()"
      via: "mqtt_pub_topic/mqtt_sub_topic field.id"
      pattern: "mqtt_pub_topic|mqtt_sub_topic"
    - from: "specs/devices/WIZ550WEB.yaml"
      to: "WIZ550Profile.parse_web()"
      via: "uart0_*/uart1_* field.id"
      pattern: "uart0_baud_rate|uart1_baud_rate"
---

<objective>
WIZ550SR, WIZ550S2E, WIZ550WEB 세 장치의 DeviceSpec YAML 파일을 작성한다.
각 YAML 은 device.wiz550.schema.json 검증을 통과하고, field.id 가 WIZ550Profile.py 반환 dict 키와 정확히 일치한다.

Purpose: Phase 6 _build_wiz550_panel(device_type) 함수가 spec.sections.items() 로 YAML 을 순회하여 UI 위젯을 동적 생성할 수 있는 데이터 소스를 제공한다.
Output: specs/devices/WIZ550SR.yaml, WIZ550S2E.yaml, WIZ550WEB.yaml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-devicespec-yaml/05-CONTEXT.md
@.planning/phases/05-devicespec-yaml/05-RESEARCH.md
@.planning/phases/05-devicespec-yaml/05-00-SUMMARY.md

<interfaces>
<!-- WIZ550Profile.py 에서 확인된 정확한 필드명 — YAML field.id 의 단일 진실 소스 -->
<!-- Phase 6 에서 dev_info[field.id] 로 직접 조회하므로 이름이 완전히 일치해야 함 -->

parse_sr() / parse_s2e() 공유 기본 162B 필드 (_parse_base_162 반환):
```
mac               str    — 'XX:XX:XX:XX:XX:XX', type: readonly
local_ip          str    — '0.0.0.0', type: ip
gateway           str    — '0.0.0.0', type: ip
subnet            str    — '255.255.255.0', type: ip
working_mode      int    — 0=TCP Client, 1=TCP Server, 2=UDP, type: dropdown
remote_ip         str    — '0.0.0.0', type: ip
local_port        int    — 0~65535, type: uint16
remote_port       int    — 0~65535, type: uint16
baud_rate         int    — 실제 bps 값 (115200 등), NOT 인덱스!, type: dropdown, choices키=bps값
data_bits         int    — 값 3 = 8bit (SR은 8bit만 지원), type: readonly, value: "8-bit"
parity            int    — 0=None, 1=Odd, 2=Even, type: dropdown
stop_bits         int    — 0=1bit, 1=2bit, type: dropdown
flow_control      int    — 0=None, 2=RTS/CTS (1=XON/XOFF 지원 안 함!), type: dropdown
pw_setting        str    — 10s, type: text
pw_connect        str    — 10s, type: text (WEB 에는 없음!)
dhcp_use          int    — 0=Static, 1=DHCP, type: checkbox
dns_use           int    — type: checkbox
dns_server_ip     str    — type: ip
serial_command    int    — at_cmd 플래그, type: checkbox
```

parse_s2e() MQTT 확장 필드 (condition: mqtt):
```
mqtt_user         str    — 10s, type: text
mqtt_pw           str    — 10s, type: text
mqtt_pub_topic    str    — 25s, type: text  ← 주의: publish_topic 아님
mqtt_sub_topic    str    — 25s, type: text  ← 주의: subscribe_topic 아님
```

parse_s2e() Modbus 확장 필드 (condition: modbus):
```
modbus_use        int    — type: checkbox
modbus_mode       int    — 0=RTU, 1=ASCII, type: dropdown
```

parse_web() 전용 필드 (WEB 133B):
```
mac               str    — type: readonly
local_ip          str    — type: ip
gateway           str    — type: ip
subnet            str    — type: ip
dhcp_use          int    — type: checkbox
dns_use           int    — type: checkbox
dns_server_ip     str    — type: ip
dns_domain_name   str    — type: text
uart0_baud_rate   int    — 실제 bps, type: dropdown
uart0_data_bits   int    — 값 3=8bit, type: readonly, value: "8-bit"
uart0_parity      int    — type: dropdown
uart0_stop_bits   int    — type: dropdown
uart0_flow_control int   — type: dropdown
uart1_baud_rate   int    — type: dropdown
uart1_data_bits   int    — type: readonly, value: "8-bit"
uart1_parity      int    — type: dropdown
uart1_stop_bits   int    — type: dropdown
uart1_flow_control int   — type: dropdown
pw_setting        str    — type: text
(pw_connect 없음 — WEB 구조체 미포함)
```

WEB 에서 disabled:true 로 포함할 필드 (구조체에 없음, SPEC-03):
```
working_mode, remote_ip, remote_port, local_port, serial_command
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WIZ550SR.yaml 작성 (SPEC-01, per D-02/D-03/D-06)</name>
  <read_first>
    - WIZ550Profile.py — _parse_base_162() 반환 dict 키 전체 (SR field.id 의 단일 진실 소스)
    - .planning/phases/05-devicespec-yaml/05-RESEARCH.md — Architecture Patterns 섹션 3 (SR YAML 패턴), Pitfall 1 (baud_rate choices)
    - .planning/phases/05-devicespec-yaml/05-CONTEXT.md — D-02 (최상위 구조), D-06 (SR 섹션 구성)
    - specs/schema/device.wiz550.schema.json — 검증 대상 스키마 (required 필드 확인)
  </read_first>
  <files>specs/devices/WIZ550SR.yaml</files>
  <behavior>
    - test_wiz550sr_schema_valid: validate_schemas.py 가 WIZ550SR.yaml 포함 후 종료 코드 0
    - test_wiz550sr_field_ids_match_profile: mac/local_ip/gateway/subnet/working_mode/remote_ip/local_port/remote_port/baud_rate/data_bits/parity/stop_bits/flow_control/pw_setting/pw_connect/dhcp_use/dns_use/dns_server_ip/serial_command 모두 field.id 에 포함
  </behavior>
  <action>
아래 정확한 내용으로 specs/devices/WIZ550SR.yaml 을 작성한다.

**핵심 제약 (위반 시 Phase 6 파괴):**
1. baud_rate choices 키 = 실제 bps 정수값 (300, 600, ..., 460800) — 인덱스(0,1,2...) 절대 금지
2. flow_control choices: {0: "None", 2: "RTS/CTS"} — XON/XOFF(1) 제외 (PROF-01 제약)
3. data_bits: type: readonly, value: "8-bit" — SR 은 8-bit 전용
4. protocol.handler: WIZ550MSGHandler — 모듈 파일명 기준 (Phase 6 에서 실제 클래스 매핑)

```yaml
# specs/devices/WIZ550SR.yaml
# WIZ550SR DeviceSpec — Phase 5 (SPEC-01)
# field.id 기준: WIZ550Profile._parse_base_162() 반환 dict 키
# Phase 6: _build_wiz550_panel('WIZ550SR') 이 이 파일을 로드하여 UI 동적 생성

name: WIZ550SR
display_name: "WIZ550SR"
aliases:
  - "WIZ550SR"
family: wiz550
channels: 1

protocol:
  handler: WIZ550MSGHandler
  port: 6550
  product_code: [0x02, 0x00, 0x00]
  config_size: 162
  parser: parse_sr
  builder: build_sr

ui:
  sections:
    - id: network
      label: "Network"
      fields:
        - {id: mac,           label: "MAC Address",  type: readonly}
        - {id: dhcp_use,      label: "DHCP",         type: checkbox}
        - {id: local_ip,      label: "IP Address",   type: ip}
        - {id: subnet,        label: "Subnet Mask",  type: ip}
        - {id: gateway,       label: "Gateway",      type: ip}
        - {id: dns_server_ip, label: "DNS Server",   type: ip}
        - id: working_mode
          label: "Working Mode"
          type: dropdown
          choices:
            0: "TCP Client"
            1: "TCP Server"
            2: "UDP"
        - {id: local_port,  label: "Local Port",   type: uint16}
        - {id: remote_ip,   label: "Remote IP",    type: ip}
        - {id: remote_port, label: "Remote Port",  type: uint16}

    - id: serial
      label: "Serial"
      fields:
        - id: baud_rate
          label: "Baud Rate"
          type: dropdown
          choices:
            300: "300 bps"
            600: "600 bps"
            1200: "1200 bps"
            2400: "2400 bps"
            4800: "4800 bps"
            9600: "9600 bps"
            19200: "19200 bps"
            38400: "38400 bps"
            57600: "57600 bps"
            115200: "115200 bps"
            230400: "230400 bps"
            460800: "460800 bps"
        - {id: data_bits,    label: "Data Bits",    type: readonly, value: "8-bit"}
        - id: parity
          label: "Parity"
          type: dropdown
          choices:
            0: "None"
            1: "Odd"
            2: "Even"
        - id: stop_bits
          label: "Stop Bits"
          type: dropdown
          choices:
            0: "1 bit"
            1: "2 bits"
        - id: flow_control
          label: "Flow Control"
          type: dropdown
          choices:
            0: "None"
            2: "RTS/CTS"

    - id: options
      label: "Options"
      fields:
        - {id: pw_setting,     label: "Search Password",  type: text}
        - {id: pw_connect,     label: "Connect Password", type: text}
        - {id: serial_command, label: "AT Command Mode",  type: checkbox}
        - {id: dns_use,        label: "DNS Enable",       type: checkbox}
```

**검증:** 작성 후 uv run python validate_schemas.py 실행, WIZ550SR.yaml 이 OK 로 출력되어야 함.
  </action>
  <verify>
    <automated>uv run python validate_schemas.py 2>&1 | grep -E "WIZ550SR|FAIL|All schemas"</automated>
  </verify>
  <acceptance_criteria>
    - specs/devices/WIZ550SR.yaml 파일 존재
    - uv run python validate_schemas.py 에서 "OK  WIZ550SR.yaml" 출력
    - grep 으로 'baud_rate:' 아래 choices 에 '115200:' 포함 (bps 정수키 확인)
    - grep 으로 'flow_control' choices 에 '1:' 키가 없음 (XON/XOFF 제외 확인)
    - grep 으로 'data_bits' 에 'type: readonly' 포함
    - grep 으로 'family: wiz550' 포함
    - grep 으로 'protocol_code: [0x02, 0x00, 0x00]' 또는 'product_code:' 아래 '0x02' 포함
    - uv run pytest tests/test_wiz550_spec.py::test_wiz550sr_schema_valid -v 가 PASS
    - uv run pytest tests/test_wiz550_spec.py::test_wiz550sr_field_ids_match_profile -v 가 PASS
  </acceptance_criteria>
  <done>WIZ550SR.yaml 이 validate_schemas.py 통과, field.id 가 parse_sr() 반환 키와 일치, baud_rate choices 키가 실제 bps 정수값.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WIZ550S2E.yaml 작성 (SPEC-02, per D-04/D-06)</name>
  <read_first>
    - WIZ550Profile.py — parse_s2e() 함수 전체 (MQTT 확장 필드: mqtt_pub_topic/mqtt_sub_topic, Modbus: modbus_use/modbus_mode)
    - .planning/phases/05-devicespec-yaml/05-CONTEXT.md — D-04 (condition 필드 의미), D-06 (S2E 섹션 구성)
    - specs/devices/WIZ550SR.yaml — SR YAML 을 기반으로 network/serial/options 섹션 복사
  </read_first>
  <files>specs/devices/WIZ550S2E.yaml</files>
  <behavior>
    - test_wiz550s2e_schema_valid: validate_schemas.py 종료 코드 0
    - test_wiz550s2e_conditional_sections: "mqtt" 및 "modbus" condition 섹션 모두 존재
    - test_wiz550s2e_mqtt_field_ids: mqtt_pub_topic, mqtt_sub_topic, mqtt_user, mqtt_pw 모두 존재
  </behavior>
  <action>
아래 내용으로 specs/devices/WIZ550S2E.yaml 을 작성한다.

**핵심 제약:**
1. MQTT field.id: `mqtt_pub_topic`, `mqtt_sub_topic` — publish_topic/subscribe_topic 절대 금지 (RESEARCH.md Pitfall 확인)
2. condition 값: 문자열 "mqtt", "modbus" (Phase 6 분기 키)
3. network/serial/options 섹션은 SR 과 동일 구조 (product_code 와 config_size 만 다름)
4. protocol.product_code: [0x00, 0x00, 0x00] — S2E 식별자

```yaml
# specs/devices/WIZ550S2E.yaml
# WIZ550S2E DeviceSpec — Phase 5 (SPEC-02)
# 가변 구조: 기본 162B + MQTT 70B(condition:mqtt) 또는 Modbus 2B(condition:modbus)
# field.id 기준: WIZ550Profile.parse_s2e() 반환 dict 키

name: WIZ550S2E
display_name: "WIZ550S2E"
aliases:
  - "WIZ550S2E"
family: wiz550
channels: 1

protocol:
  handler: WIZ550MSGHandler
  port: 6550
  product_code: [0x00, 0x00, 0x00]
  config_size: 162
  parser: parse_s2e
  builder: build_s2e

ui:
  sections:
    - id: network
      label: "Network"
      fields:
        - {id: mac,           label: "MAC Address",  type: readonly}
        - {id: dhcp_use,      label: "DHCP",         type: checkbox}
        - {id: local_ip,      label: "IP Address",   type: ip}
        - {id: subnet,        label: "Subnet Mask",  type: ip}
        - {id: gateway,       label: "Gateway",      type: ip}
        - {id: dns_server_ip, label: "DNS Server",   type: ip}
        - id: working_mode
          label: "Working Mode"
          type: dropdown
          choices:
            0: "TCP Client"
            1: "TCP Server"
            2: "UDP"
        - {id: local_port,  label: "Local Port",   type: uint16}
        - {id: remote_ip,   label: "Remote IP",    type: ip}
        - {id: remote_port, label: "Remote Port",  type: uint16}

    - id: serial
      label: "Serial"
      fields:
        - id: baud_rate
          label: "Baud Rate"
          type: dropdown
          choices:
            300: "300 bps"
            600: "600 bps"
            1200: "1200 bps"
            2400: "2400 bps"
            4800: "4800 bps"
            9600: "9600 bps"
            19200: "19200 bps"
            38400: "38400 bps"
            57600: "57600 bps"
            115200: "115200 bps"
            230400: "230400 bps"
            460800: "460800 bps"
        - {id: data_bits,    label: "Data Bits",    type: readonly, value: "8-bit"}
        - id: parity
          label: "Parity"
          type: dropdown
          choices:
            0: "None"
            1: "Odd"
            2: "Even"
        - id: stop_bits
          label: "Stop Bits"
          type: dropdown
          choices:
            0: "1 bit"
            1: "2 bits"
        - id: flow_control
          label: "Flow Control"
          type: dropdown
          choices:
            0: "None"
            2: "RTS/CTS"

    - id: options
      label: "Options"
      fields:
        - {id: pw_setting,     label: "Search Password",  type: text}
        - {id: pw_connect,     label: "Connect Password", type: text}
        - {id: serial_command, label: "AT Command Mode",  type: checkbox}
        - {id: dns_use,        label: "DNS Enable",       type: checkbox}

    - id: mqtt
      label: "MQTT"
      condition: mqtt
      fields:
        - {id: mqtt_user,      label: "MQTT Username",    type: text}
        - {id: mqtt_pw,        label: "MQTT Password",    type: text}
        - {id: mqtt_pub_topic, label: "Publish Topic",    type: text}
        - {id: mqtt_sub_topic, label: "Subscribe Topic",  type: text}

    - id: modbus
      label: "Modbus"
      condition: modbus
      fields:
        - {id: modbus_use,  label: "Modbus Enable", type: checkbox}
        - id: modbus_mode
          label: "Modbus Mode"
          type: dropdown
          choices:
            0: "RTU"
            1: "ASCII"
```

**주의:** condition 값은 Phase 6 분기 키로 사용된다. Phase 6 코드가 `section.get("condition") == "mqtt"` 로 판별.
  </action>
  <verify>
    <automated>uv run python validate_schemas.py 2>&1 | grep -E "WIZ550S2E|FAIL|All schemas" && uv run pytest tests/test_wiz550_spec.py::test_wiz550s2e_mqtt_field_ids -v</automated>
  </verify>
  <acceptance_criteria>
    - specs/devices/WIZ550S2E.yaml 파일 존재
    - uv run python validate_schemas.py 에서 "OK  WIZ550S2E.yaml" 출력
    - grep 으로 'mqtt_pub_topic' 포함 (publish_topic 이면 오류)
    - grep 으로 'mqtt_sub_topic' 포함 (subscribe_topic 이면 오류)
    - grep 으로 'condition: mqtt' 포함
    - grep 으로 'condition: modbus' 포함
    - uv run pytest tests/test_wiz550_spec.py::test_wiz550s2e_conditional_sections -v 가 PASS
    - uv run pytest tests/test_wiz550_spec.py::test_wiz550s2e_mqtt_field_ids -v 가 PASS
  </acceptance_criteria>
  <done>WIZ550S2E.yaml 이 validate_schemas.py 통과, condition:mqtt 섹션에 mqtt_pub_topic/mqtt_sub_topic 포함, condition:modbus 섹션에 modbus_use/modbus_mode 포함.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WIZ550WEB.yaml 작성 + 전체 검증 통과 (SPEC-03, SPEC-04, per D-05/D-06)</name>
  <read_first>
    - WIZ550Profile.py — parse_web() 반환 dict 전체 키 (uart0_*/uart1_* 필드명, pw_connect 없음 확인)
    - .planning/phases/05-devicespec-yaml/05-CONTEXT.md — D-05 (disabled 필드 목록), D-06 (WEB 섹션 구성)
    - .planning/phases/05-devicespec-yaml/05-RESEARCH.md — Pitfall 2 (pw_connect 포함 금지), Pitfall 4 (disabled 필드 SPEC-03)
  </read_first>
  <files>specs/devices/WIZ550WEB.yaml</files>
  <behavior>
    - test_wiz550web_schema_valid: validate_schemas.py 종료 코드 0
    - test_web_disabled_fields: working_mode/remote_ip/remote_port/local_port/serial_command 모두 disabled:true
    - test_web_no_pw_connect: pw_connect field.id 없음
    - test_web_uart0_uart1_sections: uart0, uart1 섹션 모두 존재
    - test_validate_schemas_all_pass: 3개 YAML 포함 후 전체 validate_schemas.py PASS
  </behavior>
  <action>
아래 내용으로 specs/devices/WIZ550WEB.yaml 을 작성한다.

**핵심 제약 (위반 시 SPEC-03 실패 또는 Phase 6 파괴):**
1. working_mode, remote_ip, remote_port, local_port, serial_command → disabled: true 로 포함 (SPEC-03)
2. pw_connect → YAML 에 포함하지 않음 (WEB 구조체에 없음 — Pitfall 2)
3. uart0_baud_rate, uart1_baud_rate → 별도 섹션(uart0, uart1)에 포함
4. protocol.product_code: [0x01, 0x02, 0x00] — WEB 식별자
5. protocol.config_size: 133 (WEB 구조체 크기)
6. channels: 2 (UART0/UART1 2채널)

```yaml
# specs/devices/WIZ550WEB.yaml
# WIZ550WEB DeviceSpec — Phase 5 (SPEC-03)
# 133B 구조체: network + UART0/UART1 시리얼 + pw_setting + dhcp/dns
# disabled:true 필드: working_mode/remote_ip/remote_port/local_port/serial_command (SPEC-03)
# pw_connect 없음 — WEB 구조체 미포함 (Pitfall 2)
# field.id 기준: WIZ550Profile.parse_web() 반환 dict 키

name: WIZ550WEB
display_name: "WIZ550WEB"
aliases:
  - "WIZ550WEB"
family: wiz550
channels: 2

protocol:
  handler: WIZ550MSGHandler
  port: 6550
  product_code: [0x01, 0x02, 0x00]
  config_size: 133
  parser: parse_web
  builder: build_web

ui:
  sections:
    - id: network
      label: "Network"
      fields:
        - {id: mac,           label: "MAC Address",  type: readonly}
        - {id: dhcp_use,      label: "DHCP",         type: checkbox}
        - {id: local_ip,      label: "IP Address",   type: ip}
        - {id: subnet,        label: "Subnet Mask",  type: ip}
        - {id: gateway,       label: "Gateway",      type: ip}
        - {id: dns_server_ip, label: "DNS Server",   type: ip}
        - {id: dns_use,       label: "DNS Enable",   type: checkbox}
        # WEB 구조체에 없는 필드 — disabled:true 로 명시 (SPEC-03)
        - id: working_mode
          label: "Working Mode"
          type: dropdown
          disabled: true
          choices:
            0: "TCP Client"
            1: "TCP Server"
            2: "UDP"
        - {id: remote_ip,   label: "Remote IP",   type: ip,     disabled: true}
        - {id: remote_port, label: "Remote Port", type: uint16, disabled: true}
        - {id: local_port,  label: "Local Port",  type: uint16, disabled: true}

    - id: uart0
      label: "UART0"
      fields:
        - id: uart0_baud_rate
          label: "Baud Rate"
          type: dropdown
          choices:
            300: "300 bps"
            600: "600 bps"
            1200: "1200 bps"
            2400: "2400 bps"
            4800: "4800 bps"
            9600: "9600 bps"
            19200: "19200 bps"
            38400: "38400 bps"
            57600: "57600 bps"
            115200: "115200 bps"
            230400: "230400 bps"
            460800: "460800 bps"
        - {id: uart0_data_bits,    label: "Data Bits",    type: readonly, value: "8-bit"}
        - id: uart0_parity
          label: "Parity"
          type: dropdown
          choices:
            0: "None"
            1: "Odd"
            2: "Even"
        - id: uart0_stop_bits
          label: "Stop Bits"
          type: dropdown
          choices:
            0: "1 bit"
            1: "2 bits"
        - id: uart0_flow_control
          label: "Flow Control"
          type: dropdown
          choices:
            0: "None"
            1: "XON/XOFF"
            2: "RTS/CTS"

    - id: uart1
      label: "UART1"
      fields:
        - id: uart1_baud_rate
          label: "Baud Rate"
          type: dropdown
          choices:
            300: "300 bps"
            600: "600 bps"
            1200: "1200 bps"
            2400: "2400 bps"
            4800: "4800 bps"
            9600: "9600 bps"
            19200: "19200 bps"
            38400: "38400 bps"
            57600: "57600 bps"
            115200: "115200 bps"
            230400: "230400 bps"
            460800: "460800 bps"
        - {id: uart1_data_bits,    label: "Data Bits",    type: readonly, value: "8-bit"}
        - id: uart1_parity
          label: "Parity"
          type: dropdown
          choices:
            0: "None"
            1: "Odd"
            2: "Even"
        - id: uart1_stop_bits
          label: "Stop Bits"
          type: dropdown
          choices:
            0: "1 bit"
            1: "2 bits"
        - id: uart1_flow_control
          label: "Flow Control"
          type: dropdown
          choices:
            0: "None"
            1: "XON/XOFF"
            2: "RTS/CTS"

    - id: options
      label: "Options"
      fields:
        - {id: pw_setting,     label: "Search Password", type: text}
        # pw_connect 없음 — WEB 구조체 미포함 (Pitfall 2)
        - {id: serial_command, label: "AT Command Mode", type: checkbox, disabled: true}
```

**WEB 의 flow_control:** WEB 구조체(WEB_FORMAT 'B') 는 값 범위가 명시되지 않음. SR/S2E 와 달리 XON/XOFF(1) 를 포함한 3개 choices 로 작성 (Assumption A2). 실물 장치 확인 전 포함이 더 안전.

**작성 완료 후 최종 검증 실행:**
```
uv run python validate_schemas.py
uv run pytest tests/test_wiz550_spec.py -v
```
양쪽 모두 PASS 여야 Task 완료.
  </action>
  <verify>
    <automated>uv run python validate_schemas.py && uv run pytest tests/test_wiz550_spec.py -v</automated>
  </verify>
  <acceptance_criteria>
    - specs/devices/WIZ550WEB.yaml 파일 존재
    - uv run python validate_schemas.py 에서 "OK  WIZ550WEB.yaml", "OK  WIZ550SR.yaml", "OK  WIZ550S2E.yaml", "All schemas valid." 출력 — 종료 코드 0
    - grep 으로 'disabled: true' 가 WIZ550WEB.yaml 에 5개 이상 포함 (working_mode/remote_ip/remote_port/local_port/serial_command)
    - grep 으로 'pw_connect' 가 WIZ550WEB.yaml 에 없음
    - grep 으로 'uart0_baud_rate' 포함
    - grep 으로 'uart1_baud_rate' 포함
    - uv run pytest tests/test_wiz550_spec.py::test_web_disabled_fields -v 가 PASS
    - uv run pytest tests/test_wiz550_spec.py::test_web_no_pw_connect -v 가 PASS
    - uv run pytest tests/test_wiz550_spec.py::test_web_uart0_uart1_sections -v 가 PASS
    - uv run pytest tests/test_wiz550_spec.py -v 에서 전체 PASSED (SKIP 없음)
  </acceptance_criteria>
  <done>3개 YAML 모두 validate_schemas.py 통과, pytest 전체 PASS, WEB disabled 필드 5개 모두 포함, pw_connect 없음 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML 파일 → Phase 6 코드 | YAML field.id 가 parse_*() 반환 dict 키와 일치해야 함 — 이름 불일치 시 Phase 6 KeyError |
| choices 키 → Phase 6 UI | baud_rate choices 키 타입(int vs str) 불일치 시 QComboBox 선택 실패 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | YAML field.id 와 parse_*() 키 불일치 | mitigate | test_wiz550sr_field_ids_match_profile 테스트로 자동 검증 |
| T-05-01-02 | Tampering | baud_rate choices 키 인덱스/bps 혼용 | mitigate | Task 1 acceptance_criteria 에서 '115200:' grep 으로 확인 |
| T-05-01-03 | Tampering | WEB 에 pw_connect 포함 | mitigate | test_web_no_pw_connect 테스트 + Task 3 acceptance_criteria grep 확인 |
| T-05-01-04 | Information Disclosure | YAML 에 실제 비밀번호/자격증명 포함 | accept | YAML 은 UI 구조 정의용 — 실제 값 없음 |
</threat_model>

<verification>
Phase 5 완료 조건:
1. `uv run python validate_schemas.py` → "All schemas valid." 종료 코드 0
2. `uv run pytest tests/test_wiz550_spec.py -v` → 전체 PASS (8개 테스트)
3. 3개 YAML 파일 존재: WIZ550SR.yaml, WIZ550S2E.yaml, WIZ550WEB.yaml
4. WIZ550SR.yaml: baud_rate choices 키가 실제 bps 정수값 (115200: "115200 bps" 형태)
5. WIZ550S2E.yaml: condition:mqtt 섹션에 mqtt_pub_topic, mqtt_sub_topic 포함
6. WIZ550WEB.yaml: working_mode/remote_ip/remote_port/local_port/serial_command 가 disabled:true
7. WIZ550WEB.yaml: pw_connect field.id 없음
</verification>

<success_criteria>
- SPEC-01: WIZ550SR.yaml 스키마 통과, parse_sr() 필드명 일치
- SPEC-02: WIZ550S2E.yaml 스키마 통과, MQTT/Modbus condition 섹션 완비
- SPEC-03: WIZ550WEB.yaml 스키마 통과, disabled 필드 5개 명시, pw_connect 없음
- SPEC-04: validate_schemas.py 전체 통과 (종료 코드 0)
- pytest 8개 테스트 전체 PASS
</success_criteria>

<output>
완료 후: .planning/phases/05-devicespec-yaml/05-01-SUMMARY.md 생성
포함 내용: 3개 YAML 섹션 구성 요약, 필드 수, validate_schemas.py 출력, pytest 결과
</output>
