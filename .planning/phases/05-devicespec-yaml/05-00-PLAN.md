---
phase: 05-devicespec-yaml
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - specs/schema/device.wiz550.schema.json
  - tests/test_wiz550_spec.py
  - validate_schemas.py
autonomous: true
requirements:
  - SPEC-04

must_haves:
  truths:
    - "uv run python validate_schemas.py 가 종료 코드 0으로 실행된다 (WIZ550 YAML 없어도 오류 없음)"
    - "tests/test_wiz550_spec.py 가 존재하며 pytest가 수집한다 (SKIP 또는 PASS 상태)"
    - "device.wiz550.schema.json 가 family: wiz550 필드를 const로 강제한다"
  artifacts:
    - path: "specs/schema/device.wiz550.schema.json"
      provides: "WIZ550 전용 JSON Schema"
      contains: "\"const\": \"wiz550\""
    - path: "tests/test_wiz550_spec.py"
      provides: "SPEC-03/04 검증 테스트 스텁"
      exports: ["test_wiz550sr_schema_valid", "test_wiz550s2e_schema_valid", "test_wiz550web_schema_valid", "test_web_disabled_fields"]
    - path: "validate_schemas.py"
      provides: "family 기반 스키마 라우팅"
      contains: "wiz550_schema"
  key_links:
    - from: "validate_schemas.py"
      to: "specs/schema/device.wiz550.schema.json"
      via: "json.loads + Path.read_text"
      pattern: "device.wiz550.schema.json"
    - from: "validate_schemas.py"
      to: "family == \"wiz550\" 분기"
      via: "data.get(\"family\")"
      pattern: "family.*wiz550"
---

<objective>
Wave 0 사전 인프라: WIZ550 전용 JSON Schema, pytest 테스트 스텁, validate_schemas.py 라우팅 추가.

Purpose: Wave 1의 YAML 파일 3개가 즉시 스키마 검증을 받을 수 있도록 검증 인프라를 먼저 구축한다 (SPEC-04).
Output: device.wiz550.schema.json, tests/test_wiz550_spec.py, validate_schemas.py 수정본.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-devicespec-yaml/05-CONTEXT.md
@.planning/phases/05-devicespec-yaml/05-RESEARCH.md

<interfaces>
<!-- validate_schemas.py 기존 구조 (수정 대상) -->
<!-- validate_all() 내부 device YAML 루프 — 라우팅 분기 삽입 위치 -->

From validate_schemas.py (현재):
```python
def validate_all() -> None:
    device_schema = json.loads((SCHEMA_DIR / "device.schema.json").read_text(encoding="utf-8"))
    cmd_schema    = json.loads((SCHEMA_DIR / "command-group.schema.json").read_text(encoding="utf-8"))
    ...
    for f in sorted((SPECS_DIR / "devices").glob("*.yaml")):
        data = yaml.safe_load(f.read_text(encoding="utf-8"))
        jsonschema.validate(instance=data, schema=device_schema)  # ← 이 줄만 수정
```

수정 후:
```python
    device_schema = json.loads((SCHEMA_DIR / "device.schema.json").read_text(encoding="utf-8"))
    wiz550_schema = json.loads((SCHEMA_DIR / "device.wiz550.schema.json").read_text(encoding="utf-8"))  # 추가
    ...
    for f in sorted((SPECS_DIR / "devices").glob("*.yaml")):
        data = yaml.safe_load(f.read_text(encoding="utf-8"))
        schema = wiz550_schema if data.get("family") == "wiz550" else device_schema  # 추가
        jsonschema.validate(instance=data, schema=schema)  # schema 변수로 교체
```

From specs/schema/device.schema.json (참조 패턴):
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "...",
  "type": "object",
  "required": [...],
  "additionalProperties": false,
  "properties": {...}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: device.wiz550.schema.json 신규 작성 (per D-01)</name>
  <read_first>
    - specs/schema/device.schema.json — $schema, title 패턴 및 additionalProperties 구조 확인
    - .planning/phases/05-devicespec-yaml/05-RESEARCH.md — Architecture Patterns 섹션 1 (스키마 전체 구조)
    - .planning/phases/05-devicespec-yaml/05-CONTEXT.md — D-01, D-03 (field type enum 목록)
  </read_first>
  <files>specs/schema/device.wiz550.schema.json</files>
  <action>
아래 구조대로 specs/schema/device.wiz550.schema.json 을 신규 작성한다.

**최상위 required 필드:** name, family, channels, protocol, ui
**additionalProperties: false** — display_name, aliases 는 optional

**protocol 객체 required:** handler, port, product_code, config_size, parser, builder

**ui 객체 required:** sections (배열)
**section 객체 required:** id, label, fields (condition 은 optional string)

**field 정의 ($ref 사용):**
- required: id, label, type
- additionalProperties: false
- type enum: ["ip", "text", "uint16", "dropdown", "checkbox", "readonly"]
- optional: choices (object, 정수키 → 문자열값), disabled (boolean), value (any)

**family const:** "wiz550" — 기존 device.schema.json 의 enum 목록과 분리

최종 JSON 구조 (이 그대로 작성):
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WIZnet WIZ550 Device Spec",
  "description": "Schema for specs/devices/WIZ550*.yaml files. Binary protocol devices without command_groups.",
  "type": "object",
  "required": ["name", "family", "channels", "protocol", "ui"],
  "additionalProperties": false,
  "properties": {
    "name":         {"type": "string"},
    "display_name": {"type": "string"},
    "aliases":      {"type": "array", "items": {"type": "string"}},
    "family":       {"type": "string", "const": "wiz550"},
    "channels":     {"type": "integer", "minimum": 1, "maximum": 2},
    "protocol": {
      "type": "object",
      "required": ["handler", "port", "product_code", "config_size", "parser", "builder"],
      "additionalProperties": false,
      "properties": {
        "handler":      {"type": "string"},
        "port":         {"type": "integer"},
        "product_code": {"type": "array", "items": {"type": "integer"}, "minItems": 3, "maxItems": 3},
        "config_size":  {"type": "integer"},
        "parser":       {"type": "string"},
        "builder":      {"type": "string"}
      }
    },
    "ui": {
      "type": "object",
      "required": ["sections"],
      "additionalProperties": false,
      "properties": {
        "sections": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["id", "label", "fields"],
            "additionalProperties": false,
            "properties": {
              "id":        {"type": "string"},
              "label":     {"type": "string"},
              "condition": {"type": "string"},
              "fields":    {
                "type": "array",
                "items": {"$ref": "#/definitions/field"}
              }
            }
          }
        }
      }
    }
  },
  "definitions": {
    "field": {
      "type": "object",
      "required": ["id", "label", "type"],
      "additionalProperties": false,
      "properties": {
        "id":       {"type": "string"},
        "label":    {"type": "string"},
        "type":     {"type": "string", "enum": ["ip", "text", "uint16", "dropdown", "checkbox", "readonly"]},
        "choices":  {"type": "object", "additionalProperties": {"type": "string"}},
        "disabled": {"type": "boolean"},
        "value":    {}
      }
    }
  }
}
```

**주의:** choices 키가 YAML에서 정수로 파싱되므로 additionalProperties 는 type:string 만 검증한다. 정수키 → 문자열값 구조는 YAML에서 자동 처리.
  </action>
  <verify>
    <automated>uv run python -c "import json; s=json.load(open('specs/schema/device.wiz550.schema.json')); assert s['properties']['family']['const']=='wiz550'; assert 'readonly' in s['definitions']['field']['properties']['type']['enum']; print('schema OK')"</automated>
  </verify>
  <acceptance_criteria>
    - specs/schema/device.wiz550.schema.json 파일 존재
    - grep 으로 '"const": "wiz550"' 문자열 포함 확인
    - grep 으로 '"readonly"' 가 type enum 에 포함 확인
    - grep 으로 '"value": {}' 가 field definitions 에 포함 확인 (Pitfall 3 예방)
    - python -c import json; json.load(open(...)) 가 예외 없이 실행됨 (JSON 문법 오류 없음)
  </acceptance_criteria>
  <done>specs/schema/device.wiz550.schema.json 이 존재하며 JSON 파싱 성공. family const=wiz550, field type enum 6개, value optional 모두 포함.</done>
</task>

<task type="auto">
  <name>Task 2: validate_schemas.py 라우팅 + tests/test_wiz550_spec.py 스텁 (per D-01, SPEC-04)</name>
  <read_first>
    - validate_schemas.py — validate_all() 함수 전체 구조, SCHEMA_DIR 경로 변수 위치
    - .planning/phases/05-devicespec-yaml/05-RESEARCH.md — Architecture Patterns 섹션 2 (라우팅 변경 패턴), Common Pitfalls Pitfall 5
    - tests/ 디렉토리 내 기존 test 파일 확인 (conftest.py 패턴 파악)
  </read_first>
  <files>validate_schemas.py, tests/test_wiz550_spec.py</files>
  <action>
**[A] validate_schemas.py 수정 (최소 변경, 4줄 추가/수정):**

validate_all() 함수에서 device_schema 로드 직후(cmd_schema 로드 전) wiz550_schema 를 추가 로드하고,
device YAML 루프 내 jsonschema.validate 호출 시 단일 device_schema 대신 family 분기로 스키마 선택:

변경 전 (line 30):
```python
device_schema = json.loads((SCHEMA_DIR / "device.schema.json").read_text(encoding="utf-8"))
cmd_schema = json.loads((SCHEMA_DIR / "command-group.schema.json").read_text(encoding="utf-8"))
```
변경 후:
```python
device_schema = json.loads((SCHEMA_DIR / "device.schema.json").read_text(encoding="utf-8"))
wiz550_schema = json.loads((SCHEMA_DIR / "device.wiz550.schema.json").read_text(encoding="utf-8"))
cmd_schema = json.loads((SCHEMA_DIR / "command-group.schema.json").read_text(encoding="utf-8"))
```

변경 전 (device YAML 루프 내 validate 호출):
```python
jsonschema.validate(instance=data, schema=device_schema)
```
변경 후:
```python
schema = wiz550_schema if data.get("family") == "wiz550" else device_schema
jsonschema.validate(instance=data, schema=schema)
```

기존 WIZ750SR.yaml 등의 family 는 "one_port" 이므로 영향 없음. 수정 후 uv run python validate_schemas.py 가 종료 코드 0 이어야 한다.

**[B] tests/test_wiz550_spec.py 신규 작성 (스텁 — Wave 1 YAML 작성 후 PASS 예정):**

아래 구조로 작성. YAML 파일이 없을 때는 pytest.skip 으로 건너뛴다:

```python
"""
tests/test_wiz550_spec.py — Phase 5 WIZ550 DeviceSpec YAML 검증 테스트

SPEC-01: WIZ550SR.yaml 스키마 + 필드명 검증
SPEC-02: WIZ550S2E.yaml 스키마 + 조건부 섹션 검증
SPEC-03: WIZ550WEB.yaml disabled 필드 존재 검증
SPEC-04: validate_schemas.py 전체 통과 (subprocess)
"""
import subprocess
import sys
from pathlib import Path

import pytest
import yaml

SPECS_DIR = Path(__file__).parent.parent / "specs" / "devices"
SR_YAML   = SPECS_DIR / "WIZ550SR.yaml"
S2E_YAML  = SPECS_DIR / "WIZ550S2E.yaml"
WEB_YAML  = SPECS_DIR / "WIZ550WEB.yaml"


def _load_yaml(path: Path) -> dict:
    if not path.exists():
        pytest.skip(f"{path.name} not yet created")
    return yaml.safe_load(path.read_text(encoding="utf-8"))


# ── SPEC-01 ──────────────────────────────────────────────────────────────────

def test_wiz550sr_schema_valid():
    """WIZ550SR.yaml 이 validate_schemas.py 검증을 통과한다."""
    _load_yaml(SR_YAML)
    result = subprocess.run(
        [sys.executable, "validate_schemas.py"],
        capture_output=True, text=True, cwd=Path(__file__).parent.parent
    )
    assert result.returncode == 0, f"validate_schemas.py failed:\n{result.stdout}\n{result.stderr}"


def test_wiz550sr_field_ids_match_profile():
    """WIZ550SR YAML field.id 가 WIZ550Profile.parse_sr() 반환 dict 키와 일치한다."""
    data = _load_yaml(SR_YAML)
    all_field_ids = {
        f["id"]
        for section in data["ui"]["sections"]
        for f in section["fields"]
        if not f.get("disabled")
    }
    # parse_sr 반환 dict 의 UI 관련 키 (내부용 제외)
    # inactivity/reconnection: WIZ550Profile._parse_base_162() 반환 필드 — options 섹션에 포함
    expected_ui_keys = {
        "mac", "local_ip", "gateway", "subnet", "working_mode",
        "remote_ip", "local_port", "remote_port",
        "baud_rate", "data_bits", "parity", "stop_bits", "flow_control",
        "pw_setting", "pw_connect", "dhcp_use", "dns_use",
        "dns_server_ip", "serial_command",
        "inactivity", "reconnection",
    }
    missing = expected_ui_keys - all_field_ids
    assert not missing, f"SR YAML 에서 누락된 field.id: {missing}"


# ── SPEC-02 ──────────────────────────────────────────────────────────────────

def test_wiz550s2e_schema_valid():
    """WIZ550S2E.yaml 이 validate_schemas.py 검증을 통과한다."""
    _load_yaml(S2E_YAML)
    result = subprocess.run(
        [sys.executable, "validate_schemas.py"],
        capture_output=True, text=True, cwd=Path(__file__).parent.parent
    )
    assert result.returncode == 0, f"validate_schemas.py failed:\n{result.stdout}"


def test_wiz550s2e_conditional_sections():
    """WIZ550S2E.yaml 에 condition=mqtt 섹션과 condition=modbus 섹션이 존재한다."""
    data = _load_yaml(S2E_YAML)
    conditions = {s.get("condition") for s in data["ui"]["sections"]}
    assert "mqtt" in conditions, "S2E YAML 에 condition=mqtt 섹션 없음"
    assert "modbus" in conditions, "S2E YAML 에 condition=modbus 섹션 없음"


def test_wiz550s2e_mqtt_field_ids():
    """WIZ550S2E YAML MQTT 섹션의 field.id 가 parse_s2e() 반환 키와 일치한다."""
    data = _load_yaml(S2E_YAML)
    mqtt_section = next(
        (s for s in data["ui"]["sections"] if s.get("condition") == "mqtt"), None
    )
    assert mqtt_section is not None
    mqtt_ids = {f["id"] for f in mqtt_section["fields"]}
    assert "mqtt_pub_topic" in mqtt_ids, "mqtt_pub_topic 없음 (publish_topic 이면 오류)"
    assert "mqtt_sub_topic" in mqtt_ids, "mqtt_sub_topic 없음 (subscribe_topic 이면 오류)"
    assert "mqtt_user" in mqtt_ids
    assert "mqtt_pw" in mqtt_ids


# ── SPEC-03 ──────────────────────────────────────────────────────────────────

def test_wiz550web_schema_valid():
    """WIZ550WEB.yaml 이 validate_schemas.py 검증을 통과한다."""
    _load_yaml(WEB_YAML)
    result = subprocess.run(
        [sys.executable, "validate_schemas.py"],
        capture_output=True, text=True, cwd=Path(__file__).parent.parent
    )
    assert result.returncode == 0, f"validate_schemas.py failed:\n{result.stdout}"


def test_web_disabled_fields():
    """WIZ550WEB YAML 에 disabled:true 필드 5개가 명시적으로 포함된다 (SPEC-03)."""
    data = _load_yaml(WEB_YAML)
    disabled_ids = {
        f["id"]
        for section in data["ui"]["sections"]
        for f in section["fields"]
        if f.get("disabled") is True
    }
    required_disabled = {"working_mode", "remote_ip", "remote_port", "local_port", "serial_command"}
    missing = required_disabled - disabled_ids
    assert not missing, f"WEB YAML 에서 disabled:true 미포함 필드: {missing}"


def test_web_no_pw_connect():
    """WIZ550WEB YAML 에 pw_connect 필드가 없다 (WEB 구조체 미포함, Pitfall 2)."""
    data = _load_yaml(WEB_YAML)
    all_ids = {f["id"] for s in data["ui"]["sections"] for f in s["fields"]}
    assert "pw_connect" not in all_ids, "WEB YAML 에 pw_connect 포함됨 — 구조체에 없음"


def test_web_uart0_uart1_sections():
    """WIZ550WEB YAML 에 uart0 섹션과 uart1 섹션이 존재한다."""
    data = _load_yaml(WEB_YAML)
    section_ids = {s["id"] for s in data["ui"]["sections"]}
    assert "uart0" in section_ids, "WEB YAML 에 uart0 섹션 없음"
    assert "uart1" in section_ids, "WEB YAML 에 uart1 섹션 없음"


# ── SPEC-04 ──────────────────────────────────────────────────────────────────

def test_validate_schemas_all_pass():
    """validate_schemas.py 전체가 종료 코드 0 으로 통과한다."""
    result = subprocess.run(
        [sys.executable, "validate_schemas.py"],
        capture_output=True, text=True, cwd=Path(__file__).parent.parent
    )
    assert result.returncode == 0, (
        f"validate_schemas.py 실패:\n{result.stdout}\n{result.stderr}"
    )
```

**주의:** Wave 1 YAML 파일이 없을 때 test_wiz550sr_schema_valid 등은 pytest.skip 으로 건너뜀. validate_schemas.py 전체 통과 테스트는 YAML 파일 3개 작성 후 PASS 됨.
  </action>
  <verify>
    <automated>uv run python validate_schemas.py && uv run pytest tests/test_wiz550_spec.py -v --collect-only 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - validate_schemas.py 가 종료 코드 0 으로 실행됨 (기존 YAML 검증 그대로, WIZ550 YAML 없어도 오류 없음)
    - grep 으로 validate_schemas.py 에 'wiz550_schema' 포함 확인
    - grep 으로 validate_schemas.py 에 'data.get("family") == "wiz550"' 포함 확인
    - tests/test_wiz550_spec.py 파일 존재
    - uv run pytest tests/test_wiz550_spec.py --collect-only 가 8개 이상의 테스트를 수집 (SKIP 포함)
    - uv run pytest tests/test_wiz550_spec.py -v 가 오류(ERROR) 없이 실행됨 (SKIP 또는 PASS)
    - expected_ui_keys 에 "inactivity", "reconnection" 포함 확인
  </acceptance_criteria>
  <done>validate_schemas.py 수정 완료 (wiz550_schema 로드 + family 분기), tests/test_wiz550_spec.py 8개 테스트 스텁 존재 (inactivity/reconnection 포함), validate_schemas.py 종료 코드 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파일 시스템 → Python | YAML/JSON 파일 읽기 — 악의적 YAML 콘텐츠 가능성 (yaml.safe_load 사용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | validate_schemas.py YAML 로드 | accept | yaml.safe_load 이미 사용 중 — 코드 실행 불가 |
| T-05-00-02 | Denial | device.wiz550.schema.json 미존재 시 validate_all() 크래시 | mitigate | Task 1 에서 스키마 파일 먼저 생성, Task 2 에서 수정 — 순서 보장 |
</threat_model>

<verification>
Wave 0 완료 조건:
1. `uv run python validate_schemas.py` 종료 코드 0
2. `uv run pytest tests/test_wiz550_spec.py --collect-only` 가 ERROR 없이 8개 이상 수집
3. specs/schema/device.wiz550.schema.json 에 `"const": "wiz550"` 포함
4. validate_schemas.py 에 `wiz550_schema` 및 `data.get("family") == "wiz550"` 포함
5. tests/test_wiz550_spec.py 의 expected_ui_keys 에 `inactivity`, `reconnection` 포함
</verification>

<success_criteria>
- specs/schema/device.wiz550.schema.json 존재, JSON 문법 오류 없음
- validate_schemas.py 가 family=wiz550 YAML 을 wiz550 스키마로 라우팅
- tests/test_wiz550_spec.py 가 pytest 에 수집됨 (SKIP/PASS, ERROR 없음)
- 기존 WIZ750SR.yaml 등 검증 흐름 변경 없음 (validate_schemas.py 그린)
</success_criteria>

<output>
완료 후: .planning/phases/05-devicespec-yaml/05-00-SUMMARY.md 생성
포함 내용: 생성 파일 목록, validate_schemas.py 변경 줄 수, pytest 수집 테스트 수
</output>
